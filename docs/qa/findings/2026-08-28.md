# QA findings — 28 August 2026

Requested directly: a complete mobile end-to-end pass of `https://qa.fleetsettle.com/` covering UI/UX, access control, workflow correctness, financial accuracy, and persisted data. This pass used the real username/password identity flow, the QA Neon branch, three application roles, four mobile viewport shapes, live writes, report-to-database reconciliation, CSV export, and the repository's full automated quality gate.

## Executive summary

The application is broadly usable end to end on mobile and the core accounting writes tested here persist correctly. Vehicle, driver, customer, monthly lease, daily lease, trip, expense, advance, deposit, payment, incident, insurance, banking, report, export, admin, member, and linked-driver flows were all reached in the deployed QA build. The synthetic business story balanced to the cent in the database and in the screens that read it.

Five new defects were confirmed and filed as **GAP-191…GAP-195**. GAP-190 is already reserved by the user-owned `fix/gap-190-write-guards-and-loan-payment` work present in this workspace:

| ID | Severity | Finding | User impact |
|---|---|---|---|
| **GAP-191** | **P1 — money direction** | A trip detail read can return its driver-fee payable as the customer receivable | A fully paid customer trip can still say `Due Rs 25` and open `Collect payment`, inviting collection of money the customer does not owe |
| **GAP-192** | **P2 — statement accuracy** | Driver statement days are not ordered chronologically | A printed/read statement presents 23 Aug before 22 Aug after already showing 24–28 Aug, making the ledger hard to audit |
| **GAP-193** | **P2 — money rounding** | Utilisation truncates rather than half-up rounding | Rs 33,000 / 90 available days displays Rs 366.66 instead of Rs 366.67 |
| **GAP-194** | **P2 — mobile interaction regression** | The sticky primary action still covers ordinary content on a tall phone | At 384×824, `Collect payment` covers most of `Show all (5)`; taps at the top and centre hit the payment action |
| **GAP-195** | **P3 — accessibility** | An opening-balance remove button shrinks below the project's 44 px floor | A long party label compresses the icon target to 35×44 at 360 px |

There were **no console errors** on the audited routes. The repository gate passes with a larger Node heap: guard, CSS lint, formatting, typecheck, **148 web files / 897 tests**, **3 API files / 10 tests**, and **13 shared files / 99 tests**. ESLint reports **66 existing warnings, 0 errors**. The first default-heap run stopped in ESLint at Node's 4 GB heap limit; that was an infrastructure failure, not a failing check.

## Environment and method

- Target: `qa.fleetsettle.com`, real deployed QA build.
- Identity: real Asgardeo username/password flow; no auth stub. Credentials came from the repository-local `testCred.md` and were never printed or copied into this report.
- Business: `TESTA` (`019fd4c9-0360-7655-9e48-7451b488e818`), LKR, `Asia/Colombo`.
- Database: QA Neon project `spring-sunset-96946055`, branch `br-square-sound-afb68wft`, read-only SQL cross-checks after each group of writes.
- Roles checked:
  - owner-manager/platform admin on `TESTA`;
  - manager on `TestBusinesByChamath`;
  - linked driver through the second supplied credential.
- Viewports:
  - 360×640 — primary acceptance viewport;
  - 320×640 — WCAG reflow edge;
  - 384×824 — tall-phone sticky-action regression check;
  - 640×360 — landscape mobile.
- Theme: the in-app browser exposed the host's live dark colour scheme. Chrome/light-scheme automation was unavailable in this environment, so light mode was not claimed as a live-browser pass. Token and automated tests still passed.
- Every audited route was checked for document overflow, interactive target size, nested overflow, visible clipping, and console errors/warnings. Financial results were then reconciled against QA Postgres rather than trusted from the UI alone.

## Coverage matrix

| Area | Live coverage | Result |
|---|---|---|
| Authentication and business selection | Username/password sign-in, callback, business chooser, switch between two businesses, sign-out affordance | Pass |
| Role boundaries | Owner-manager, manager, linked driver; direct route attempts to `/`, `/vehicles`, `/more`, `/reports`, `/admin`; owner-only period close; platform admin | Pass. Linked driver is forced to `/me`; manager is denied owner-only close. A directly typed manager `/review` route briefly renders empty sections before the protected read fails, but no protected data was exposed |
| Home and attention | Current-day cards, earlier days, rent due, trips, expected income, paperwork warning, deposits to release, attention bell destinations | Pass |
| Vehicles | List, add vehicle, detail, arrangement, daily/monthly/trip entry points, calendar, paperwork, service interval, expense, history | Pass |
| Drivers and customers | Create, list, detail, balances, payments, advances, deposits, offsets, statements/history, archive affordances | Pass except GAP-192 and customer statement remains the existing phase-2 GAP-170 scope |
| Monthly lease | Start, customer creation/selection, rent, deposit, mileage options, payment, early close, same-day pro-ration, deposit refund, close-out | Pass; Rs 100 / 31 days = Rs 3.23 displayed and persisted correctly |
| Daily lease | Start, day pattern/rate, confirm earned vs received, end lease | Pass; Rs 20 earned / Rs 15 received remained two independent facts |
| Trips | Book, driver fee, customer fare, advance, costs, close, payment, payout, P&L/readback | Writes pass; trip detail receivable read fails in GAP-191 |
| Costs | Fuel, other expense, borne-by/paid-by, vehicle/trip/incident links, receipt affordance, void sheet reachability | Pass for create/read. Final void was held for action-time confirmation because it is destructive |
| Incidents | Report, off-road day, repair, insurer claim/excess/settlement, customer contribution affordance, close | Pass. Date-only incident creation is intentional level-1 behaviour, not a defect |
| Cash and partners | Cash screen, banking event, ownership/management setup, capital/payout affordances, partner detail | Pass |
| Opening balances | Entry creation, party selection, draft/confirm screen, remove control, existing materialised positions | Functional pass; GAP-195 target-size failure |
| Review | This month, money, vehicles, per-vehicle review | Pass for owner-manager; manager permissions enforced by API |
| Reports | Vehicle month, lost days, trips, fuel efficiency, receivables, cash position, distributable cash, ageing, goodwill, utilisation, vehicle year | Pass except GAP-193 |
| CSV export | 1 Jan–28 Aug export, browser download, file header, row count, synthetic rows | Pass: 34 data rows plus header, correct decimal Rs format and `Belongs to` column |
| Members/admin | Member list/invite/role controls, admin home, users, businesses, requests, audit log, management | Pass for reachable read/form paths |
| Period close/corrections | Checklist, warning counts, recent-payment correction sheets, permanent close, successor-period creation | Pass: September closed and October opened atomically; destructive void-and-replace stopped at its confirmation boundary |
| Messaging/WhatsApp | Navigation/source status | Not built by design: P14 is deferred/blocked on the product decision and Meta templates; not re-filed |

## Synthetic end-to-end story and persisted facts

Created only in `TESTA`, with `QA-E2E-0828-*` names:

- trip vehicle `QA-E2E-0828-A`, driver `QA E2E Driver 0828`, customer `QA E2E Customer 0828`;
- closed trip: fare Rs 123.45, driver fee Rs 25, fuel Rs 10.50, other cost Rs 10.50;
- Rs 5 advance issued and settled; Rs 123.45 customer payment; Rs 5 offset and Rs 20 driver payout;
- monthly vehicle `QA-E2E-0828-L`: Rs 100 rent, Rs 50 deposit, same-day early close, Rs 3.23 prorated rent, full deposit refund, Rs 3.23 payment;
- daily vehicle `QA-E2E-0828-D`: Rs 30/day lease, day recorded as Rs 20 earned / Rs 15 received, then ended;
- closed incident: Rs 12 repair, one off-road day, Rs 8 insurance claim, Rs 2 excess, Rs 6 received.

The transaction CSV reproduced those rows as:

| Date | Vehicle | Type | Direction | Amount | Belongs to |
|---|---|---|---|---:|---|
| 2026-08-28 | QA-E2E-0828-D | Daily lease amount | In | Rs 20.00 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-L | Rent | In | Rs 3.23 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-A | Driver fee | Out | Rs 25.00 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-A | Trip income | In | Rs 123.45 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-A | Fuel | Out | Rs 10.50 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-A | Other | Out | Rs 10.50 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-D | Repairs | Out | Rs 12.00 | 2026-08-01 |
| 2026-08-28 | QA-E2E-0828-D | Insurance settlement | In | Rs 6.00 | — |

The `Belongs to` values are expected: this heavily-used QA business already has August closed and September open, so late 28 Aug facts post into the open period while retaining August attribution.

## Accuracy reconciliation

| Figure | UI/export result | Independent database/arithmetic check | Verdict |
|---|---:|---:|---|
| Monthly same-day pro-ration | Rs 3.23 | Rs 100.00 / 31 = Rs 3.2258…, half-up = Rs 3.23 | Pass |
| Vehicle-year earned | Rs 38,011.29 | QA report/query | Pass |
| Vehicle-year spent | Rs 10,507.76 | QA report/query | Pass |
| Vehicle-year profit | Rs 27,503.53 | 38,011.29 − 10,507.76 | Pass |
| Receivables | Rs 10,000 | Ageing 1,000 + 2,000 + 3,000 + 4,000 | Pass |
| Distributable | Rs 44,199.79 | cash 50,199.79 − deposits 6,000 − loan due 0 | Pass |
| Trip fare obligation | Rs 123.45 paid | `owed_to_us`, `trip_fare`, 12,345 minor, settled 12,345 | Pass in ledger |
| Trip driver fee obligation | Rs 25 paid | `owed_by_us`, `driver_fee`, 2,500 minor, settled 2,500 | Pass in ledger; wrong row can be chosen for trip detail, GAP-191 |
| Utilisation revenue/day | **Rs 366.66** | 3,300,000 minor / 90 available days = 36,666.66… minor; half-up = **36,667** | **Fail — GAP-193** |

## Confirmed findings

### GAP-191 — trip detail confuses payable and receivable (P1)

**Reproduction.** Close a trip that has both a customer fare and a driver fee. Pay the customer fare in full while leaving the driver fee unpaid. Re-open trip detail. The `Received` row renders `Due Rs 25` and opens `Collect payment`, even though the only Rs 25 due is money the business owes the driver. During this pass the live database held both obligations against the same trip source:

- `trip_fare`, `owed_to_us`, Rs 123.45, paid;
- `driver_fee`, `owed_by_us`, Rs 25, initially pending.

**Root cause.** `api/src/queries/obligation.ts:105` `findObligationBySource` filters only `source_type`, `source_id`, and `voided_at`, then returns one row. It does not constrain direction or kind. `api/src/handlers/trip.ts:229` calls that generic function and labels whichever row it receives as `receivable`. A trip is the first source in this flow that legitimately raises two opposite-direction obligations, so the old one-row assumption is false.

**Expected.** Trip detail's customer collection row must select only `direction = 'owed_to_us'` and the trip-fare kind; driver fee belongs in the separate driver-payment side.

### GAP-192 — driver statement days are out of order (P2)

**Reproduction.** Open Sunil's statement for 29 Jul–28 Aug. The live page displayed 24–28 Aug first, then 10–21 Aug, then 23 Aug before 22 Aug. There were no section labels or grouping that could explain the order.

**Root cause.** `api/src/queries/day-record.ts:305` `listDayRecordsForDriver` has no `ORDER BY business_date`; `web/src/features/people/DriverActivitySections.tsx:107` maps `view.days` as received without sorting.

**Expected.** A statement should use one explicit chronological rule, preferably newest-first to match the surrounding history screens, and the API should guarantee it.

### GAP-193 — utilisation money is truncated (P2)

**Reproduction.** `NC-1234`, 30 May–28 Aug: 91 total days, 1 off-road day, 90 available days, Rs 33,000 earned. UI: `Revenue per available day Rs 366.66`.

**Root cause.** `api/src/domain/reports.ts:693` computes `earnedMinor / BigInt(availableDays)`. BigInt division truncates; it does not use the project's required half-up money rounding.

**Expected.** Rs 366.67.

### GAP-194 — sticky action still obscures content on tall mobile (P2)

**Reproduction.** At 384×824, open a customer with five payments. Without scrolling:

- `Show all (5)`: y 723–767, 352×44;
- `Collect payment`: y 680–736, 352×56;
- visible intersection: 13 px;
- `elementFromPoint` at the top and centre of `Show all` resolves to `Collect payment`; only the lower sample resolves to `Show all`.

The existing GAP-171 fix adds bottom padding in `Screen.tsx:144–147`, but the sticky element at `:151–161` can still clamp over a child that initially lands beneath it. Scrolling works around the problem. This is a regression/new reproduction against the closed GAP-171 guarantee, not a reopening by assumption.

### GAP-195 — opening-balance remove target shrinks to 35 px (P3)

**Reproduction.** At 360×640, add an opening-balance entry with the long `QA2 Driver…` label. The icon-only `Remove …` button measures 35×44, below the product's 44×44 commitment.

**Root cause.** `web/src/features/opening-balance/OpeningBalanceScreen.tsx:334–342` places the 44 px icon `Button` in a flex row but does not give it `shrink-0`; the long sibling consumes its width.

## UI/UX observations that are not new defects

- Report table views use a table-local horizontal scroller (`326→635 px` on Trips, `326→672 px` on Receivables, `326→679 px` on Ageing at a 360 px viewport). The page body itself never overflows. This is intentionally implemented by `ReportTable` and is permitted by the project's WCAG 1.4.10 table-container exception, so it is documented as a usability trade-off, not filed as a gap. A card/stacked mobile alternative would reduce one-thumb panning if report tables become a primary mobile task.
- At 320 px, long customer titles visually truncate, but the full accessible name remains; this is deliberate and prevents body overflow.
- In 640×360 landscape, two audited detail/form scroll containers reported a 4 px internal width difference while the document remained exactly 640 px. No visible clipping or blocked control was found, so it is a watch item only.
- Monthly-lease photo capture says it is not supported yet in the current live flow. This is known scope, not re-filed.
- Date-only incident creation is the intentional U-2 level-1 save path. The earlier provisional concern was rejected after checking the tests and source contract.
- Customer/unauthenticated printed statements remain the already-tracked phase-2 GAP-170; a second gap was not created.

## Access-control results

- Linked-driver credentials land on `/me`; direct attempts to reach owner/staff routes redirect back to Mine. No staff navigation or other driver's identifier is exposed.
- The manager business shows operational reports and mileage packages but not owner-only close-month controls. A direct `/period/close` attempt is denied by the capability-protected read.
- Platform-admin routes are reachable only from the platform-admin account. Users, businesses, request queue, audit log, and management screens loaded without console errors.
- Cross-business switching updated the selected membership and data; no `TESTA` entity appeared in `TestBusinesByChamath` views.

## Period-close final state

The period close was deliberately the last write, after every reversible test and the export had completed. The open QA period was **1–30 September 2026** even though the test date was 28 August, because earlier QA had already closed August. Its checklist loaded and warned rather than blocked: 30 unconfirmed days, 4 open trips, 1 unsettled advance, 4 outstanding dues, and 4 open incidents. After confirming the explicit `This cannot be undone` dialog, the UI reported September closed and **1–31 October 2026** open. Postgres independently confirms September has `status = closed` with `closed_at` populated and October is the sole open successor. No console error occurred.

## Limitations and honest boundaries

- Light theme was not live-tested because this run's in-app browser exposed only the host dark scheme and the separate Chrome surface was unavailable. Automated theme/token tests passed, but that is not represented as a visual light-mode pass.
- No physical Android/iPhone hardware was attached, so camera capture, native date-picker styling, keyboard avoidance, and real safe-area behaviour remain emulator/browser evidence rather than a hardware pass.
- Destructive void-and-replace was taken to its final confirmation sheet but not submitted without action-time confirmation. The full automated correction/void coverage passed, including expense void, adjustment void, advance settlement void, offset void, skipped-day undo, incident contribution void, and payment correction tests.
- Messaging is intentionally unbuilt/deferred. A test cannot turn an absent product phase into a pass.

## Automated verification

Command: `NODE_OPTIONS=--max-old-space-size=8192 npm run check`

- guard: clean;
- ESLint: 0 errors, 66 warnings;
- Stylelint: pass;
- Prettier check: pass;
- TypeScript: API, web, shared pass;
- web: 148 files, 897 tests pass;
- API unit: 3 files, 10 tests pass;
- shared: 13 files, 99 tests pass.
