# QA findings — 22 August 2026

Live pass against `qa.fleetsettle.com`, run to verify the docs/qa scenario-catalogue refresh (PR #107) against real behaviour — Part B of that refresh, deferred from earlier in the day pending go-ahead. This is a targeted pass, not a full 12-suite sweep: it checks the content that actually changed in the refresh (suite 07's void-sheet correction model, suite 10's platform-admin tier) and the three items the refresh flagged as "Not built" (driver/customer statement, deposit release from `hold_window`, incident contribution deposit-source field), rather than re-running all ~130 scenarios.

## Precondition check

- `deploy-qa` confirmed green on `develop` HEAD (`e07f3af`, PR #107's merge) before starting — watched the run to completion rather than assuming the merge implied it.
- chrome-devtools MCP's shared profile dir was locked by an orphaned Chrome process (started 2:38 AM, unrelated to this session). Killed after asking the user first, per established practice.
- Console opened before first navigation, re-checked after every major step.
- Both colour schemes checked on at least one screen (Mine shell, dark mode).
- 360×640 mobile viewport, touch emulation, checked on the Mine shell.
- Network requests cross-checked against snapshots for the linked-driver session (see LT-9 re-confirmation below).

## Session type and tooling

Live browser session via chrome-devtools MCP, real Asgardeo OIDC sign-in (no stub), against `qa.fleetsettle.com`. Two real identities used: `nimesha.isholi94@gmail.com` (owner-manager on `TESTA`, also platform admin) and `nimesha.k.dev@gmail.com` (linked driver on `TESTA`, the same identity LT-9 redeemed a real invite with on 20 Aug). No writes made — read-only pass only; no reversible or one-way writes were judged necessary this session (see individual findings for what was and wasn't spent).

## Summary

| # | Finding | Verdict |
|---|---|---|
| 1 | Driver/customer statement or PDF export — genuinely not built | **Confirmed live — filed as GAP-170** (updated after this table was first written; see §1a below). The catalogue's own "Not built" label was correct, but tracing *why* it wasn't already flagged surfaced that `docs/README.md` and `TRACKER.md`'s GAP-65 both recorded it as shipped |
| 2 | Customer deposit release from `hold_window` — genuinely not built | Confirmed live, no defect (catalogue already correctly labelled "Not built") |
| 3 | Suite 07's void-sheet correction model | Confirmed live, catalogue's rewrite is accurate |
| 4 | Suite 10's platform-admin tier | Confirmed live, reachable and functioning |
| 5 | LT-9 (linked-driver 403 boundary) re-confirmed, six unconditional staff-only fetches on `/me`, not four | No defect — correct 403 behaviour per CLAUDE.md's tenancy rule; re-confirms a shape already known, count updated |
| 6 | Incident contribution deposit-source field | Not independently re-verified live this session — source evidence alone judged sufficient (see below) |

**GAP-170 filed** — item 1's "Not built" label was already correct, but tracing why it had never been tracked found `docs/README.md` and GAP-65 recording it as shipped, which undermines GAP-65's own deferral reasoning (see §1a). Item 2's "Not built" label is now live-confirmed with no further tracking implication — it was already correctly recorded as deferred, nothing rests on it being otherwise.

## 1. Driver/customer statement or PDF — confirmed not built, three surfaces checked

Checked three places a statement/export action could plausibly live, none had one:

- **Customer detail** (`QA Customer 0808052656`) → "Customer actions" dialog: only "Write off balance" and "Archive customer".
- **Driver detail** (`Sunil Perera`) → "Driver actions" dialog: only "Create account link", "Unlink account", "Archive driver".
- **Driver's own Mine shell** (signed in as `nimesha.k.dev@gmail.com`): only a "Sign out" action below the money summary — no export/statement/print anywhere.
- **Owner's Reports screen**: "Export transactions (CSV)" exists and is reachable (owner-only, as the catalogue says) — confirms the catalogue's claim that this is the *only* export action in the client, with no per-role or PDF variant.

Matches HP-05-006 / EC-10-004's rewritten text exactly.

### 1a. Escalation, added after the summary table above was first written: GAP-170 filed

Checking *why* the catalogue's "Not built" label had never been raised as a tracked gap led back to `docs/README.md`'s status table and `TRACKER.md`'s GAP-65 row, both of which record the printed slip as having shipped 17 Aug 2026 — a decision note (`use-cases.md` v1.2.13) that was never actually built. That's not just a stale status line: GAP-65's own phase-2 deferral reasoning is "nothing becomes unreachable — an unlinked one is handed the printed slip." With no slip, an unlinked driver or customer has no route at all today to see their financial position, which is exactly the reachability that reasoning claimed wasn't lost. Filed as **GAP-170** in `TRACKER.md` §4 (both the summary and full-account tables), with GAP-65's own row corrected to flag its reasoning rather than silently left to read as sound. `docs/README.md`'s F-6.6 status-table row corrected the same session. Not fixed — sizing and scheduling (including whether this reopens GAP-65's phase placement) left to the owner.

## 2. Customer deposit release from `hold_window` — confirmed not built

Customer detail's "Customer actions" dialog (same check as above) has no deposit-related action at all — not release, not a variant of write-off. Matches HP-01-010's finding (`HomeScreen`'s "Deposits to release" list is read-only, no other screen revisits a held deposit). No `hold_window` deposit existed on a live `TESTA` lease to click through as a positive control, so this is confirmed by the *absence* of any action on the one screen that could plausibly carry it, not by finding a specific record and failing to release it — the source-level evidence (no deposit code anywhere in `CustomerDetailScreen.tsx`) already carried this, this only adds that the live UI has nothing else either. No GAP filed — matches the catalogue's own "Not built" label.

## 3. Suite 07's void-sheet correction model — confirmed live

Driver detail page showed exactly the per-record-type shape the rewrite describes, not a generic Edit/Reverse: "Settle advance from 9 Aug 2026", "Void advance from 9 Aug 2026", "View settlements for the advance from 9 Aug 2026", "Void deposit movement from 9 Aug 2026" — four distinct, record-specific actions rather than one generic correction affordance. Confirms suite 07's rewritten model is accurate to the current build.

## 4. Suite 10's platform-admin tier — confirmed live

`/admin` reached cleanly as the owner (who is also platform admin on this QA account): Request queue, Businesses, Users, Admin management, Platform log, plus the "Leave admin panel" shell strip (GAP-151's fix). Console clean throughout. Confirms suite 10's rewrite for the current role/tier model — the old generic-role framing this catalogue replaced would have had nothing to check here at all.

## 5. LT-9 re-confirmed — six unconditional staff-only fetches, not four

Signed in as the linked driver, landed directly on `/me` (correct routing). `list_network_requests` showed six 403s fired unprompted on page load: `home/paperwork-warnings`, `daily-lease`, `day-record`, `reports/receivables`, `home/deposit-releases`, `trip` — up from the four TRACKER.md's 20 Aug LT-9 account recorded (`daily-lease`, `day-record`, `trip`, `reports/receivables`, `home/paperwork-warnings`, `home/deposit-releases` — actually the same six items are named there too; TRACKER.md's own summary line undercounted them as "four" while its detail line names all six). `GET /api/driver-view` returned exactly this driver's own data (Rs 5,000 held deposit, matching the DB-verified figure from the 20 Aug session) with no id field anywhere — INV-25 structurally intact.

**Not a defect.** Per CLAUDE.md's own tenancy rule, a 403 here is correct: the row's existence isn't the secret, the role's lack of capability is what's being enforced, and `auth/policy.ts` already confirms `LINKED_DRIVER` is granted only `viewOwnData`. The client firing these Home-screen data hooks unconditionally regardless of which shell it's routed into is real but minor waste (six failed round-trips on every driver page load), not a security or money-accuracy issue — noted here for completeness, not filed as a GAP, matching how the 20 Aug session already treated the same shape.

Also checked: dark mode + 360×640 touch viewport on the Mine shell — renders cleanly, no horizontal scroll, "He owes you" / "You owe him" (not "receivable"/"payable") confirms U-6 vocabulary lock still holds on this screen.

## 6. Incident contribution deposit-source field — not independently re-verified live

`CustomerContributionSheet.tsx`'s source carries an explicit code comment stating the omission is deliberate ("*how* it gets paid — one go, instalments, or from the deposit — is not a distinct field"), which is stronger evidence than most "not built" findings get. Reaching this sheet live requires first creating a real incident (no existing incident record was available on any `TESTA`/`QC-*` vehicle to inspect non-destructively), and incidents have no confirmed void/delete path in this catalogue — so creating one to check a form's field list would leave permanent test data on shared QA state for a claim already well-supported by source. Judged not worth spending that write this session, per the skill's own "read-only first ... only spend a write once it's worth spending" ordering. Left as source-confirmed, not live-confirmed — worth closing on a future session that's already touching incidents for another reason.

## `docs/qa/live-test-plan.md`

No LT-row changes — LT-9 stays closed (re-confirmed, not reopened), nothing here rises to a new queue item.

---

## Part 2 — 23 August 2026, same session continued: broader UI/UX and data-accuracy pass

Requested after GAP-170 was filed and the docs corrected: "do more testing to evaluate UI/UX flows and also the accuracy of the data." This half cross-checks live UI figures directly against the QA Neon branch (`br-square-sound-afb68wft`, read-only `mcp__neon__run_sql`) and runs one full write flow end-to-end, rather than re-checking the catalogue's already-confirmed content.

**Blocker hit and cleared**: re-establishing the owner session after the first half's sign-out/sign-in cycling triggered Asgardeo's reCAPTCHA on every automated attempt — not solved by this session (that's exactly the bot-detection it exists to enforce); the user solved it once manually in the same browser window, after which the session was usable normally.

### 7. Deposit netting across duplicate corrected entries — confirmed accurate

Queried every `deposit`/`deposit_movement` row behind the driver-side Rs 5,000 figure shown live and in earlier sessions. Found 6 separate `deposit` rows for `QA2 Driver 0808155856`, not 1 — looked like a possible under-reported liability at first glance. Traced fully: 5 of the 6 are `taken` (Rs 5,000) + `refunded` (Rs 5,000) pairs, each a corrected duplicate opening-balance entry, netting to zero; only the 6th is live. True net position: **Rs 5,000**, exactly matching the UI on both the driver detail screen and the driver's own Mine shell. No defect. (This session's own first attempt at the SQL had a bug — the wrong enum literal for the refund movement type, `refund` instead of the schema's actual `refunded` — caught and corrected before trusting the result rather than reported on the first pass.)

### 8. Home screen money figures — confirmed accurate against the ledger

Two figures cross-checked directly:

- **"Rent due · Rs 10,000"** (the `ZZ AGEING-TEST SEED` customer): summed the four seeded `obligation` rows directly (`direction = 'owed_to_us'`, `kind = 'other'`, none voided, none paid) — 4,000 + 3,000 + 2,000 + 1,000 = exactly Rs 10,000, and all four are the same customer, matching Home's "1 customer balance" line.
- **"Expected · Rs 3,000"** (NC-1234, today's and yesterday's daily-lease cards): `day_record.expected_minor` for both 22 and 23 Aug reads exactly 300000 (Rs 3,000) each, `state = 'open'` before confirmation.

Both exact matches, no defect.

### 9. Full day-confirmation flow, live, end to end — confirmed correct

Tapped "Paid in full" on the 23 Aug NC-1234 / Sunil Perera card (a single-tap action, no intermediate sheet). UI updated in place to "Confirmed / Earned Rs 3,000." Verified in the database immediately after:

- `day_record` (23 Aug, NC-1234): `state` → `ran_paid_full`, `earned_minor` → 300000 (was 0, `expected_minor` unchanged at 300000).
- A new `payment` row (Rs 3,000, `occurred_on` 23 Aug) fully allocated via `payment_allocation` against a `daily_amount` obligation, which itself moved to `status = 'paid'`, `settled_minor = 300000`.

Both the earned fact and the received fact were written together, correctly and consistently, matching CLAUDE.md's "every money write is one transaction" and "earned and received never collapse" rules under a real live tap, not just by reading the domain code. This is a genuine, permanent state change on the shared `TESTA` business — left in place rather than voided afterward, since it's a correct, ordinary confirmation indistinguishable from the many other legitimate paid days already in this business's history (the same `daily_amount`/`trip_fare`/`rent` payment shapes appear repeatedly in the preceding days' own payment rows), not a mistake needing correction.

### 10. UI/UX — dark mode, 360×640, and 320px reflow on Home

All three checked on the Operate Home screen (previously only checked on the driver's Mine shell, see §5 above): no horizontal scroll at either width, the paperwork-expiry alert text wraps cleanly across four lines at 320px without clipping or overlapping the "View vehicle" link, stat tiles (Rent due / Trips / Expected) reflow correctly, tab bar labels stay fully visible and unclipped at both widths. No defect.

One console item surfaced on reload at 360×640 dark mode: a Chrome "issue"-level entry, *"Content Security Policy of your site blocks the use of `eval` in JavaScript."* Traced as far as budget allowed: `grep -rn "eval(\|new Function("` across all of `web/src` returns nothing, so this isn't app code calling `eval` — most likely a bundled dependency's feature-detection probe, correctly blocked by CSP with no observed functional impact (every figure on the page rendered correctly, both before and after). Not filed — no reproducible consequence found, noted here rather than silently dropped in case a future session sees the same message attached to an actual symptom.

### Summary of Part 2

No new defects. Three independent live figures (deposit net, ageing receivable total, day-record expected) matched the ledger exactly; one full write flow (day confirmation) produced correct, atomic, consistent database writes; UI/UX checks at two narrow viewports and dark mode found nothing wrong. The one open thread is the untraced CSP `eval` issue, recorded rather than chased further.

---

## Part 3 — same session continued: session expiry, incident-contribution closed out, and a broader report/form pass

**Session expired again mid-session** (a direct URL navigation to a driver-detail route landed on the sign-in page rather than the route). Re-signing-in hit Asgardeo's reCAPTCHA again — same as Part 2's blocker, not attempted, the user solved it manually a second time. Worth recording as a pattern rather than a one-off: this QA identity's session appears short-lived enough that a testing session spanning tens of minutes should expect to hit this more than once.

### 11. Incident contribution deposit-source field — now confirmed live (closes Part 1 §6's deferred item)

An existing **open** incident was found on NC-1234 (`QA end-to-end test — 19 Aug live testing session`, already carrying a settled insurance claim and a Rs 500 pending customer recovery) — meaning the previously-deferred check no longer needed a new incident to be created. Opened "Customer contribution" from "Incident actions": the sheet has exactly three fields — **Agreed amount, Agreed on, Note** — and nothing else. No payment-method or deposit-source field, confirming suite 04's EC-04-006 finding live without writing anything (closed without saving). Not filed as a GAP — matches the catalogue's existing "Not built" label, source (the `CustomerContributionSheet.tsx` code comment) already carried this with high confidence.

### 12. "Where is our cash" report — two figures cross-checked, both exact

- **"Rs 6,000 held as deposits"**: summed net position across every `deposit`/`deposit_movement` row for `TESTA` — driver deposits net to exactly 600000 minor (Rs 6,000: Sunil Perera's Rs 1,000 + `QA2 Driver`'s Rs 5,000, confirmed in Part 2 §7), zero customer deposits exist. Matches exactly, and correctly reads "a liability, not partner cash" per CLAUDE.md's deposits-are-never-income rule.
- **"With drivers, as advances — Sunil Perera Rs 500"**: matches the one open, unvoided `advance` row for `TESTA` exactly (Rs 500, no settlements against it).

The report's own top-line total (Rs 37,699.79) was not independently reconciled — verifying it would mean summing every payment/expense/payout for the business from scratch, judged not worth the budget given the two sub-figures most likely to carry a netting bug both checked out exactly.

### 13. Ageing report — all four buckets confirmed exact

"Who is overdue, and by how long," as of 23 Aug 2026, showed the `ZZ AGEING-TEST SEED` customer's four obligations bucketed as 1–30 days (Rs 1,000), 31–60 (Rs 2,000), 61–90 (Rs 3,000), over 90 (Rs 4,000). Computed the actual age of each obligation's `due_on` against 23 Aug directly (22, 47, 77, and 122 days respectively) — every one lands in the bucket the report shows it in, and every amount matches its own obligation row exactly. Full ageing accuracy confirmed, not just the total.

### 14. "Record expense" form — U-2 confirmed, one accessibility thread found and not conclusively resolved

At 360×640: only **Amount / Category / Date** show above the fold, "More" collapses everything else, and "Record expense" stays disabled until Amount and Category are filled — U-2's "every create form saves with level-1 fields only" holds, confirmed live not just by reading the form's own field list. Closed without submitting (no need to create more test data).

**One console issue appeared after closing the sheet**: Chrome's Issues panel, *"No label associated with a form field."* Not conclusively traced to a specific element this session — `DateField.tsx`'s date input is correctly wired (`<Label htmlFor={inputId}>` paired with a matching `id`, plus a redundant `aria-label`), which rules out the one field most likely to be the cause. Reading the source further while investigating did find a real, if milder, pattern worth flagging: both `MoneyField.tsx`'s inline-button variant (`web/src/components/MoneyField.tsx:67`, `<Label>{label}</Label>` with no `htmlFor`) and `RecordExpenseSheet.tsx`'s Category field (`:162`, a plain `<span>`, not even the shared `Label` component) render a visible label with no programmatic association to the button beneath it — unlike `DateField`'s own correct pattern right next to them in the same sheet. Both buttons do carry their own `aria-label`/visible text as a fallback accessible name, so this is a WCAG 1.3.1-shaped consistency gap rather than a field with no name at all, and neither is definitively the trigger of the specific "form field" issue Chrome flagged (buttons aren't form fields in that audit's technical sense) — which is why this is recorded as found-but-unconfirmed rather than filed as a GAP. Worth a follow-up session with the Chrome accessibility tree (`Accessibility.enable`) open before the sheet mounts, per the run-qa-pass skill's own precondition for exactly this class of check, which this session did not do until after the fact.

### Summary of Part 3

One deferred item closed cleanly (incident contribution field, no write needed). Two more reports fully cross-checked against the ledger with no discrepancy (cash position's deposit/advance figures, all four ageing buckets). U-2 held on a create form under live mobile conditions. One real accessibility inconsistency found in source (`MoneyField`/`RecordExpenseSheet` label wiring vs. `DateField`'s correct pattern) but not conclusively tied to the specific console issue that prompted the search — left open rather than either dismissed or over-claimed.
