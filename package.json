{
  "name": "fleetsettle",
  "private": true,
  "type": "module",
  "workspaces": [
    "api",
    "web",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "guard": "node scripts/check-forbidden.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:scoped": "eslint api scripts eslint.config.js stylelint.config.js && eslint packages && eslint web",
    "lint:css": "stylelint \"web/**/*.css\" --allow-empty-input",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "node scripts/workspaces.mjs typecheck",
    "test": "node scripts/workspaces.mjs test",
    "build": "node scripts/workspaces.mjs build",
    "check": "npm run guard && npm run lint:scoped && npm run lint:css && npm run format:check && npm run typecheck && npm run test",
    "deploy:qa": "npm run deploy:qa -w @fleetsettle/web && npm run deploy:qa -w @fleetsettle/api",
    "deploy:production": "npm run deploy:production -w @fleetsettle/web && npm run deploy:production -w @fleetsettle/api"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.8.0",
    "prettier": "^3.4.2",
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^37.0.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.65.0"
  }
}
