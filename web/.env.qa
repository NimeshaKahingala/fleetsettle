# QA. Loaded by `vite build --mode qa` (see deploy:qa) — Vite's default build
# mode is "production", so QA needs its own mode or it would silently ship
# production's client id.
VITE_ASGARDEO_CLIENT_ID=h8HJK3NgWyj6qPndmaKYrhh8d8Ma
VITE_ASGARDEO_BASE_URL=https://api.asgardeo.io/t/fleetsettle
