# Build tracker

**Not a specification.** `docs/` says what to build and why; this says what is done. Where the two disagree, `docs/` is right. [Plan.md](Plan.md) says what remains and in what order; where *those* two disagree, this one is right, because it is the record and that one goes stale.

**Rewritten 4 August 2026** from `b2cf367`, and **the build log below was compacted 9 August 2026** — 31 dated narrative entries covering 4–9 August became the changelog that follows. Nothing was dropped that is still load-bearing: what each item *did* is in [§4](#4-every-open-gap)'s closed table and in [Plan.md](Plan.md)'s own item sections, what each item *taught* is in [§5](#5-the-traps), and the full prose is in git history. The entries were a running log written forward; past a certain length that stops being a record and starts being a place where live items hide.

## 0. Build log

**5 August** — CI's integration workflow configured and verified end to end (`e7efa71`), the first PR run that actually tested anything · a second validation pass over A2/A3/B2/B3 before building them, finding GAP-37 and GAP-38 (`2822193`) · **A2** partner/banking/cash reads, 52 tests, and GAP-39 found as a live gap the endpoint works around · **A3** period/write-off/payment reads · **A4** customer-scoped reads · **A5** driver history reads · **B8** real Asgardeo auth — the item this plan had costed at "ten minutes of console work" while its whole client half was unbuilt · **B0** the `/more` hub and sign-out · **A9a** migration `0008` · **deployed**: `fleetsettle.com` and `qa.fleetsettle.com` both live · A6–A10 re-validated against the code ahead of building them.

**6 August** — **GAP-3 closed out of sequence**: `confirmDay` had been silently discarding real confirmations in production since P13, found by an independent review rather than by this tracker · **A6** the trip receivable · **A0/GAP-42**: every signup had been landing in the one role with no screen · Track B validated before building, three items corrected · GAP-43 given a specified mechanism (W-57) · `UI-UX-REVIEW.md` validated → GAP-44…GAP-48 · **the first real browser round trip against QA**, which found GAP-49, GAP-50 and GAP-51 in twenty minutes and added four traps to §5.

**7 August** — **B11** (GAP-49/GAP-50, both worse than reported) · the A11 access model settled · **B10** (GAP-51) arrangement B can be started at last · **A11** a second person can join a business · merged to `develop`, QA redeployed, GAP-54 found · **A12** (GAP-56/GAP-57), the only new money defect either review produced.

**8 August** — A12 merged; a flow-inventory audit against both trackers found nine unrecorded gaps (GAP-61…GAP-69) · a live QA browser pass (`QA-BROWSER-TEST-FINDINGS`) · **B14, B0b, B12, B13 and B3's core** all shipped in one session · a triage pass over the queue itself, correcting four scheduling errors · a QA rerun, mostly re-confirmations plus GAP-83 · **the queue re-sequenced around B4**, with six facts checked before the order was written · **F1** (GAP-84) · **B4 Wave 1** — the Review shell, the catalogue and all six phase-1 reports.

**9 August** — **12a**, the DM §15 / UI §11.1 `doc-change` for GAP-70/GAP-71, found complete but uncommitted in the working tree and committed rather than left at risk · **B16 Phase 1**, a visual-semantics pass from an independent review, with two of its recommendations declined against source (the 8px card radius contradicts UI §5.3; the proposed status words contradict U-6's locked vocabulary — both re-raised on 9 Aug and re-declined, §6 item 22) · **GAP-70/71's four new DM §15 queries verified against real Postgres**, which found `golden.py` silently broken since migration `0004` (§5) · a CI regression from F1 fixed in two test files never re-run after it (§5) · **GAP-85** · **the ninth validation pass** over four external review documents, adding GAP-88…GAP-99 · **F2 (server half)**, closing the revoked-member 500 (GAP-90) · **A17**, two route-def validation holes (GAP-92, GAP-93) · **A16**, a vehicle's trip history (GAP-77) · **A15**, changing a daily lease's driver (GAP-62) · **A13**, changing a vehicle's arrangement (GAP-54) — moving off B auto-closes the open daily lease via A15's own mechanism, since `generate-day-cards` never reads `vehicle_arrangement` · **A10a**, the management fee that never fires (GAP-39), migration `0011`, **G-1 re-verified at 134,000 through the real close endpoint**. Track A's queue is now clear down to A10b, A8, A14, A7 and A9b.

**Later the same day — five standing decisions asked and answered, then built.** GAP-88, GAP-10, GAP-87, GAP-60 and GAP-1 had each stalled on a decision only the owner could make (§2's own rows on all five). Asked directly, with a recommended option on each; all five came back "recommended." Two needed a `doc-change` first and got one, in the order this tracker has used since GAP-70/71: **D-9** (DM §4.1, §17) settles GAP-88 — `startDailyLease` and `changeDailyLeaseDriver` now materialise their own rolling 90-day horizon synchronously, inside the same transaction, using the identical write `generate-day-cards` already runs; the cron's role changes from originating the fact to extending it, which is what CLAUDE.md's "no cron is a prerequisite for a user action" required all along. `user-flows.md` F-3.4 (v1.1.5) settles GAP-10 — **A10b** shipped: `recordCustomerContribution` now opens an `Obligation` a customer can actually pay against (migration `0012` adds the `incident_recovery.obligation_id` FK the schema had left toothless since `0001`), and `recordRecoveryReceived` was extended to settle that obligation and post the real `payment`/`payment_allocation` pair alongside it — found necessary only once the obligation existed: without it, the recovery's own receipt and the obligation's `settledMinor` would have been two independently-updatable facts about the same money, exactly the split-brain CLAUDE.md's "earned and received never collapse" rule exists to prevent. **GAP-87** closed the same pass, no doc-change needed: `bookTrip` now mirrors the client's own `canBookTrip` gate (arrangement B or C, never A, never unset) — the identical hole GAP-84 closed on the other two start screens, found while fixing those and deliberately left for its own turn. Its test blast radius was large (35 vehicle-creation call sites across four integration files had never set a standing arrangement) and was fixed by a subagent, then independently re-run end to end before being trusted — all four files green, G-1's 134,000 untouched. **GAP-60** and **GAP-1** were decided but not built this pass — see their own rows in §2/§4 for what was decided and why building stays for A9b and its own slot respectively. `npm run check` clean throughout; golden fixtures re-verified at 39/39 (134,000 / 15,000 / 7,500 all unmoved).

**Still open from B16, and carried by the user separately:** **Phase 2** (`Section`/`EmptyState`/`NotAvailable` variants) and **Phase 3** (reports-catalogue grouping, Review-shell money-direction consistency). Both independent and pickable any time; Phase 3 wants the outstanding `develop` merge first so it is visible on hosted QA to verify against.

**10 August — B4 Wave 2, the half of B4 that stayed open since Wave 1.** GAP-70 and GAP-71 built against the queries the 9 Aug `doc-change` (12a) already verified against real Postgres. **GAP-70**: `listBankedByDestination` and `listAdvancesOutstandingByDriver` (`queries/reports.ts`) give the two subtrahends `heldMinor` already nets their own rows — `cashPositionResponseSchema` gains `banked[]`/`driverAdvances[]`, kept arithmetically consistent with `heldMinor`'s own simplification (a `part_settled` advance counts at its full amount in both places, deliberately). `CashPositionReportScreen` retitles to "Where is our cash" and shows both breakdowns as their own tables, with an honest "nothing yet" message rather than an empty table. **GAP-71**: `listLostDaysByMonth`/`listLostDaysByReason` join `listLostDays` as siblings, not replacements — `lostDaysResponseSchema` restructures from a flat weekday array into `{ byWeekday, byMonth, byReason }`, a breaking wire change absorbed in one pass since the only consumers were this branch's own screen and its tests. `LostDaysReportScreen`'s primary chart is now genuinely "column per month" (UI §11.1), with weekday distribution and the reason breakdown as its two second-tier charts; the per-driver table is unchanged. `LOST_REASON_LABEL`/`LOST_REASON_OPTIONS` extracted to `web/src/lib/lostReasonLabel.ts` so the reason chart and `ConfirmDayCard`'s own picker share one source rather than drifting, the same pattern `ARRANGEMENT_LABEL` already set. 8 new API integration tests (both endpoints), full suite **32 files / 463 tests green**, including a concurrency-adjacent reconciliation check (an advance's `issued_by_user_id` must be set for `heldMinor` and `driverAdvances` to agree — the two queries are scoped differently on purpose, DM §15's own stated reason). Web: 97 files / 418 tests green. `npm run check` clean throughout; golden fixtures unmoved. **B4 is now fully done, both waves.**

**10 August, later the same day — GAP-101, the read-error contract, planned and then built.** A full implementation plan (`GAP-101-READ-ERROR-CONTRACT-PLAN-2026-08-10.md`) went first: it corrected the tracker's own "50 of 52 screens" figure to **30 of 32 files, 81 of 83 call sites** (the wider count double-counted 27 files that only call `useQueryClient` for post-mutation invalidation), and named two failure modes the original finding hadn't — a `?? []`/`?? 0` fallback turning a failed read into a confident wrong answer (`HomeScreen` rendering "Nothing needs you today" while the Worker is down), and a failed read silently deleting a safety warning (`StartLeaseScreen`/`BookTripScreen`'s expired-paperwork strip, `VehicleCalendarScreen`'s INV-1 grid rendering every day as free). Five open decisions in the plan were put to the user and confirmed as recommended before any code moved. **The `doc-change` landed first**, per the standing rule: `ui-ux-guidelines.md` to v1.2.5 — **M-28** ("every read has three visible outcomes: data, a real nothing, or a stated failure"), a `QueryState` row in §6.4, §9.5 retitled "Loading and failing" with the status→copy table, a `NotAvailable`-vs-`QueryState` distinction in §11.4, and a new enforcement row in §12.6; `implementation-guidelines.md` to v1.5.1 for the matching `IG §16.1` row. **The primitive**: `useQueryState` (`web/src/lib/useQueryState.ts`) resolves one `UseQueryResult` into `idle | pending | error | ready` — `idle` kept distinct from `pending` specifically because a disabled query (`enabled: false`, used at 23 call sites) sits in `isPending: true, fetchStatus: "idle"` forever, and collapsing that into "loading" would have reproduced the exact bug on the ten screens with the most queries. `combineQueryStates` resolves several reads at once, error beating pending beating idle. `QueryStateFailure`/`QueryState` (`web/src/components/QueryState.tsx`) render `EmptyState`'s sibling, never `AlertStrip` (that's Home's urgency channel) and never `NotAvailable` (that means the number doesn't exist, not that the read failed) — status-mapped copy (401 → sign in, 403 → no access, 404 → gone, 5xx → the request id as a caption and Try again), inside the §9.6 vocabulary lock. `main.tsx` gained a `shouldRetryQuery` predicate (`web/src/lib/queryRetry.ts`): a 4xx `ApiError` never retries, since retrying a decided answer only delays the error branch it precedes. **Migration ran in four waves, worst first**: **W1** — the ten screens making a false claim (`HomeScreen`'s six-query false-empty, `PeopleListScreen`, the `StartLeaseScreen`/`BookTripScreen` paperwork and pause warnings, `VehicleCalendarScreen`'s INV-1 grid, `OpeningBalanceScreen` — which turned out to have a second, sharper bug: its save is a full replace, so a failed hydration read could have silently overwritten a previously-saved batch, now blocked rather than degraded — and the four picker sheets, `ReasonPicker` gaining an optional `error` prop along the way). **W2** — the six reports and four Review screens plus `DriverDetailScreen`, **closing F2's client half** (`ReviewMoneyScreen` is GAP-90's own screen). **W3** — the remaining eight screens, `CloseLeaseScreen`'s deposit-settlement gate the second write-corruption-shaped find (a failed deposit read used to fall through to "no deposit, Next," which would have skipped a real settlement — the primary action now reads "Try again" instead). **W4** — the `check-forbidden.mjs` guard rule (a `useQuery(` with no `QueryState`/`useQueryState` reference in the same file fails the gate, verified against both a real violation and the whole repo), which caught one genuine miss the manual pass had left (`CorrectPaymentSheet.tsx`, fixed alongside it), and the retirement of the two hand-rolled `isError` branches (`FirstRunGate`, `CloseMonthScreen`) onto the shared primitive. **97 → 105 web test files, 418 → 492 tests, all green; `api/` untouched throughout, so the golden fixtures were never at risk rather than re-verified.**

**10 August, later still — a fresh live QA pass finds the most severe gap this tracker has recorded.** `LIVE-BROWSER-FINDINGS-2026-08-10.md`, run against `qa.fleetsettle.com` at its then-current deploy (B4 Wave 2, `2c0499e` — GAP-101 had landed on `build/p0-foundation` by this point but not yet reached `develop`/QA), with the new reports catalogue giving it something to cross-check live data against for the first time. **GAP-103**: an opening balance can be entered, confirmed, and the screen says so — but `saveOpeningBalance`/`commitOpeningBalance` never write an `obligation`, `deposit`, or `advance` row for any of the six entry kinds, confirmed both by source (`domain/opening-balance.ts`'s entire write surface is two tables, neither read anywhere else in the API) and live (a committed Rs 2,000 customer due invisible on "Who owes us"; a committed Rs 5,000 driver deposit contributing nothing to "Where is our cash", which shows exactly the one real deposit and no more). **GAP-104**: the mobile `ActionSheet`→`Sheet` history race documented in `MOBILE-SHEET-AND-DATE-PICKER-FINDINGS-2026-08-09.md` but never triaged into this tracker is now directly reproduced under real touch/coarse-pointer emulation — Quick Add's Fuel/Expense/New trip silently do nothing on a phone, and a nested `MoneyField` sheet can close its own parent sheet, discarding an in-progress opening-balance entry with no error of any kind. **GAP-106**: the new "Where is our cash" report hardcodes "Held for customers" onto a total that is party-agnostic by design and, in the test data, entirely a driver's deposit — three call sites in `CashPositionReportScreen.tsx`, with the existing unit test pinning the wrong wording as correct, the same shape GAP-75 already taught this queue to watch for. **Also confirmed live, no code change needed**: LT-7/GAP-3 fully closed (the overnight cron produced a real placeholder, confirming it wrote all three money facts correctly); GAP-84's arrangement gate holds under direct URL navigation; GAP-85's blank-vs-zero fix holds. **GAP-83 gets a second, sharper confirmation** — a full DOM tab-order trace, not just a source read, showing the exact keyboard path a sighted user takes through an invisible focus stop. Full account, including two findings that could not be tested and why (MP-08's `Dialog`-in-`Sheet` stacking has no reachable precondition anywhere in the client; MP-06's `showPicker()` fallback needs a real Safari, not this session's Chromium), in `LIVE-BROWSER-FINDINGS-2026-08-10.md` and TRACKER §6's eleventh pass.

**10 August, later still — B9's two blocking decisions asked and answered; `feature/image-upload` evaluated and found further along than this branch's own tracker knew.** GAP-44 and GAP-89 had both stalled B9 on a real product decision rather than a build question — asked directly, with a recommended option on each. **GAP-44** came back *against* the recommendation: the fuller option (enrich `VehicleDoubleBookedError` with the actual conflicting dates/entity, so the Dialog states precisely what's blocking) was chosen over the cheaper name-and-link-to-calendar default, which makes this item bigger than B9's row previously sized it — it now touches the error class, its catch sites, and the wire schema, not only the client. **GAP-89** came back both times as recommended: `/review*`/`/reports*`/`/period/close`/`/opening-balances` stay in Operate's own shell and highlight "More" rather than switching an owner-manager into the Review shell's tab bar for the same routes; vehicle-scoped detail routes highlight "Vehicles" as the domain parent regardless of which of the three places sent them there. Neither is built yet — both are recorded in their own §4 rows so the next sitting starts from a decision, not a re-litigation, the same pattern GAP-88/GAP-10/GAP-87/GAP-60/GAP-1 already set 9 Aug. **Separately, `feature/image-upload` (A7/GAP-16) turned out to be a full seven-commit implementation, not the "fully planned, not started" this branch's own tracker still read** — built and pushed 9–10 Aug, closing GAP-16 for expense receipts specifically, but carrying its own punch list (`ATTACHMENT-UPLOAD-PENDING-2026-08-10.md`) of real blockers before it can be trusted: its 26-case integration suite has never run, golden fixtures are unverified, no PR is open, and it is now three commits behind this branch. GAP-16's row here is corrected to describe that state precisely rather than either of the two wrong claims (not-started, or closed) — see its own row for the full account. **GAP-103 (F3) is unaffected by any of this and remains the actual head of the queue**: it still carries its own undecided fork (materialise the six opening-balance entry kinds into `obligation`/`deposit`/`advance` synchronously on commit, or keep them a separate ledger the reports learn to read alongside the real tables) and nobody has been asked yet.

**10 August, later still — A7 merged.** PR #20 (`feature/image-upload` → `develop`) landed clean: CI's `gate` and `integration` workflows both green, the 26-case attachment suite included, golden fixtures re-confirmed unmoved (134,000/15,000/7,500) as part of the full suite passing. `ATTACHMENT-UPLOAD-PENDING-2026-08-10.md`'s own blocking list is now stale on every point it named — the PR did open, CI did run the integration suite, golden fixtures did get re-verified. **Not yet on `main`** — no `develop`→`main` PR exists, so expense-receipt uploads are live on `qa.fleetsettle.com` but not `fleetsettle.com`; per CLAUDE.md, opening that PR is a deploy decision, not a mechanical follow-up. Track A's remaining queue: A8, A14, A9b.

**10 August, later still — `PENDING-REVIEW-ITEMS-2026-08-10.md` evaluated and absorbed (TRACKER §6's twelfth pass), then B9's first batch built.** The seventeen root review/audit files accumulated since 6 August had already been consolidated into one pending-items checklist and removed from the working tree (uncommitted, recoverable via git history) by the time this pass opened it — the consolidation itself got the same source-checked treatment every external review here gets, rather than being trusted on its own say-so. It held up: every checklist item mapped onto a gap id this tracker already carried, five items were re-raised without noting they'd already been parked in `Recorded, unowned, and correct to leave` (context restored on each row rather than silently reopened), and nine of B9's smallest, least-ambiguous rows were built the same sitting — **GAP-45** (the trip-title year now conditional, no more doubled-date clipping at 360px), **GAP-46** (every occupied calendar cell gets the legend's own wording as its `aria-label`), **GAP-55** (`autoComplete` tokens on the four real name/phone/address/organisation fields), **GAP-89** (`tabForPathname` widened per the 10 Aug decision — More for review/reports/period/opening-balances, Vehicles for entity detail routes), **GAP-91** (`variant="destructive"` on the one irreversible confirm), **GAP-96** (`Section` gained an optional `total` slot; Vehicle Overview's Costs section uses it), **GAP-97** (Book trip added to the vehicle-actions sheet, gated the same way `bookTrip` itself is server-side), **GAP-99** (the two leaked "presigned upload storage" strings reworded — and the row's own prediction that A7 would make this moot corrected, since A7 shipped expenses only, not condition photos), plus **M-25** (Excess rate → Excess fee per km, riding with GAP-97's same forms). Full gate green after two flaky re-runs ruled out by isolation (105/105 web files, 513 tests; 12/12 shared files, 85 tests; `api/` untouched, so golden fixtures were never at risk). **GAP-106 followed the same day** (11 Aug): the same three "Held for customers" strings reworded to "Held as deposits", and `CashPositionReportScreen.test.tsx`'s own pinned-wrong assertion corrected rather than left to keep passing against the bug. What's left in B9: GAP-44 (needs the wire-schema change GAP-44's own decision requires), GAP-47 (landscape, two shared primitives), GAP-48 (a new `Toast` primitive plus call-site decisions), GAP-83/GAP-105 (`DateField`'s own rebuild).

**11 August — F3/GAP-103 built: the most severe recorded gap, closed.** The undecided fork (materialise on commit vs. a parallel ledger) was put to the user and came back **materialise** — into the exact tables the reports already read, not a second ledger every report would need a new `UNION` to know about. `commitOpeningBalance` now turns each entry into a real row on its first commit: `customer_due`/`driver_arrears`/`owed_to_driver` become an `obligation` (a new `kind: 'opening_balance'`, migration `0014` widens `obligation_kind_check` the same way `0009` did for `trip_fare`); `deposit_held` becomes a `deposit` + a `taken` `deposit_movement`; `advance_outstanding` becomes an `advance`; and `cash_held` — which turned out to have no table of its own at all, `listPartnerCashPositions`'s `heldMinor` is pure derived arithmetic (`received − banked − advanced`) — becomes an unallocated `payment` (`direction: 'received'`, `handledByUserId` the partner), exactly the shape a real surplus payment already leaves, so the cash-position report needs no new read path either. F-0.2's own Alternates clause ("editable until the first period closes") meant this had to handle a correction *after* commit, not just the first one: a new table, `opening_balance_posting` (migration `0014`, deliberately not a money table — no `posted_period_id`, not in `assert_period_open()`'s array, the same reasoning `0013` gave `attachment`), links each entry to whatever it produced, so a later `saveOpeningBalance` call on an already-committed batch first reverses the prior generation (`obligation`/`advance` void by id — both sums already respect `voided_at`; `payment` flips `status` to `reversed`, which `listPartnerCashPositions` already filters on, never routed through `payment_correction`'s allocation-unwind machinery since these rows never have any; `deposit_movement` gets a real offsetting entry, since `sumDepositMovements` turned out to never consult `voided_at` at all — confirmed by reading it, not assumed) before posting the corrected figures. Two integration tests added to the existing suite: one exercises materialisation for all six kinds against the three reports GAP-103's own text named (`GET /api/reports/receivables`, `GET /api/driver/{id}/balances`, `GET /api/reports/cash-position`); the other extends the existing happy-path test to prove a post-commit correction leaves exactly the corrected figure behind, never both. Full API suite: 490/490 across 33 files, golden fixtures unmoved (134,000/15,000/7,500), DM §13's drift assertion clean. **Current queue head is now F4** (GAP-104, the mobile `Sheet` history race) — still fully designed, not yet built.

**11 August, later — low-API UI branch.** The UI slice after B15 closed B6 (customer detail plus vehicle-paperwork write), B5's core (driver `/me` and staff driver history), and the B2 callers: `/cash`, `/partners/:userId`, banking, capital contribution, partner payout/settlement, mileage-package create/archive, Members, driver access links, `BorneByPaidBy`'s paid-by member picker, and `/vehicle-sharing` for initial ownership-share setup plus management-agreement create/revoke. The branch also exposed a small API contract correction: `GET /api/business-member` had to return the membership row `id`, because revoke/change-role routes address membership ids, not user ids. A follow-up in the same branch closed GAP-100 by adding trip-scoped advance issue from Trip detail and advance settlement from staff Driver detail. **Still open by design:** GAP-1's per-vehicle access scoping, ownership-share replacement after an existing current split, richer settlement context until driver-view advances expose `settledMinor`/`tripId`, and API hardening so management agreements reject a non-`manager` member even outside the UI.

---

## 1. Where things stand

**Backend is complete through P13. Frontend is complete through Web-P8b. Both environments are deployed, and a real person can now log in to them.** As of 5 August 2026 the only thing still blocked on anyone outside this repository is **P14 messaging**, waiting on twelve Meta template approvals.

**"Complete" in the row above means built, not reachable — those turned out to be different claims.** Until A0 (6 Aug), the account the product's own signup flow created held `role: "owner"`, the one role with no screen — every real signup landed on `NotBuiltYetScreen` with nothing else to do. The Operate rows in §3 below were never wrong about what was *built*; they were silent about whether anyone could *become* the role that reaches it. A0 fixed new signups; **A11 (7 Aug) fixed the other half** — a second owner, a manager, or a linked driver can now actually get an account (`POST /api/business-member/invite` + `POST /api/invite/redeem`, `POST /api/driver/{id}/link-invite`). **B0b (8 Aug) narrowed the gap further without closing it**: an `owner`/`driver` token now lands inside a real Review/Mine shell (redirect-to-default-tab, `<Can>`-gated affordances, the whole plumbing) — but every screen inside it is still `NotBuiltYetScreen`. Reachable, still not usable; B4/B5 are what change that.

That changes what "remaining work" means again. Until 5 Aug the product could not be reached at all — no deployment, and an auth stub whose token the Worker was always going to refuse. Until 6 Aug, the one role reachable through it was the one with nothing built. **As of 8–9 Aug every role can get an account, land inside its own real shell, and — for the owner and owner-manager — actually use it**: B4 Wave 1 turned the Review shell and all six phase-1 reports from placeholders into screens. **9 Aug closed the live 500 (GAP-90) and both money facts that read as zero (A10a/GAP-39, A10b/GAP-10)**, plus GAP-88's cron/user-action conflict and GAP-87's sibling to F1. **10 Aug closed B4 entirely** (Wave 2: GAP-70/GAP-71), **then closed GAP-101 — F2's client half, and the largest single item inside B9**: the client now has a real read-error contract (`QueryState`/`useQueryState`, UI §6.4/M-28), and a guard-script rule stops the next screen from shipping without one. **What is left on Track A is A8, A14 and A9b — A7 merged into `develop` 10 Aug (PR #20), closing GAP-16 for expense receipts; on the frontend, the rest of B9 (GAP-44/47/48/83/105/106 — GAP-45/46/55/89/91/96/97/99 closed 10 Aug, see §4/§0), B15, B6, one shell (B5, the linked driver) and B2.** [Plan.md](Plan.md) sequences it. **A live QA pass later the same day found two gaps ahead of all of it** — GAP-103 (opening balances confirm live and connect to no money table anywhere — the most severe gap this tracker has recorded) and GAP-104 (the mobile `ActionSheet`/`Sheet` history race, which breaks Quick Add's Fuel/Expense/New trip on any real touch device) — filed as **F3** and **F4** respectively, on the same "live-and-silently-wrong outranks unbuilt" rule that put GAP-3, GAP-56, GAP-75 and GAP-90 ahead of their own queues. Full account in §0's dated entry and §6's eleventh pass.

**Two standing observations about `docs/`, both still true.** The 7 August pass found that **`docs/` needed no edit for any of its five gaps** — each was the code failing a specification already correct and already specific (GAP-56 against UC-94 read with U-8; GAP-54 fully written as F-1.2/UC-94; GAP-57 as F-5.3; GAP-1's scoping as UC-70/71/72 and W-49). **But four gaps since then are the opposite case, and they are now a recognisable class**: GAP-10, GAP-70, GAP-71 and GAP-88 are all places where the owning document is silent, incomplete, or in tension with another one, and **the documents move before the code does**. GAP-70/71's `doc-change` landed 9 Aug; **GAP-10's and GAP-88's landed later the same day** (`user-flows.md` v1.1.5, `data-model.md` v1.1.3/D-9), both settled by asking the standing question directly rather than guessing at it — see §0's later entry. The two that could arguably want a document but are really design decisions — GAP-59 (a cross-entity invariant) and GAP-60 (a `replaces_id` column) — stay unwritten rather than smuggled into `docs/` as a side effect of a review; GAP-60's own shape was decided the same pass (§2), and still correctly waits for a document only in the sense that no `docs/` edit is actually needed for a column decision.

CI runs the full gate and the integration suite on every PR, `develop` deploys to QA and `main` to production, both on merge.

| Gate | Result |
|---|---|
| `npm run check` | clean across `api` / `web` / `packages/shared` — verified fresh 9 Aug 2026 after F2/A17/A16/A15/A13/A10a and again after D-9/A10b/GAP-87, again 10 Aug 2026 after GAP-101 and the B9 batch (§6's twelfth pass), and again 11 Aug 2026 after the low-API UI branch |
| `web` | 112 files / 557 tests — verified fresh 11 Aug 2026 after B6/B5/B2-safe UI, mileage packages and paid-by picker |
| `packages/shared` | 11 files / 78 tests |
| `api` unit | 3 files / 10 tests |
| **`api` integration** | **33 files / 489 tests, 480/489 green on PR #20's own CI run** (10 Aug 2026, A7/GAP-16 merging to `develop`) — the 9 failures were connection timeouts scattered across 8 unrelated files, the documented Neon-contention flake (§5), confirmed environmental by a different random subset failing on retry, not a regression. `attachment.test.ts` (26 cases) is A7's own addition to the 32 files this row previously carried. G-1/G-2/G-3 all unmoved (134,000 / 15,000 / 7,500). **This shared Neon test branch shows severe, non-deterministic connection instability, confirmed recurring since 9 Aug** — a single `trip.test.ts` test timed out once on a full-file run and passed cleanly in isolation immediately after (§5 trap) |
| `npm run lint` | 0 errors, 48 warnings — all `react-refresh/only-export-components`, expected ([§5](#5-the-traps)) — the count keeps rising with every new route/context, correctly |
| **Deployed** | `fleetsettle.com` and `qa.fleetsettle.com` live since 5 Aug 2026. **No longer at the same migration — checked 10 Aug 2026, not previously stated here**: `qa`/`develop` is current through A7 (migration `0013`, `attachment`); `main`/production's last sync was PR #16, migration `0010` — **29 commits behind `develop`**, missing A10a (management-fee generator), A10b (incident receivables), B4 Wave 2, GAP-101 (the whole read-error contract — production screens still render `Loading…` forever on a failed request), the GAP-103/GAP-104 live findings, and A7 (receipt uploads) itself. No `develop`→`main` PR is open. Per CLAUDE.md, opening one is the deploy decision — this row records the gap, not a recommendation to close it |
| **Login** | Real Asgardeo, PKCE, JWT access tokens — one browser round trip still unexercised by a human |

**Routes: 34, as of the 11 Aug low-API UI branch** (15 before B0b, 22 after B4 Wave 1). `/review`, `/review/vehicles`, `/review/money`, `/reports`, `/me`, `/people/customers/:id`, `/cash`, `/partners/:userId`, `/vehicle-sharing`, `/members`, and `/mileage-packages` all render real screens. Still with no screen at all: `/settings/*` (level-3 config, unscoped). `/more` carries nine real rows — sign-out, opening balances, close the month, Reports, Cash, Vehicle sharing, Members, Mileage packages, My share. GAP-89 already fixed the tab highlight for these More-owned routes.

### The five rules that decided the order

Still binding on everything in [Plan.md](Plan.md):

1. **`packages/shared` is the root of the dependency graph.** A bug in it is structural, not local.
2. **The schema is the synchronisation point, and the main lever for parallelism.** Once a resource's Zod schema lands in `packages/shared`, the two tracks separate: the Worker builds against a real Neon branch, the client against mocks derived from *the same schema object*. Hand-mirrored types drift and what drifts is a money field. **This rule is what makes the plan's Track A / Track B split legal.**
3. **Auth blocks the client, not the Worker.** `tests/support/auth.ts` mints a user per W-49 role, so endpoints are tested before Asgardeo works.
4. **No cron is ever a prerequisite for a user action.**
5. **Reports come after the writes that feed them.**

### The golden fixtures

The regression suite. Any change that moves one is breaking and must fail loudly (FL §9.1).

| Fixture | Figure | Reproduces at | Status |
|---|---|---|---|
| **G-3** Mileage on an open-ended rental | **7,500** | P5 | ✅ end to end through real endpoints |
| **G-2** One accident | **15,000** | P8 | ✅ against the real period lifecycle |
| **G-1** One month of the bus | **134,000** | P9 | ✅ open July → write → close → open August |

---

## 2. Backend — P0 to P14

Each row is complete unless its Gaps column says otherwise. Gap ids resolve in [§4](#4-every-open-gap).

| Phase | Delivers | Proof | Gaps |
|---|---|---|---|
| **P0** Foundation | Money codec (`Minor = bigint`, largest-remainder split), business-date helper, error codes · Hono + Drizzle + Neon skeleton, per-request db middleware (`neon()` reads / `Pool` transactions) · migration runner (advisory lock + SHA-256 per filename) · `0001` (DM §16.0 DDL) and `0002` (`audit_log` trigger) · `/api/health`, `/api/ready`, `/api/docs` 404 in prod · Vite + React 19 + Tailwind v4 tokens, both dark selectors diffed declaration-by-declaration · PWA shell precache · CSP/HSTS via a native `public/_headers` | codec covers U+2212, M-16 cents, split-sums-to-whole | GAP-28 |
| **P1** Identity + access boundary | `jose` against a **local** JWKS through KV, refetch-once on unknown `kid`, one undifferentiated 401 otherwise · `sub → app_user → business_member` **or** `driver.linked_user_id` in one query · `auth/policy.ts` per W-49 row · **cross-tenant 404, missing capability 403** · the linked-driver test class · rate limiting on `CF-Connecting-IP` · `GET /api/me` | full matrix through the real middleware chain; a real ES256 keypair signs test tokens — only the IdP is mocked | GAP-1 |
| **P2** Entities + setup | CRUD for business/vehicle/driver/customer · three arrangements with the original start date preserved · opening balances (owners-only `manageOpeningBalances`) · paperwork expiry dates · **opens the first accounting period** as part of business creation · the creator is granted `owner_manager` (GAP-42, A0 — was `owner`, the one role with no screen) | | GAP-17, GAP-31, ~~GAP-42~~ |
| **P3** The daily loop | `domain/confirmDay.ts` — **four inserts, one transaction**; one endpoint serves all three F-4.2 buttons via a discriminated union; `paid_in_full` resolves the rate **in force on that date** server-side · idempotent on `(daily_lease_id, business_date)` **and on row *state*, not just existence** (a P13 placeholder is filled in via `UPDATE`, never mistaken for a confirmed fact — GAP-3, fixed 6 Aug 2026) · **`PERIOD_CLOSED` mapped from the trigger, no pre-check** · `earned`/`received` never collapsed | 14 integration | GAP-2, ~~GAP-3~~, GAP-30 |
| **P4** Costs, adjustments, driver money | `POST /api/expense` with **`borne_by` and `paid_by` as two fields**; absent `vehicleId` is a valid overhead cost · UC §6.7's default-owner matrix resolved **server-side** from the vehicle's current arrangement · adjustments/waivers — a waiver raises `waived_minor` only, `amount_minor` stays the charge · `computeObligationStatus()` is the one place status is derived · advances + settlement · deposits as money held · `offset_record`, the only thing that moves both balances · `GET /api/driver/{id}/balances` — two `SUM`s, no subtraction anywhere | 30 integration + 13 shared unit; §6.4's walkthrough lands 0 / 4,000 | GAP-4 |
| **P5** Rent, billing periods, mileage · **G-3** | Billing periods idempotent on `(lease_id, seq)`, boundaries from the lease's own anchor via `addCalendarMonths`, never chained off the previous period · `POST /api/lease` writes lease + handover odometer + **first billing period** in one transaction · **rent fixed per period, mileage allowance per day × days** (W-25) · one-directional excess; multi-period assessments split by days and marked `isEstimated`; at-or-below-threshold auto-waives · F-2.5 renew · F-2.2 payment, oldest-`due_on`-first, surplus returned as `unallocatedMinor` | 22 integration + 18 shared unit; **G-3 = 7,500** split 152/148 | GAP-5 |
| **P5+** F-2.6 close the lease | Four endpoints on `active → closing → closed`: `close` (stop the clock, final period via `applyAdjustmentTx`, final mileage through the unmodified `assessMileage`), `closure-summary` (INV-18), `settle-deposit` (refund / retain / `hold_window`), `close-out` (frees future occupancy). The covering period's `period_end`/`allowance_km` shorten on early closure **regardless of `finalPeriodMode`**; `rent_amount_minor` is never touched | 17 integration, through the real chain start → close-out | GAP-6, GAP-16 |
| **P6** Trips and charters | Booking **pauses** existing `day_record` rows (bulk `UPDATE`, never a loop) · close in one transaction — **INV-17 blocks unconditionally** on an unsettled advance; closing odometer and driver-fee obligation both written **at close, never at booking** (W-41/INV-30); trip P&L degrades distance/litres/km-per-litre to `null` · cancel reconciles the advance via `advance_settlement`, resumes paused days, **deletes** allocation rows (that table has no `voided_at`) · vehicle calendar | 19 + 5 integration; UC §7.1 reproduces **26,000** | GAP-7, GAP-8 |
| **P7** Partners, banking, cash | Ownership shares as **one bulk insert sharing one `effectiveFrom`** — `assert_shares_total()` is a *deferred* trigger, so 60/40 lands as one legal multi-row write; violation → 400, never pre-checked · capital kept distinct from ownership (W-52) · management agreements, `EXCLUDE` overlap → 409, revoke sets `effective_to` · banking events; the request schema offers **only** `absorbed`/`unattributed` · partner payouts | 27 integration | GAP-9, GAP-16 |
| **P8** Incidents · **G-2** | The container: open · off-road with three treatments (`continue` / `credit_days` via `applyAdjustmentTx` / `extend` writing `lease_extension`) · repairs reuse `expense.incident_id` · customer contribution agreed then received · insurance claim and its paired recovery in one transaction · bottom line as a **live snapshot** · closed manually | 14 integration; **G-2 = 15,000** | GAP-10, GAP-11, GAP-16 |
| **P9** Period close, corrections, audit · **G-1** | **`audit_log.changed_by` actually populated** — `withActor` wraps `.transaction` once in `db/client.ts` · close opens its successor in the same transaction; the checklist **warns and lists, never blocks** (U-7) · payment correction where `bearer` decides everything: `back_to_arrears` unwinds allocations newest-first (INV-22), `absorbed_loss` touches no obligation · void-and-replace for `expense` · opening balance locked once the first period closes | 26 integration; **G-1 = 134,000** | GAP-12, GAP-13, GAP-14 |
| **P10** Write-offs, post-closure charges | `write_off` flips the obligation straight to `written_off`, never touching `settled_minor`/`waived_minor` — the bucket stays separate from a waiver (INV-14) · recovery recorded as an ordinary `payment` **deliberately never allocated**; the `write_off_recovery` row is what marks it · post-closure charge posts to the *currently open* period and never checks whether the source is closed — being closed is the point | 13 integration | GAP-15 |
| **P11** Reports | Nine `GET /api/reports/*`, all read-only. DM §15's six ported close to verbatim; UC-71/72/77/79 designed this phase. `profitPerKm`/`kmPerLitre` **degrade to `null`, never zero** · `listAgeingBuckets` takes `asOfDate`, never `CURRENT_DATE` · `viewReports` (STAFF) gates seven, `viewOwnerOnlyReports` (OWNERS) gates UC-77/79 · **a linked-driver token 403s all nine**, proven in one loop | 17 integration | GAP-18, GAP-19 |
| **P12** Driver-view (backend half) | `GET /api/driver-view` — both balances never netted, every day **including excused ones**, closed trips and fees, advances, offsets, held deposit. **INV-25 is structural**: `driverId` is never a request parameter anywhere in the route, so no request shape can ask for another driver · new `viewOwnData` capability, `driver` role alone | 4 integration; INV-25 proven with two drivers on two vehicles | frontend half → Track B |
| **P13** Scheduled work | `generate-day-cards` + the arrangement-A calendar extension — **this is what finally enforced INV-1 for lease and daily lease** · `generate-billing-periods` reusing P5's exact function, catching up in a bounded 24-period loop · both **unscoped by `business_id` deliberately** (a cron is not a request), each unit in its own try/catch · idempotency via `.onConflictDoNothing()` on a **separate insert path** from the one a real booking uses, where a conflict must stay a 409 · real Cron Trigger via `Object.assign(app, { scheduled })`, preserving `app.request()` · `GET /api/home/paperwork-warnings` + `/deposit-releases` — reads, not jobs | 15 tests | GAP-20, GAP-21 |
| **P14** Messaging | 🔴 **Blocked** — twelve Meta template approvals (6 messages × 2 languages), minutes to ~2 days each. `dispatch-messages`, the fifth Cron Trigger, waits with it | | |
| **A2** Partner, banking and cash reads | `GET /api/business-member` (GAP-31) · five list endpoints — `ownership-share` (current split only, `effective_to IS NULL`, safe because `assert_shares_total` structurally forbids two open sets per vehicle), `capital-contribution`, `management-fee-agreement` (**revoked agreements returned, not filtered**), `banking-event`, `partner-payout` · the composed `GET /api/partner/{userId}` (F-7.6/F-7.3) — `putIn`/`takenOut` all-time, `earned` scoped to the open period only and named as such, `holding` reused from P11's cash-position query. No new table, no new write | 25 new integration tests (business-member 4, partner +16, partner-summary 5) in 2 new files; full suite 31 files / 353 tests, 353/353 green (one unrelated file's known connection-flake ruled out by re-running alone) | GAP-4 ✅, GAP-9 ✅, GAP-31 ✅, GAP-39 |
| **A3** Period, write-off and payment reads | `GET /api/accounting-period` (list, newest first, `viewReports`) · `GET /api/write-off` (list, every filter optional, gated `writeOffOrWaiveAboveThreshold` to mirror recording one) · **`GET /api/payment`** (GAP-38 — `dailyOperations`, mirroring `recordPayment`; carries the W-49 linked-driver 403 class) · close checklist gains **`unconfirmedDays`** (GAP-13 — `day_record` rows still `state = 'open'`, scoped to the open period's `posted_period_id`, one `COUNT(*)`, honest under-report if the cron hasn't run yet). No new table, no new write, no domain layer — three straight filtered reads and one added count | 13 new integration tests across 3 existing files; full suite 31 files / 366 tests, 366/366 green (`day-record.test.ts`'s known connection-flake under parallel load ruled out by re-running alone, 16/16) | GAP-13 ✅, GAP-38 ✅ |
| **A4** Customer-scoped reads | `GET /api/customer/{id}/obligation` (GAP-22 backend half — outstanding dues only, oldest due first, reusing `findOutstandingObligationsForParty` rather than a second party-scoped query so the screen can never disagree with what `recordPayment` allocates against; row shape reused from the lease obligation hub's own schema) · `GET /api/customer/{id}/payment` (reuses `listPaymentsForBusiness` scoped by `partyCustomerId`, and its response mapper via a handler-to-handler import, the same pattern `vehicle.ts` already uses for incidents). Both `dailyOperations`, 404 cross-tenant. No new table, no new write, no domain layer | 8 new integration tests in the existing `customer.test.ts`; full suite 31 files / 374 tests, 374/374 green (three files' known connection-flake under full-parallel load — `accounting-period`, `dailyLease`, `vehicle` — ruled out by re-running all three alone, 47/47 clean) | GAP-22 ✅ (backend half) |
| **A5** Driver history reads | `GET /api/driver/{id}/view` — the staff-facing twin of `GET /api/driver-view`, **reusing `getDriverOwnView` unchanged** and sharing one wire mapper (`toDriverViewResponse`), so two screens cannot disagree about one driver's money. `dailyOperations` + explicit business-scoped `{id}`, vs `viewOwnData` + `requireDriverId`. INV-25 intact — the driver's own route still has no id slot; a linked-driver token 403s this one for any id, their own included. GAP-29 closed **without** a separate `GET /api/advance`: the composed read already returns advances | 4 new integration tests in the existing `driver.test.ts` (10 total), green in isolation and inside the full suite | GAP-24 ✅, GAP-29 ✅ |
| **A11** Member and driver access | Migration `0010`: `business_member`'s pair-unique made partial (GAP-52), an explicit audit trigger (GAP-53), `business_member_invite`, **INV-31** (deferred, `AFTER UPDATE` only — see §5 for why not `INSERT` too) · `POST /api/business-member/invite`/`.../{id}/revoke`/`.../{id}/change-role` (new `manageMembers`, OWNERS) · `POST /api/invite/redeem` (one mechanism, two destinations — `business_member` or `driver.linked_user_id`, decided by the code alone; mounted on `verifyTokenMiddleware`, no business resolved yet) · `POST /api/driver/{id}/link-invite`/`.../unlink` (`manageEntities`, matching F-1.6/F-1.8's own "Manager" actor — **not** `manageMembers`, corrected before shipping, see above) | 39 new integration tests across 3 files; full suite **32 files / 406 tests, 406/406 green** against a fresh ephemeral Neon branch | GAP-43 ✅, GAP-52 ✅, GAP-53 ✅ |
| **F2** Review-money 500 (server half) | `findBusinessMemberUserId` (queries/partner.ts) now filters `revoked_at IS NULL`, agreeing with `listPartnerCashPositions` — a revoked member's `GET /api/partner/{userId}` is a clean 404, not a `TypeError` behind a `!`. All six of the function's callers (setOwnershipShares, capital contribution, grant management, banking event, payout, partner summary) wanted "active member" semantics; none regressed | 1 new integration test (revoked member → 404); full 43/43 on `partner.test.ts`, 50/50 on `partner-summary.test.ts` | GAP-90 ✅ (server half) |
| **A17** Two API validation holes | A shared `.refine()` on `windowQuery`/`vehicleWindowQuery` (route-defs/reports.ts) rejects `from > to` with 400 before any query runs, across fuel-efficiency, lost-days, goodwill and utilisation · `POST /api/payment`'s `partyType: "partner"` now validated against `findBusinessMemberUserId`, closing the one party type that skipped the "never trust an id from the body" rule; `route-defs/payment.ts`'s 404 description corrected to match | 4 new tests (reports.test.ts) + 2 new tests (payment.test.ts); 28/28 and 15/15 | GAP-92 ✅, GAP-93 ✅ |
| **A16** A vehicle's trip history | `GET /api/vehicle/{id}/trip` — every status, newest first, party names resolved (left-joined, matching `listInProgressTripsForBusiness`'s own shape). `dailyOperations`, matching `listVehicleIncidentsRoute`'s gate — operational content, not vehicle master-data | 3 new integration tests; `vehicle.test.ts` 40/40 | GAP-77 ✅ |
| **A15** Change a daily lease's driver (F-4.7) | `POST /api/daily-lease/{id}/change-driver` (`domain/dailyLease.ts::changeDailyLeaseDriver`) — closes the current row, opens a new one carrying the pattern and rate forward unchanged; the close must land before the new row's `INSERT` in the same transaction, since the exclusion constraint is not deferred. Its own `endDailyLeaseRow` query is what A13's B-side auto-close reuses | 8 new integration tests; `dailyLease.test.ts` 19/19 | GAP-62 ✅ |
| **A13** Change a vehicle's arrangement (F-1.2) | `POST /api/vehicle/{id}/arrangement` (`domain/vehicles.ts::changeVehicleArrangement`) — the row is never overwritten. **Pre (UC-94) enforced, not just documented**: a lease not yet `closed` refuses (409, F-2.6 is the only way out); an open `hold`/`booked`/`in_progress` trip covering the effective date refuses too. **Moving off B auto-closes the open daily lease** rather than requiring it closed beforehand — `generate-day-cards` never reads `vehicle_arrangement` at all, so closing the `daily_lease` row is the only thing that actually stops cards, F-1.2's own "daily cards stop" line read literally | 9 new integration tests; `vehicle.test.ts` 40/40 | GAP-54 ✅ |
| **A10a** The management fee that never fires | `domain/management-fee.ts::generateManagementFeeObligationsTx` — one `management_fee` obligation per agreement effective on a period's own start date, idempotent via a new partial unique index (`obligation_management_fee_once`, migration `0011`, scoped to `source_type = 'management_fee_agreement'`) rather than an application check — a bulk `INSERT … ON CONFLICT DO NOTHING`, since Postgres aborts a whole transaction on an uncaught statement error, ruling out a per-row try/catch. Called from `closeAccountingPeriod` for immediacy and a new `generate-management-fee` cron unit for catch-up | 3 new integration tests; **G-1 re-run end to end through the real close endpoint — still exactly 134,000**, confirming the fixture's business has no management-fee agreement and the new hook changes nothing for it | GAP-39 ✅ |

---

## 3. Frontend — Web-P1 to Web-P8b

| Phase | Delivers | Tests |
|---|---|---|
| **Web-P1** Router + first-run | Code-based TanStack Router from a factory (`createAppRouteTree(today, history?)`) so tests inject memory history — no `routeTree.gen.ts`, same reason migrations are hand-written · `FirstRunGate` with `retry: false`; 404 → `CreateBusinessForm`; role → shell · `auth-stub.ts` behind `VITE_AUTH_MODE=stub`, set on the **build**, because env vars inline at build time | 4 e2e |
| **Web-P2** Home reads (backend) | `GET /api/daily-lease`, `/api/day-record`, `/api/trip` — the three lists Home needed and no earlier phase's tests had asked for. `GET /api/trip` filters `status = 'booked'`, not a literal `'in_progress'` that would have returned nothing forever | full matrix each |
| **Web-P3** The Home tab | §3.2's ordered stack from six independent reads · new `AlertStrip` + `EmptyState` · `DayCard`/`ConfirmDayCard` gained `elevated?: boolean` | 50 / 154 |
| **Web-P4** Driver detail | `DriverDetailScreen` + a real working `OffsetSheet`. **W-2 by construction** — `TwoBalances` gets two independent totals and derives its own sentence; the screen never computes a net | 52 / 158 |
| **Web-P5** Vehicle overview + calendar | Backend: four vehicle-scoped reads (`/document`, `/expense`, `/lease`, `/daily-lease`) · frontend: Paperwork/Costs/History sections, a **voided expense stays visible struck through with its reason** · calendar with colour **plus glyph** per day; `findVehicleCalendar` extended with a `LEFT JOIN` onto `day_record` so "ran" and "lost" are distinguishable | 54 / 171; api 24/24 |
| **Web-P6a** Two gaps first | F-1.9 mileage packages, entirely unbuilt (the table existed since `0001` and nowhere else) — **archive, never delete** · `GET /api/lease/{id}/obligation`, a **three-way reassembly** because `obligation` has no `lease_id`: rent → `billing_period`, excess → `mileage_assessment`, post-closure → `lease` directly | api 46/46 |
| **Web-P6b** The lease hub | `/leases/:id` — Terms card + dues + `CollectPaymentSheet` (client-side `AllocationPreview`; the server still performs the real allocation and is the only authority), `AdjustObligationSheet`, `RenewLeaseSheet` · `Timeline` gained per-entry `onClick` | 57 / 185 |
| **Web-P6c** Start a lease | `/vehicles/:id/lease/new` — seven steps, six level-1 fields, **U-2 verified against the actual POST body** having no optional keys at all · mileage-package chips · calendar tap-through, arrangement A half | 58 / 194 |
| **Web-P6d** Odometer + closure | `GET /api/lease/{id}/deposit` (returns `null` when none was taken — that is not a W-56 gap) · `ReadOdometerSheet` · `CloseLeaseScreen`, an in-screen stepper on one route. **Back is removed once step 0 succeeds** — the lease is really `closing`, a posted fact, not a draft | 60 / 206; api 20/20 |
| **Web-P7** Trips | `tripResponseSchema` fixed — `closingDate`/`cancelReason`/`advanceDisposition` were fetched but never projected · `GET /api/trip/{id}/expense` · `BookTripScreen`, `TripDetailScreen`, `CloseTripSheet` (**INV-17 blocks with a `Dialog`**, its first real caller), `CancelTripSheet` (never blocks; a 409 reveals a Refund/Keep-as-fee chooser inline) · calendar tap-through, B/C half | 64 / 226; api 26/26 |
| **Web-P8a** Incidents | Three wire gaps closed: recoveries + claim now reach `GET /api/incident/{id}`; `GET /api/vehicle/{id}/incident`; `GET /api/incident/{id}/expense` · `IncidentScreen` as a **container, not a wizard** — every action stays available regardless of `status` · five single-purpose sheets · `VehicleOverviewScreen`'s one app-bar action became a menu | 71 / 254; api 18/18, 27/27 |
| **Web-P8b** Costs + quick-add | **`GET /api/expense`** — every filter optional, newest first, voided rows included; no 404 for a filter naming another business's row, since the `businessId` AND already excludes it · `RecordExpenseSheet` + `FuelFillSheet` — **`BorneByPaidBy`'s first real caller** · `QuickAddSheet` on `AppShell`'s `＋`, shipping 3 of M-4's 5 actions — **that shortfall carried no gap id until GAP-82 (8 Aug), which is why nothing ever scheduled the other two**. **`borne_by` is never recomputed client-side** — omitted from the request unless explicitly overridden to "Us", so the server's §6.7 matrix always wins. **No `ExpenseListScreen`**, correcting the plan's own guess: §3.3's route map has no business-wide costs route, and `docs/` outranks the plan | 74 / 264; api 20/20 |

**Web-P8b's own open gaps** — GAP-29 (`GET /api/advance` skipped for want of a caller), GAP-30 (`expense.odometer_reading_id` never wired), GAP-31 (no business-member list, so paid-by has one option), GAP-32 (borne-by overrides only to "Us"), GAP-33 (`GET /api/expense` has no caller), GAP-34 (fuel fill's U-3 prefill degrades to the first vehicle).

---

## 4. Every open gap

One table, because scattering these is what let GAP-22 sit unowned through four phases. **Track** resolves in [Plan.md](Plan.md); `—` means nothing schedules it yet.

### Scheduled

| id | Gap | Track |
|---|---|---|
| **GAP-1** | Per-vehicle capability scoping. `managePartnerCapital`/`viewOwnerOnlyReports` are flat, business-wide stand-ins — an `owner_manager` shared one vehicle reaches every vehicle's capital, payouts and reports. **`viewReports` is the same stand-in from the other side**, named 7 Aug 2026: UC-70/71/72 say a `manager` sees only *shared* vehicles, and every P11 report reads across the whole business regardless of role. Same missing `WHERE`-clause scoping and the same root, so it stays one gap rather than a second id — but it is the *manager* who over-sees here, not the owner-manager, and that half was previously unnamed. `ownership_share` has existed as data since P7; what is unbuilt is the scoping on top of it, and which table (ownership or management) decides "his" vehicles for which action. Recorded in `auth/policy.ts` since P7. **Do not build UI implying the scoping exists.** **Status changed 7 Aug 2026 — this stops being theoretical, and the reason is A11.** Every prior edition of this row rested, unstated, on the fact that **no `manager` could exist**: `business-member` was GET-only until 7 Aug, so "a manager sees every vehicle's reports" described nobody. A11 shipped `POST /api/business-member/invite`, `businessMemberRoleSchema` admits `manager`, and `manager` sits in `STAFF` — so **a real manager can now be invited today and will read the whole business's reports, capital and payouts**, against UC-70/71/72's own "only shared vehicles". **This is the fourth instance of the pattern §6 names three times** (GAP-13, GAP-23, GAP-3): a gap's standing reason is a fact with a date on it, and A11 is the date this one expired. It remains genuine design work (which table decides "his" vehicles), so it is not reclassified as a build item here — but "correct to leave" was a judgment about *reachability*, and that judgment no longer holds. **Practical guard until it is scoped: do not invite a `manager` to a real business.** Re-raised, at P0, by `MUST-FIX-FINDINGS.md` §7 — right that it matters, though it did not name the A11 interaction that is what actually changed. **Which table decided 9 Aug 2026, not yet built**: `ownership_share` scopes an owner/owner-manager's vehicles (equity, already effective-dated since P7); `management_fee_agreement` scopes a manager's (the operational assignment, already effective-dated since A10a's own migration `0011`) — split by role, `auth/policy.ts` queries whichever table matches the acting role, no new table or migration needed. The practical guard stands until it is actually wired in | **needs a Plan.md slot — design decided** |
| **GAP-107** | **`attachment` retention is undefined — genuinely undecided, not merely unbuilt.** No document states how long an `attachment` row or its R2 object should live, and the table has no archival column. "Forever" may well be right — condition photos and expense receipts are dispute evidence — but nobody has actually decided that; it is an omission recorded as one rather than smuggled in as an assumption the first purge job would encode by accident. Named in the A7 implementation plan's own Open Questions, filed properly 10 Aug 2026 while wrapping up A7's verification | — |
| **GAP-12** | **Void-and-replace exists for `expense` only.** The other twelve W-50 tables carry the `voided_*` trio structurally, so the mechanism is proven — not each instance of it. A record created by mistake in any of the twelve cannot currently be reversed | **A9b** |
| **GAP-30** | **`expense.odometer_reading_id` has never been wired through any layer** — a DB column since P3, unlike `trip.opening_odometer_id` which P6 wired. Blocks fuel-fill's odometer and trip-link fields (both level 2). Wiring it means deciding whether a fuel fill creates its own `odometer_reading` row transactionally | A8 |
| **GAP-32** | Borne-by can only be overridden to **"Us"**. Overriding to a *specific* driver or customer other than the vehicle's current party needs either a live preview endpoint or a second "who currently holds this vehicle" lookup | A8 |
| **GAP-33** | **`GET /api/expense` has no caller.** Shipped deliberately ahead of any screen (report-adjacent, real value regardless), but §3.3's IA calls for none in phase 1. Revisit if a costs view is ever specified | — |
| **GAP-36** | **`driver` and `customer` have no soft-delete column at all**, so a row created by mistake is permanent. `vehicle.lifecycle` has existed since `0001` but no endpoint ever moves it off `'active'`. `mileage_package`'s `archived_at` + archive endpoint is the reference pattern | **A9b** |
| **GAP-44** | **INV-1 (double-booked vehicle-day) has no `Dialog` anywhere in the client.** `Dialog.tsx`'s own doc comment reserves it to three call sites — INV-1, INV-17, M-10 — and `grep`ing for real `<Dialog` usage (not the doc comments referencing it) finds exactly one: `CloseTripSheet.tsx`, for INV-17. Nothing renders it for INV-1. A double-booking conflict today surfaces as a bare line of `mutation.error.message` in both `StartLeaseScreen.tsx` ([:448](web/src/features/vehicles/StartLeaseScreen.tsx#L448)) and `BookTripScreen.tsx` ([:307](web/src/features/trips/BookTripScreen.tsx#L307)) — confirmed, `VEHICLE_DOUBLE_BOOKED` ([app-error.ts:97](api/src/errors/app-error.ts#L97)) is never referenced anywhere under `web/src`. Not merely a component swap — §9.3 requires "the fix offered inline," and that copy doesn't exist yet either. Found in `UI-UX-REVIEW.md` §2.1, confirmed against source 6 Aug 2026. **Decided 10 Aug 2026, not yet built**: the fix enriches the backend error with real conflict data (the conflicting dates and the entity holding them) rather than a generic name-and-dismiss — the one existing precedent, INV-17's own `Dialog` ([CloseTripSheet.tsx:165-175](web/src/features/trips/CloseTripSheet.tsx#L165-L175)), still only names its block and dismisses; GAP-100 later added a staff settlement caller, but this dialog still does not deep-link to it. Chosen over the cheaper "name it, link to the calendar" option because a precise conflict statement is worth the extra surface. **Bigger than a client-only fix as a result**: `VehicleDoubleBookedError` needs a structured payload, every catch site of the exclusion-constraint violation needs to supply it (`domain/trip.ts` today; check `lease.ts`/`dailyLease.ts` for the same shape), and the wire schema changes to carry it | **B9** |
| **GAP-58** | **`docs/testing/test-manifest.yaml` is 178 cases, every one `not_started`, and it has never been run.** `last_full_run: null`, `last_updated: null`, and `grep` returns `178 × status: not_started` with **zero** in any other state; 38 of them are P0. The file's own header calls itself "the single source of truth for test execution progress" and says "the AI agent reads it before each run" — no run has ever happened. This is not the same claim as "the code is untested" (406 integration + 306 web tests are real and green); it is that **the acceptance spine that maps use-cases to evidence is declared and empty**, so nothing currently proves a *flow* works end to end across UI and API — which is precisely how GAP-51 and GAP-54 both reached production-adjacent QA unbuilt. Untracked anywhere before now. Found by `MUST-FIX-FINDINGS.md` (web edition) §13, confirmed by counting 7 Aug 2026 | — |
| **GAP-59** | **Related ids on one request are each checked against the business, never against each other — so an expense can name a vehicle and a trip that have nothing to do with one another.** [handlers/expense.ts:38-46](api/src/handlers/expense.ts#L38-L46) fetches the trip and the incident purely to prove they belong to this business, **then discards both rows** — `trip.vehicleId` is never compared to `body.vehicleId`, and neither is `incident.vehicleId`. Two same-business ids that describe different events are accepted as one coherent fact. The row then lists under `GET /api/trip/{id}/expense` (filtered by `trip_id` alone, [queries/expense.ts:309](api/src/queries/expense.ts#L309)) *and* under the other vehicle's costs, so one cost is attributed to a trip on one vehicle and to the profit of another. **Not reachable from the client today** — no screen sends both ids (the fuel-fill trip link is GAP-30, unbuilt) — which is what keeps this latent rather than live, the same standing as GAP-1 before A11. **`post-closure-charge.ts` has the same shape** (source, party and vehicle each validated independently). Worth deciding deliberately whether the fix is request-level or a DB constraint; CLAUDE.md's "validate at boundaries" argues for the handler, and DM's preference for constraints-over-code argues for the schema. Found by `MUST-FIX-FINDINGS.md` (backend edition) §6, confirmed against source 7 Aug 2026 | — |
| **GAP-65** | **F-6.6 — the printed slip's "not optional" mechanism doesn't exist**: a signed, expiring, no-login share link, per the spec's own words. B5's "statement link" checklist item is the driver's own authenticated view, a different requirement it does not satisfy. Found by the flow-inventory audit (AUDIT-5) | **A14** |
| **GAP-73** | **Goodwill windows on the wrong date entirely, and returns no breakdown.** Two halves of the same UC-77 work, both deferred with its screen. **(a) Wrong date basis:** `created_at` is when the row was *inserted*, not when the waiver was *given* — U-8 is explicit that "any record can be entered for a past date", so a waiver given 15 June and entered 2 July lands in July, and in the wrong year if it straddles one. **Same class as GAP-56**, the borne-by date bug already treated as a real money defect. `adjustment` has no business-date column (only `created_at` and `posted_period_id`), so the fix is a forward-only migration plus a backfill decision — or windowing on `posted_period_id`, which is consistent with W-40 but turns UC-77's "per year" into a set of period ids. **(b) No breakdown:** UI §11.1 asks for "Single number + a table by reason"; `goodwillResponseSchema` is `{ totalMinor }` and the query has no `GROUP BY`. Group by `adjustment_type` (a real enum) rather than the sparse free-text `reason`. Also unenforced: nothing in the DDL ties `adjustment.sign` to `adjustment_type`, and the sum ignores `sign`. **Distinct from GAP-72**, which is the comparison being wrong for the date it already uses. Found 7 Aug 2026 by the `B4-REPORTS-DESIGN.md` verification pass (§8.3b) | — |
| **GAP-60** | **A void and its replacement are not linked, so "what corrected this?" has no answer in the data.** `expense` carries the `voided_*` trio and W-50's void-and-replace is the prescribed correction path, but **there is no `replaces_id`/`corrects_id` column anywhere in the schema** (`grep`ped `api/src`, `api/migrations`, `packages/shared` — the only hit is [queries/expense.ts:67](api/src/queries/expense.ts#L67)'s own comment saying the column does not exist). So the corrected expense and its correction are two unrelated rows that happen to be near each other in time. UC-97/F-8.6 ("who changed what, readable from the record itself") is satisfiable for a *payment* — `payment_correction` is a real table — and not for the twelve other money tables W-50 covers. **Distinct from GAP-12**, which is about void endpoints not existing; this is about the linkage being absent even where voiding works. Relevant to A9b, which will build ~15 void paths and would otherwise build all fifteen without it. Found by `MUST-FIX-FINDINGS.md` (backend edition) §11, confirmed 7 Aug 2026. **Shape decided 9 Aug 2026, not yet built**: a nullable, self-referencing `replaces_id uuid` column **on each of the 12 tables**, not one shared polymorphic correction table — typed, indexable, joinable without a polymorphic lookup, and Postgres can enforce it as a real per-table FK the way a polymorphic reference can't be. Lands with A9b, as already planned | **A9b** |
| **GAP-86** | **The web test suite passes while emitting repeated non-failing warnings**, so a real regression has noise to hide in: `Not implemented: Window's scrollTo() method` (jsdom has no layout) and `Warning: useRouter must be used inside a <RouterProvider> component!` (a component rendered outside `renderWithRouter`). Neither fails a test today, which is exactly why neither has been fixed. Trivial, and a ride-along for whichever item next touches `web/src/test/` rather than a sitting of its own. Found by `QA-COMPREHENSIVE-TEST-FINDINGS-2026-08-08.md` (P3), 8 Aug 2026 | **19a** |
| **GAP-94** | **The close checklist counts rows that exist, so anything never generated is invisible at exactly the moment it matters.** `unconfirmedDays` counts `day_record` rows in `state = 'open'`; if `generate-day-cards` has not materialised a period's days, the checklist reports zero unconfirmed days for a month that has plenty. **Shared a root with GAP-88 (closed 9 Aug, D-9), but is not fixed by it**: D-9 only materialises a daily lease's own opening window synchronously — an *ongoing* lease still depends on the nightly cron to extend the horizon into each newly-opened period, and a missed run there still leaves this checklist blind. Surfacing where it is most expensive — the irreversible step. It compounds with two other absences the checklist also cannot see: management-fee obligations that no generator writes (GAP-39) and surplus payments that are not durable facts (GAP-5). U-7's "warns, never blocks" is the right shape and is not in question; the question is whether the warning set is complete enough to be trusted, and today it is complete only about facts already written. Found by BE-09, 9 Aug 2026 | **A10a-adjacent** |
| **GAP-95** | **⚠️ Re-raised, already declined once — the collapsed `More` disclosure label is UI §6.3's own specification, not a defect.** Four forms hide their optional section behind a disclosure labelled only `More` (driver fees/mobile, customer contact details, vehicle paperwork, daily-lease end date), which is genuinely hard to scan and collides with the bottom nav's own `More`. **`ui-ux-guidelines.md:509` specifies exactly this** — *"Labelled 'More' on first use, then the section name once opened"* — and `Disclosure.tsx` implements it with the rule quoted in its docstring. Declined 8 Aug on that basis (§6, item 17) and **re-raised unchanged by `QA-COMPREHENSIVE-TEST-FINDINGS` on 9 Aug**. The position has not changed: **if the argument is right, UI §6.3 is the line to change, deliberately, through a `doc-change`** — it does not become a bug by being reported twice. Kept as a row only so the third report finds the answer instead of filing it again | **not scheduled — `doc-change` or nothing** |
| **GAP-98** | **Three phase-2 report endpoints are mounted and reachable while the phase-1 UI deliberately omits them.** `routes/reports.ts` serves `/ageing`, `/goodwill` and `/utilisation`; `ReportsCatalogueScreen` offers exactly six reports and `App.test.tsx` asserts the phase-2 routes do not resolve. Backend-ahead-of-UI is this project's normal order and not itself a defect — **the risk is that two of the three are known-incomplete against their own use case and are reachable anyway**: goodwill sums `amountMinor` ignoring `adjustment.sign` (already recorded as GAP-73b) and utilisation returns day counts without UC-79's revenue-per-available-day (already GAP-19). Worth an explicit note on the route-defs that these are unfinished surfaces, so the next person to find them does not read "mounted" as "done". Found by BE-10, 9 Aug 2026 | — |

### Closed

**38 gaps, closed 5–9 August 2026**, compacted 9 August — the full accounts are in git history and in each item's section in [Plan.md](Plan.md). Kept as one line each because a closed gap's remaining value is "this id means this thing, and it is done"; where a closure taught something that still binds, the lesson lives in [§5](#5-the-traps) and the row points at it.

| id | Closed | What it was, and what closed it |
|---|---|---|
| **GAP-3** | 6 Aug, out of sequence | `confirmDay` keyed idempotency on a row *existing*, so P13's `open` placeholders silently discarded real confirmations in production. Fixed by a state-guarded `UPDATE` (`confirmOpenDayRecord`). Lesson in §5 |
| **GAP-4** | 5 Aug (A2) | Partner out-of-pocket had no current-account entry. `GET /api/partner/{userId}` sums `expense.paid_by_user_id` at read time — a `SUM` cannot drift from its source; a second money table could |
| **GAP-9** | 5 Aug (A2); 11 Aug (B2 client) | F-7.6/F-7.3 became the composed `GET /api/partner/{userId}`. The frontend now renders it in `PartnerDetailScreen`, reached from `/cash`'s partner list |
| **GAP-13** | 5 Aug (A3) | Close checklist's `unconfirmedDays` count. **Partly superseded by GAP-94** — it counts rows that exist |
| **GAP-22** | 5 Aug (A4); 11 Aug (B6 client) | Customer obligation + payment reads. `/people/customers/:customerId` now renders `CustomerDetailScreen`, showing customer details, outstanding dues, payment history and a collect-payment action over the existing party-level payment flow |
| **GAP-23** | 6 Aug (A6) | `bookTrip` posts a `trip_fare` obligation, `cancelTrip` voids it; migration `0009` widened `obligation_kind_check` |
| **GAP-24** | 5 Aug (A5); 11 Aug (B5 client) | `GET /api/driver/{id}/view` and `GET /api/driver-view` now have callers: staff driver detail renders the history read, and the linked driver's own `/me` screen renders the driver-view read-only shape. A14's statement/share link remains separate |
| **GAP-29** | 5 Aug (A5) | Advances got a caller inside the composed read; no separate `GET /api/advance`, deliberately |
| **GAP-31** | 5 Aug (A2); 11 Aug (B2/UI-8 client) | `GET /api/business-member` now serves three callers: Members, opening-balance partner selection, and `BorneByPaidBy`'s paid-by picker in `RecordExpenseSheet`. The 11 Aug UI pass also corrected the response contract to include the membership row `id`, because revoke/change-role address membership ids, not user ids. Borne-by specific-party override remains GAP-32/A8 |
| **GAP-35** | 5 Aug (A9a) | Migration `0008` narrowed `0006` so a void cannot escape a closed period |
| **GAP-37** | 5 Aug (B0) | `/more` became a real screen |
| **GAP-38** | 5 Aug (A3) | `GET /api/payment`, carrying the W-49 linked-driver test class |
| **GAP-40** | 5 Aug (B0) | Sign-out — query cache cleared *before* the SDK call, never after |
| **GAP-41** | 8 Aug | `GET /api/reports/overheads?periodId=`; a real `0` when nothing was recorded, never `NotAvailable` |
| **GAP-42** | 6 Aug (A0) | `createBusiness` assigned the creator `owner`, the one role with no screen — every signup landed in `NotBuiltYetScreen`. One word. Production had never had a signup, so nothing needed correcting |
| **GAP-43** | 7 Aug (A11) | Member and driver invite/redeem (F-1.4/F-1.8) — a second owner, a manager and every driver can get an account |
| **GAP-49** | 7 Aug (B11) | No page background outside `AppShell` — illegible dark mode on the first screen every user sees. Fixed on `html` in `@layer base`, plus the missing `color-scheme`. Lesson in §5 |
| **GAP-50** | 7 Aug (B11) | The `aria-hidden` focus violation fires on **open**, not close (vaul's `autoFocus: false`); focus restore had also never worked. Both fixed |
| **GAP-51** | 7 Aug (B10) | F-1.7 "set up the daily lease" had no implementation anywhere, client or spec. **LT-7's own live-test concern is easier now than when this closed**: GAP-88 (9 Aug, D-9) made the placeholder appear synchronously at setup rather than waiting on the next cron run, so LT-7 is confirming an immediate fact, not a delayed one |
| **GAP-52** | 7 Aug (A11) | `0010` made `business_member`'s pair-unique partial (`WHERE revoked_at IS NULL`) so revoke-then-re-invite works. **See GAP-90 — a second query still disagrees about what a revoked member is** |
| **GAP-53** | 7 Aug (A11) | `0010`'s explicit `business_member_audit` trigger; `0002`'s discovery loop only reaches `posted_period_id` tables |
| **GAP-56** | 7 Aug (A12) | Borne-by resolved as of `spentOn` rather than today — a back-dated cost had been landing on whoever holds the vehicle now. Four tests, each verified to fail pre-fix |
| **GAP-57** | 7 Aug (A12) | Trip receivable surfaced on `TripDetailScreen`; `CollectPaymentSheet` generalised off `leaseId` |
| **GAP-61** | 8 Aug (B12) | Opening balances (F-0.2), reached from `/more` |
| **GAP-63** | 8 Aug (B13) | Bigger than filed: `POST /api/payment` had never accepted `direction: "paid"`, so there was no backend path to a driver's `owed_by_us` balance at all |
| **GAP-64** | 8 Aug (B13) | `AdvanceSheet` |
| **GAP-66** | 8 Aug (B13) | `DepositSheet`, take-only by design |
| **GAP-72** | 8 Aug | Goodwill window dropped its last day — **and the first-draft fix was itself wrong**, a different `AT TIME ZONE` overload. Lesson in §5 |
| **GAP-74** | 8 Aug | Partner `balanceMinor`, all-time, a bounded loop over the periods a business has actually run |
| **GAP-75** | 8 Aug (B14) | Trip receivable rendered `amountMinor` instead of a hardcoded zero |
| **GAP-76** | 8 Aug | Field-specific required-copy on the driver and vehicle forms, replacing zod's generic fallback |
| **GAP-78** | 8 Aug | `TwoBalances` stopped repeating the driver's name under the page title |
| **GAP-79** | 8 Aug | "Incident with no description" |
| **GAP-80** | 8 Aug | Three `NotAvailable` reason strings rewritten. **Its recorded residue is now GAP-99** |
| **GAP-81** | 8 Aug | Shared `ExpenseCostRow` + `VoidExpenseSheet`; wiring three screens also deleted two stale duplicate label maps |
| **GAP-84** | 8 Aug (F1) | Vehicle arrangement validated in both start handlers and both screens. **Paired with A13**, done the next day; sibling GAP-87 left unscheduled on purpose |
| **GAP-85** | 9 Aug | `MoneyField`'s blank trigger reads `Enter {label}`. `AmountPad`'s running total and `IncidentScreen`'s computed zeroes were checked and deliberately left — both are real zeroes |
| **GAP-90** | 9 Aug (F2, server half); 10 Aug (F2, client half via GAP-101) | `findBusinessMemberUserId` filters `revoked_at IS NULL`, matching `listPartnerCashPositions` — a revoked member's summary is a clean 404, not a `TypeError`. **The client half closed the next day, as part of GAP-101** — `ReviewMoneyScreen` (GAP-90's own screen) now renders that 404 through `QueryState`, a real failure message instead of an eternal spinner |
| **GAP-92** | 9 Aug (A17) | A shared `.refine()` on `windowQuery`/`vehicleWindowQuery` (route-defs/reports.ts) — `from > to` now 400s on all four affected reports before any query runs, closing the confident-empty-result path W-56 argues against. The client's own report date pickers (ACA-03) are untouched, but the wire itself no longer accepts the nonsense range regardless of what sends it |
| **GAP-93** | 9 Aug (A17) | `POST /api/payment`'s `partyType: "partner"` now validated against `findBusinessMemberUserId`, closing the one party type that skipped the "never trust an id from the body" rule; `route-defs/payment.ts`'s 404 description corrected to match the schema |
| **GAP-39** | 9 Aug (A10a) | `domain/management-fee.ts` — one `management_fee` obligation per effective agreement per period, idempotent via a new partial unique index (`obligation_management_fee_once`, migration `0011`) rather than an application check. Called from `closeAccountingPeriod` for immediacy and a new `generate-management-fee` cron unit for catch-up. G-1 re-run end to end through the real close endpoint, still exactly 134,000 |
| **GAP-54** | 9 Aug (A13) | `POST /api/vehicle/{id}/arrangement` — the row closes and a new one opens, never overwritten. Pre (UC-94) enforced: a lease not yet closed or an open trip covering the effective date both refuse (409); moving off B auto-closes the open daily lease, since `generate-day-cards` never reads `vehicle_arrangement` and closing the row is the only real way to stop cards |
| **GAP-62** | 9 Aug (A15) | `POST /api/daily-lease/{id}/change-driver` — closes the current row, opens a new one carrying the pattern and rate forward. Its own `endDailyLeaseRow` is what GAP-54's B-side auto-close reuses |
| **GAP-77** | 9 Aug (A16) | `GET /api/vehicle/{id}/trip` — every status, newest first, party names resolved. `dailyOperations`, matching the incidents-tab gate rather than this file's default |
| **GAP-88** | 9 Aug (D-9) | `startDailyLease`/`changeDailyLeaseDriver` materialise their own rolling 90-day horizon synchronously, inside the transaction that creates the row, using the identical write `generate-day-cards` already runs — the cron now only extends the horizon it no longer originates. Settled by `data-model.md` §4.1/§17 D-9 first. **GAP-94 is not the same fix** — it's the ongoing-cron-reliability half, still open |
| **GAP-10** | 9 Aug (A10b) | `recordCustomerContribution` now opens an `owed_to_us`/`customer_contribution` `Obligation` against the incident lease's own customer (migration `0012` gives `incident_recovery.obligation_id` a real FK, toothless since `0001`); `recordRecoveryReceived` settles that obligation and posts the real `payment`/`payment_allocation` pair alongside its existing `incident_recovery` update, so the two never disagree about the same money. Settled by `user-flows.md` v1.1.5 first |
| **GAP-87** | 9 Aug | `bookTrip` now mirrors the client's own `canBookTrip` gate (arrangement B or C; `null` and `A` both refused, 409 `VEHICLE_ARRANGEMENT_MISMATCH`) — the sibling GAP-84 deliberately left unscheduled. 35 test call sites across 4 integration files had never set a standing arrangement; fixed and independently re-verified end to end, G-1 untouched |
| **GAP-101** | 10 Aug | `QueryState`/`useQueryState` (UI §6.4/M-28, `ui-ux-guidelines.md` v1.2.5) — a real four-state read contract (idle/pending/error/ready) across all 30 files with a genuine `useQuery` call, migrated worst-mode-first over four waves; closes F2's client half (`ReviewMoneyScreen`) and finds two write-corruption-shaped bugs along the way (`OpeningBalanceScreen`'s full-replace save, `CloseLeaseScreen`'s deposit-settlement skip), both now blocked rather than silently degraded. A `check-forbidden.mjs` rule enforces it going forward — caught one genuine miss (`CorrectPaymentSheet.tsx`) before it could ship. Full account in §0's own dated entry |
| **GAP-16** | 10 Aug (A7, merged PR #20) | R2 uploads for expense receipts — migration `0013`'s `attachment` table and void trio, five error codes, `POST`/`GET`/void endpoints through the Worker `R2` binding (never presigned), client upload infrastructure wired into `RecordExpenseSheet`/`FuelFillSheet`, a receipt-viewing surface on `ExpenseCostRow`. **Correction to the row's own prior text**, which read "still not merged" — PR #20 merged into `develop` on 10 Aug, CI green (26-case attachment suite + full 480/489, golden fixtures unmoved). **Not yet on `main`** — no `develop`→`main` PR is open (§0/§1's Deployed row). The other four call sites (condition photos, incident damage, the comparison) and GAP-17 (the photo pipeline's main-thread encode) stay unplanned and unbuilt regardless |
| **GAP-91** | 10 Aug (B9) | `CloseMonthScreen`'s confirm now passes `variant="destructive"` to `DialogConfirmFooter` — the one irreversible action in the product no longer wears the ordinary primary button |
| **GAP-89** | 10 Aug (B9) | `tabForPathname` widened: `/review*`, `/reports*`, `/period/close` and `/opening-balances` highlight **More** (they're rows under that hub already); `/trips/:id`, `/incidents/:id` and `/leases/:id` (including its own `/close`) highlight **Vehicles**, the domain parent regardless of origin |
| **GAP-45** | 10 Aug (B9) | `TripDetailScreen`'s title drops the year when the whole range sits inside the business's current year, showing it on both halves only when the range crosses a year boundary or falls outside it — no more unconditional doubled year clipping at 360px |
| **GAP-46** | 10 Aug (B9) | Every occupied `VehicleCalendarScreen` cell carries an `aria-label` naming its own state, the same wording `LegendRow` already renders — `cellStyle()` gained a `label` field alongside `wash`/`glyph` |
| **GAP-55** | 10 Aug (B9) | `autoComplete` tokens added at the four call sites that actually collect personal/business info — driver and customer Name (`name`), Mobile (`tel`), Address (`street-address`), Business name (`organization`). `Input` already spread arbitrary props, so no primitive change was needed, only the call sites naming their own token |
| **GAP-96** | 10 Aug (B9) | `Section` gained an optional `total` slot (heading-adjacent, backward compatible for its other 6 callers); `VehicleOverviewScreen`'s Costs section passes its non-voided total, the same exclusion rule `TripDetailScreen`'s "Costs so far" already uses |
| **GAP-97** | 10 Aug (B9) | `VehicleOverviewScreen` gained a `canBookTrip` gate (arrangement B or C, mirroring `bookTrip`'s own server-side check) and a "Book trip" vehicle-action wired to `/vehicles/:id/trip/new`. **M-25 rode along in the same sitting**: `RenewLeaseSheet`/`StartLeaseScreen`'s "Excess rate per km" renamed to "Excess fee per km" — the U-6 vocabulary lock reserves "rate" nowhere, and "fee" already matches "driver day fee"/"driver trip fee" |
| **GAP-99** | 10 Aug (B9) | The two `NotAvailable` strings on `StartLeaseScreen`/`CloseLeaseScreen` no longer say "presigned upload storage" — reworded to "condition photos aren't supported yet" / "return condition photos aren't supported yet". **Correction to the row's own prior note**: it predicted this would "die with A7" — A7 shipped expense receipts only (GAP-16's row), condition photos are still unbuilt and still explicitly out of scope, so the copy fix was live work, not something A7 made moot |
| **GAP-106** | 11 Aug (B9) | `CashPositionReportScreen.tsx`'s three "Held for customers" strings (chart segment, chart caption, table caption) reworded to "Held as deposits" — matching what `sumDepositsHeld` actually is, a party-agnostic total by design. `CashPositionReportScreen.test.tsx`'s own pinned-wrong assertion corrected, plus a new assertion that no "customer" wording renders at all |
| **GAP-104** | 11 Aug (F4) | `useMobileHistoryDismiss` rebuilt as one module-level stack manager (`Sheet.tsx`'s sole caller, so every sheet in the client inherits it unchanged) instead of each `Sheet` independently pushing/popping `window.history` and listening for its own `popstate`. Registrations/deregistrations within one React commit are diffed against a microtask-deferred flush, so `ActionSheet`'s close-then-open handoff nets to zero real `history` calls instead of racing an async `back()` against a synchronous `pushState`; the one global `popstate` listener always closes the topmost *registered* sheet rather than whichever sheet's own listener happened to still be attached, which is what fixes a nested `Sheet` (e.g. `MoneyField`'s `AmountPad`) closing its still-open parent on save. **Verified against the live symptom, not just in theory**: the pre-fix code was confirmed broken under real Chromium touch emulation (`console.log`-instrumented, not just read) — `history.back()`'s `popstate` lands 100–300ms after the click, non-deterministically, which is also why the first attempt at an e2e regression (a 300ms wait) passed against broken code by accident; the shipped spec waits 1s and was confirmed to fail reliably against the old code and pass reliably against the new one, both across repeated runs. New coverage: `useMobileHistoryDismiss.test.tsx` (manager-level, incl. the handoff-nets-to-zero and topmost-only-closes invariants), `Sheet.test.tsx` (two real nested `Sheet`s, touch-emulated — this is the one that actually reproduced the nested-closes-parent bug pre-fix; the jsdom-level handoff check did not, confirmed, and says so in its own comment), `QuickAddSheet.test.tsx` (switched from a no-op `onOpenChange` that could never have caught this to a real `useState`-backed one), and a new Playwright project (`mobile-360x640-touch`, `hasTouch`/`isMobile`) plus `e2e/mobile-sheet-history.touch.spec.ts` — the existing e2e project never carried touch emulation, so this bug could not have reproduced there either. **Not yet closed**: a real iOS Safari / Android Chrome pass (F4's own acceptance checklist item), which no automated run in this environment can substitute for |
| **GAP-103** | 11 Aug (F3) | The undecided fork (materialise on commit vs. a parallel ledger) decided **materialise**, into the exact tables `listReceivables`/`sumDepositsHeld`/`sumOutstandingByDirectionForDriver`/`listPartnerCashPositions` already read — no new report read path. `customer_due`/`driver_arrears`/`owed_to_driver` → `obligation` (new `kind: 'opening_balance'`, migration `0014` widens `obligation_kind_check`); `deposit_held` → `deposit` + a `taken` `deposit_movement`; `advance_outstanding` → `advance`; `cash_held` — which had no table of its own at all, `heldMinor` being pure derived arithmetic — → an unallocated `payment` (`direction: 'received'`), the same shape a real surplus payment already leaves. A new non-money tracking table, `opening_balance_posting` (migration `0014`, not in `assert_period_open()`'s array, same reasoning `0013` gave `attachment`), links each entry to what it produced, so F-0.2's post-commit correction clause reverses the prior generation (`obligation`/`advance` void by id; `payment` flips to `status: 'reversed'`; `deposit_movement` gets a real offsetting entry, since `sumDepositMovements` never actually consults `voided_at`) before posting the corrected figures. Two new integration tests (all six kinds against their three reports; a post-commit correction leaving exactly the corrected figure, never both). Full API suite 490/490 across 33 files, golden fixtures unmoved, DM §13 drift check clean |
| **GAP-83** / **GAP-105** | 11 Aug (B9) | `DateField` rebuilt around one real `<input type="date">` as the actual click/tap/Tab target — absolutely positioned, full-size, `opacity-0`, directly over a decorative `aria-hidden` weekday display — replacing the old visible decoy button plus a separately-focusable `sr-only` input behind it. **GAP-83** (the stray 1×1 Tab stop with no focus indicator): fixed by construction — there is only one focusable element now, confirmed by a real Chromium tab-order trace (`Vehicle` picker → Tab → lands directly on the `From` date input, `aria-label="From: Wed 13 May, opens a date picker"`, screenshotted) — plus `focus-within` on the wrapper gives it a visible ring, since `opacity-0` on the input would otherwise hide its own native outline along with everything else about it. **GAP-105** (no fallback when `showPicker()` is unavailable): fixed by the same change — a direct click/focus on a real `<input type="date">` opens the browser's native date UI on its own in every browser, `showPicker()` now only a same-tap enhancement layered on top, never the only path in; a test asserts jsdom (which has no `showPicker` at all) still lets the input take a value with no call to it. The first closure kept Today/Yesterday behind a `showShortcuts` prop and disabled it only for report ranges; GAP-108 supersedes that product decision without changing this row's native-picker/a11y fix. **Not done**: MP-06's own real-Safari confirmation — this pass again could only confirm the fallback logically (no `showPicker` present, still works), not against an actual Safari build |
| **GAP-108** | 11 Aug (B9) | M-17 revised and `DateField` simplified globally: Today/Yesterday shortcut chips removed from normal single-date fields as well as report ranges, leaving one native date picker with the same weekday display and focus ring. Root cause: the earlier fix treated the confusion as report-range-only, but the duplicate affordance exists on every date field and becomes harder to scan in any form with several dates. `showShortcuts` was removed, the two report opt-outs disappeared, and tests now assert no shortcut buttons render and a From/To pair is exactly two date inputs, not six controls |
| **GAP-47** | 11 Aug (B9) | M-26's landscape chrome built in the shared primitives: `AppShell`'s tab bar now compacts to 44px below `md` in landscape and makes labels visually `sr-only`, while `Screen`'s app bar compacts to 44px with tighter horizontal padding, smaller title text, tighter content padding, and adjusted scroll padding for sticky actions. Root cause was exactly the filed one: both primitives had hardcoded portrait-only `h-14` chrome, so every screen inherited the 640 x 360 squeeze. New primitive tests pin the landscape classes; the focused run matched 28 files/215 tests and passed |
| **GAP-48** | 11 Aug (B9) | `ToastProvider`, `useToast` and `ToastViewport` built and mounted through `App`/`AppShell`, replacing the inert `<div id="toast-root" />` with a real M-11 host. Toasts default to 5 seconds, are dismissible, use a polite live region, stack above the tab bar instead of overlaying sticky actions, and can carry an optional action. Root cause was absence of the primitive, not absence of a particular call site. No fake undo was wired: qualifying writes still need deliberate call-site decisions, and money/message/settlement writes continue through recorded correction flows. New tests cover render, dismiss, action, timeout and the no-provider host fallback |
| **GAP-82** | 11 Aug (B15) | `QuickAddSheet` now renders all five M-4 actions in the fixed order: Fuel, Expense, Payment received, Payment made, New trip. Payment received opens a party picker over customers and drivers, then posts `POST /api/payment` with `direction: "received"`; Payment made opens a driver picker, then posts the same endpoint with `direction: "paid"`. Root cause was reachability, not backend absence: B13/A17 had already made the schema and API support direction and party type, but Quick Add still had no party picker in front of those writes. Partner payouts stay in B2/F-7.2 rather than this generic payment path. New tests cover order, both payment directions, and picker read failures |
| **GAP-67** | 11 Aug (B2 client) | `MileagePackagesScreen` under More now calls the existing `GET/POST /api/mileage-package` and `POST /api/mileage-package/{id}/archive` contract: list active and archived packages, create a package with name/daily limit/excess fee per km, and archive without touching any lease terms already copied onto a lease. New tests cover list, create and archive |
| **GAP-69** | 11 Aug (B2 client) | `PartnerDetailScreen` now records partner payout or partner settlement via `POST /api/partner-payout`, and reads the same rows back through `GET /api/partner-payout?userId=`. It stays on the partner/cash screen, never Quick Add and never vehicle costs, matching F-7.2/UC-63 |
| **GAP-102** | 11 Aug (B6-adjacent client) | `VehicleOverviewScreen` gained `RenewVehicleDocumentSheet` and a "Renew paperwork" vehicle action. Paperwork rows are tappable, the sheet posts `PUT /api/vehicle/{id}/document` with document type, expiry date and optional reference, then invalidates both vehicle paperwork and Home paperwork warnings. Root cause was a pure caller absence: the metadata endpoint existed, but the UI could only read it, leaving expiry warnings permanent |
| **GAP-100** | 11 Aug (B13 remainder client) | Trip detail now records a trip-scoped advance by passing `tripId` into `POST /api/advance`, and staff Driver detail can settle open/part-settled advances through `POST /api/advance/{id}/settle`. Mine remains read-only. Residual, deliberately not hidden: `DriverViewAdvance` does not expose `settledMinor` or `tripId`, so the settlement sheet cannot yet show remaining amount or trip context; the backend still enforces over-settlement and trip-close rules |

### Recorded, unowned, and correct to leave

Each is unreachable, unbacked by the schema, or genuinely out of scope. Listed so the absence stays a decision.

| id | Gap |
|---|---|
| **GAP-2** | F-4.3's effective-dated rate change and F-4.6's bulk week-confirm — real features the size of F-4.2 itself. Several screens' "current rate" simplifications are safe **only** while this stays unbuilt |
| **GAP-5** | `recordPayment`'s surplus comes back as `unallocatedMinor` but is never applied forward against a future due. **Re-raised 10 Aug** by `PENDING-REVIEW-ITEMS-2026-08-10.md` as an active checklist item, without noting it was already parked here — evaluated the same day: nothing about reachability or size has changed since it was filed, it is still a real feature (a durable credit ledger, its own allocation-on-read path, retry-safety) rather than a fix, so it stays recorded-unowned. Worth its own Plan.md slot if picked up, not a B9-sized sitting |
| **GAP-6** | F-8.4's deposit-apply — reusing `allocateAgainstOldest` verbatim would wrongly create a `payment` row for money already held |
| **GAP-7** | Trip `hold` (ST-5) and `in_progress` are unreachable — `bookTrip` writes `booked` outright. `VehicleCalendarScreen`'s hold-outline cell and `T?` glyph render and are tested but cannot occur. **Re-raised 10 Aug** by `PENDING-REVIEW-ITEMS-2026-08-10.md`, with a sharper framing worth keeping for whoever picks this up: either build `hold`/`in_progress` end to end, or stop rendering UI/legend language that implies a state nothing can produce. Still correct to leave unbuilt — no live report has found a manager actually confused by it — but the reframing is real and cheaper than the original ask |
| **GAP-8** | A genuine concurrent double-close race is unguarded (status-based idempotency only), the same accepted gap billing-period's wrapper has. **Re-raised 10 Aug** by `PENDING-REVIEW-ITEMS-2026-08-10.md` as an active item; evaluated and left here — the accepted precedent it matches hasn't changed, and nothing has made the race more reachable than it always was |
| **GAP-11** | W-11's "hidden unless enabled" has no backing settings field anywhere in the schema — the claim action is always offered |
| **GAP-14** | Stacked partial corrections against one payment are allowed by schema and implementation; only the single-correction path has a test |
| **GAP-17** | The photo pipeline still runs on the main thread — no Worker + 3s-timeout wrapper |
| **GAP-18** | UC-73 (yearly) and UC-99 (export) — both product-phase Second |
| **GAP-19** | UC-79's `revenuePerAvailableDayMinor` — needs its own date-range-scoped earned calculation |
| **GAP-20** | `isPatternDay`'s "alternate" reference point is a judgment call (lease `effective_from` = day zero); F-1.7's skippable-individual-days has no column to hold an exception |
| **GAP-21** | One business timezone for every business — `scheduled.ts` calls `businessToday()` once. **Re-raised 10 Aug** by `PENDING-REVIEW-ITEMS-2026-08-10.md`; evaluated and left here — every business this product serves runs in `Asia/Colombo` (CLAUDE.md's own "Time" rules are written for one business timezone, not per-business), so this is a real limitation only if a second timezone is ever a real requirement, which nothing in `docs/` currently asks for |
| **GAP-25** | Nothing writes `effectiveTo` on a `daily_lease` — no closure flow for arrangement B the way F-2.6 exists for A |
| **GAP-26** | "Off the road" is not rendered on the calendar — no write path marks a vehicle-day unavailable outside an allocation |
| **GAP-27** | Arrangement C's orange reuses `--color-serious` — a colour match, not a semantic one. Worth a real token, not a raw hex |
| **GAP-28** | Error monitoring deliberately deferred. Tail Worker needs a real alert destination; Sentry needs an account and DSN. Workers Logs (`observability.enabled`) stays the only observability **until there is an on-call person and a channel to page** — revisit then, rather than build an alert that pages no one |
| **GAP-34** | Fuel fill's U-3 prefill ("the vehicle that has something pending") degrades to "first vehicle in the business's list" — nothing in this schema tracks which vehicle a manager last touched, the same simplification `HomeScreen`'s most-recently-used already made |
| **GAP-15** | F-8.4's "deduct it from his fee" — `POST /api/offset` applied afterward, never a combined one-tap path. **Moved here 8 Aug 2026 from Scheduled/B3**, where it sat on the P10 backend's existence rather than on any phase reading: F-8.4 is *Phase: 2* and UC-91 is phase **Second**. Travels with `WriteOffSheet`/`PostClosureChargeSheet` (F-8.3/UC-90, same phase), withdrawn from the queue the same day |
| **GAP-68** | F-3.5's predictive half — "system uses history + odometer to prompt next time" for scheduled maintenance. The base recording action is already covered by the generic expense form (`servicing`/`repairs` categories, GAP-30/A8 covers the odometer link); this is the missing prompt engine specifically, a real feature rather than a form. Found by the 8 Aug flow-inventory audit (AUDIT-8). **Re-raised 10 Aug** by `PENDING-REVIEW-ITEMS-2026-08-10.md`; evaluated and left here — still a real feature, not a fix, and nothing since 8 Aug has moved it up the queue |

---

## 5. The traps

Things that cost a session each when rediscovered.

**Testing and environment**

- **`npm run check` does not run the api integration suite.** `api`'s `test` is `vitest run --project unit`; `test:integration` is separate and needs a live Neon branch. A green `check` says nothing about endpoint behaviour.
- **`mintUser(db, ctx, businessId, role)` takes the role as an explicit argument everywhere it is called — which means no test in this codebase has ever exercised the role a real signup actually gets.** GAP-42's bug (`createBusiness` granting `owner`, the one role with no screen) sat behind 386 green integration tests for exactly this reason: every one of them minted its `owner`/`manager`/`driver` directly rather than going through `POST /api/business` and reading back what it assigned. `business.test.ts`'s own happy-path test does call the real endpoint — and had asserted the wrong role as correct, so even the one test positioned to catch this had been pinned to the bug (the same shape as A9a's `voidExpense` regression test). Worth a standing question on any new setup/provisioning path: is there a test that reads back what the system decided, or only tests that assume a value and mint it directly?
- **The Neon branch has a connection ceiling.** Every integration file opens its own `Pool` and holds it to `afterAll`, so file-level parallelism opens all of them at once — `vitest.config.ts` sets `fileParallelism: false` for exactly this. On one long-lived interactive branch it still surfaces as `Connection terminated unexpectedly` at a **different, random** test each run. **A failure that moves between runs, in files the change never touched, is the environment.** Re-run the touched file alone before believing a regression. Web-P6d, Web-P7, Web-P8a and Web-P8b each hit it; P8a's session saw 15 failures across 7 untouched files, P8b's saw 2 in `auth.test.ts` that passed 9/9 alone.
- **A dedicated Neon branch plus `TEST_PARALLEL=1` cuts a full local `test:integration` run from ~29 minutes to ~105 seconds** — a ~16x speedup, measured both ways on the *same* branch (`test-parallel`, `br-lingering-violet-afr5li7f`, forked from `main`) so nothing else differs. `api/.dev.vars`'s `TEST_DATABASE_URL` now points at it; the old shared `test` branch is unused, not deleted. The env var sets `fileParallelism: true` with `poolOptions.threads.maxThreads: 4` (`vitest.config.ts`) — bounded well under the ~14-file ceiling above, so it doesn't reintroduce that failure mode. Opt-in and off by default; CI and anyone who hasn't set up a personal branch are unaffected.
- **The connection flakiness above can also surface as a foreign-key violation mid-cleanup, not just a raw timeout.** Found running `TEST_PARALLEL=1` twice in a row: run one failed 6 tests in `mileage-assessment.test.ts` (`billing_period_lease_id_fkey`), run two failed 2 in `day-record.test.ts` (`day_record_daily_lease_id_fkey`) — different file, different constraint, different count, both files 25/25 re-run alone. Same "moves between runs" signature as above, not a cleanup-ordering bug — checked `trackCreatedLease`'s teardown before concluding that, and it's correctly written (re-queries and deletes child rows before the parent, one sequential closure, no bug found). A dropped connection mid-closure can still leave a delete half-done; the fix is the one already established above, not a change to test support.
- **`audit_log` no-ops every `DELETE`** (`DO INSTEAD NOTHING`, migration `0001`). An `app_user` is permanent from the moment anyone acts. Test cleanup must not try — it will compile, run, and silently do nothing. The fix lives in `tests/support/auth.ts`/`factories.ts`; production only ever revokes `business_member`.
- **17 ESLint warnings are expected**, all `react-refresh/only-export-components` in `router.tsx` (15, one per route component — 14 until B10 added `StartDailyLeaseRoute`), `ApiContext.tsx` (1) and `AuthActionsContext.tsx` (1, added in B0 — the same context-with-a-hook shape as `ApiContext.tsx`). A direct consequence of the code-based-router decision plus that shared pattern. **The count rises by one with every new route and that is correct**; 0 *errors* is the bar. Do not "fix" these by splitting the route tree or the contexts.
- **`eslint-plugin-jsx-a11y` is deliberately absent** — its published peer range caps at ESLint 8/9 and this repo is on ESLint 10. **axe-core in Playwright is the a11y gate** until it catches up.
- **Two test files sharing a base name with only the extension differing breaks typescript-eslint's `projectService`, reproducibly, with a "not found by the project service" parsing error — not an environment or caching artefact.** Found building B4: five screens each grew a `Screen.test.ts` (pure view-model functions) alongside the existing `Screen.test.tsx` (render tests), and every `.test.tsx` half of a same-stem pair started throwing, across separate process invocations, with `cwd` and the file's own existence both independently confirmed correct first (a red herring this session chased for several tool calls before testing the actual variable — file-stem collision — rather than the environment). `web/tsconfig.json`'s `include` glob covers both files individually; the crash is specific to the pair coexisting. **Fix: merge into the one file this codebase's own convention already uses** (one test file per screen, `StartLeaseScreen.test.tsx`'s own shape — pure-function `describe` blocks and render `test`s in the same file) — not a `tsconfig`/`allowDefaultProject` workaround, since the simpler fix is also the one that matches how every other screen here is already tested.
- **Neon's Free plan rejects an explicit `suspend_timeout` on branch creation outright** — a `412` with no error body surfaced by `create-branch-action`, even though the value passed (300s) matched the plan's own fixed default exactly. Confirmed against Neon's own plans doc: *"Can I disable scale-to-zero? Free: No, it's always enabled (5 min idle timeout)"* — the setting isn't configurable via the API on this plan at any value, matching or not. Cost three failed CI runs before the parameter itself was suspected (the first three failures were a separate, missing-secrets cause). Fixed by omitting it from `integration.yml` entirely; Free already suspends at 5 minutes regardless, so nothing is lost.
- **The free plan caps a project at 10 branches and starts failing branch creation around nine open PRs**, on *someone else's* PR, for a reason that looks unrelated (IG §9.2) — why `integration.yml` is one workflow, not two. Delete a hand-created scratch branch (a `migrate-check-*` from testing a migration manually) once its job is done rather than leaving it as permanent headroom loss.
- **`2822193` shipped with `npm run typecheck` broken** — `api/vitest.config.ts`'s `process.env.TEST_PARALLEL` trips `noPropertyAccessFromIndexSignature` (`tsconfig.base.json`), TS4111, since the index-signature access needs bracket notation (`process.env["TEST_PARALLEL"]`). `TEST_PARALLEL=1` had been run and verified, but plain `npm run check` apparently hadn't, on this one file. One-line fix; found and fixed at the start of A2 before `check` could pass at all.
- **Nothing pushed straight to a feature branch (`build/p0-foundation` included) has ever run through CI.** `checks.yml` and `integration.yml` both trigger only on `pull_request` into `develop`/`main`; a bare `git push` to any other branch triggers neither. Every gate result recorded in this document through B0 was a local run substituting for that, not a CI-confirmed one. **Changed 5 August 2026 (A9a):** stop running the full local integration suite at all — run only the touched file locally, push, and open a PR into `develop` so `integration.yml`'s fresh-ephemeral-branch-per-PR run carries the full-suite cost instead, in parallel with the next item. It is also strictly better at the one thing the shared branch keeps failing at: A9a's own verification, run against a throwaway branch created via the Neon MCP tools rather than `api/.dev.vars`'s long-lived one, came back **378/378 in one pass with zero connection flakiness** — the failure mode above is specifically a contention problem, and a branch nobody else is using has none.
- **The `api` integration count recorded in §1's gate table had drifted to a stale `388`** before A9a touched anything — corrected to the verified **378** (ground-truthed two ways: the full CI-equivalent run above, and a plain `grep -c` across every `tests/integration/*.ts` file). Worth a reminder that a number in this document is only as good as the run that last produced it; re-verify rather than carry one forward on trust when a full run is available anyway.
- **Mocked/stub-mode review cannot see `AuthGate` or `FirstRunGate`'s non-`AppShell` states, because the mock replaces the exact code path in question.** `VITE_AUTH_MODE=stub` — used by every test file and by `UI-UX-REVIEW.md`'s own Playwright pass — skips straight past sign-in. GAP-49 (6 Aug, live testing) lived on precisely that boundary and had already cost one near-miss: B8's own writeup flagged "one real browser round trip still unexercised" as an open risk and left it open. **Standing rule: anything touching `AuthGate`, `FirstRunGate`'s non-`renderOperate` branches, or any component that can render outside `AppShell`, needs one real-browser check, both colour schemes, before being called done.** Not a preference — mocked review is structurally incapable of catching this class.
- **Nobody had been reading the browser console as a standing step of any review, automated or manual.** It surfaced GAP-50 (a real `aria-hidden`-on-focused-ancestor WCAG violation in `Sheet.tsx`, live and reproducible) in under a minute, on the second sheet interaction tried. Cheap enough that there is no excuse to skip it — check console warnings/errors alongside any screenshot-based review from now on.
- **A route-def-by-route-def, screen-by-screen validation pass proves what exists matches spec — it cannot notice a flow that was never wireframed *and* never built**, because there is nothing on either side to diff. That is exactly how GAP-51 (F-1.7, "Set up the daily lease") went unbuilt through every prior pass: it has zero mentions in `ui-ux-guidelines.md` and zero implementation in `web/src`, so no comparison ever flagged a mismatch. None of this project's validation passes to date have enumerated `user-flows.md`'s full phase-1 flow list top-down against both documents at once. That audit hasn't run yet; F-1.7 falling through whole is reason to suspect it isn't the only one.
- **A migration comment can be right about its own object and wrong about the conclusion drawn from it — and it reads as authoritative either way.** `0003`'s header says revoked rows are excluded "so a revoked manager (F-1.4) can be re-granted". True of `0003`'s own index; false of the outcome, because `0001`'s `UNIQUE (business_id, user_id)` one migration above still blocks exactly that (GAP-52). The comment never looked up. **This is the third instance of the pattern §6 already names twice** — a stated reason that was accurate about the thing it examined and wrong about what it concluded. The habit that catches it: when a comment claims a *behaviour* is possible, check the other constraints on the same table, not just the one being added. It survived because nothing ever exercised the path — no endpoint has ever written `revoked_at`, so the claim was never tested against Postgres.
- **An accessibility assertion that watches for browser console warnings is worthless without `Accessibility.enable`, and it fails in the passing direction.** Headless Chromium computes the a11y tree lazily; with no client asking for it, checks like `aria-hidden`-on-focused-element never run and the warning is never emitted. The first version of `e2e/sheet-a11y.spec.ts` passed cleanly against code that was definitely broken. The fix is one CDP line (`await session.send("Accessibility.enable")`), but the habit that matters is the one that caught it: **before trusting a test that asserts the absence of something, make it fail on purpose.** Here that meant firing the exact violation by hand on `about:blank` and confirming the harness saw it — nothing, then two entries once a11y was enabled. Note also that `page.on("console")` alone is fine *once a11y is on*; the missing piece was never the listener.
- **`date AT TIME ZONE tz` and `timestamp AT TIME ZONE tz` are different overloads that produce different, silently-typed results — and "make it fail on purpose" catches a wrong fix, not only a missing one.** GAP-72's own suggested fix — `created_at >= $from::date AT TIME ZONE tz` — looked right and was wrong: Postgres upcasts the bare `date` to `timestamptz` at the **session's** zone first, then reinterprets that instant's wall-clock time in `tz`, landing eleven-plus hours off (`pg_typeof` even reported the result as `timestamp`, not `timestamptz` — a second, independent tell). Casting to a bare `::timestamp` first (genuinely naive, no zone attached) is what makes `AT TIME ZONE tz` read it as *that* wall-clock time *in* `tz`. **Verified directly against Postgres with a throwaway query, not assumed from the docs or from how the fix reads.** The discipline this confirms: running the new test against pre-fix code is necessary but not sufficient — run it against the *first-draft fix* too, before trusting that fix. Here, one of two new tests kept failing against the first draft; only the second run caught it.
- **`npm run typecheck` has now shipped broken twice, both times because the gate wasn't run before pushing.** `2822193` was the first (`api/vitest.config.ts`, TS4111). A0 was the second: `NewBusinessMember.role` was typed as the literal `"owner"`, so changing the one call site to `owner_manager` did not compile — found 7 Aug, a day after A0 shipped, by running `npm run check` for an unrelated reason. **The shape is worth naming: a type narrowed to the single value its only caller happens to pass.** It reads as precision and behaves as a tripwire on the next change; `role` is a set, and typing it as the set (`BusinessMemberRole`) is both more honest and less brittle. Note that no *test* failed either time — `test` and `typecheck` are separate scripts, and a green test run says nothing about whether the branch compiles.
- **A QA branch is only a meaningful live-verification target when it reflects what's actually merged.** `git log` is authoritative here, not the tracker's own "done" rows — A0 (`17da20d`) has been sitting on `build/p0-foundation`, unmerged into `develop`, since it shipped, so QA spent 6 August still running the pre-A0 signup path. Live-testing against a stale environment risks the exact failure it's meant to catch, inverted: a real fix reading as still-broken. Check `git log develop` (or the deployed commit) before trusting a live QA result to mean what it looks like it means.
- **A local branch ref is not the same fact as its remote-tracking ref, and `git fetch` only updates the second one.** Checking "has X been merged to `develop`" by diffing against a local `develop` that hasn't been checked out in days answers a question about a stale copy, not about what's actually on the remote — the exact inversion of the QA-staleness trap directly above, one layer lower in the stack. Diff against `origin/develop`, or fast-forward local `develop` first, before concluding a merge hasn't happened.
- **Before calling a single-writer column a bug, ask what the column is *for*.** A `grep` showing one writer proves one writer; it does not distinguish "everything else forgot to maintain this" from "this is a standing configuration and only one flow was ever meant to change it." GAP-54's first draft got that backwards and then went looking for live data to confirm the wrong reading — finding some, because a vehicle configured for daily lease with no daily lease yet *looks* like a stale figure and is in fact the ordinary case. The one-line answer was sitting in `createVehicle`'s own doc comment the whole time ("F-1.2 is what ever closes it"). **The distinguishing question is whether the column is derived from other rows or declared independently of them** — a derived one that nothing recomputes is a bug; a declared one with a single deliberate writer is a design, and its gap (if any) is the missing *flow*, not the missing writes.
- **An accessibility-tree snapshot taken immediately after a click can render before the async data it depends on has resolved, and the gap reads as real state.** Live-tested 7 Aug: a snapshot taken right after clicking a sheet's close button appeared to show a driver record that hadn't existed a moment before — momentarily indistinguishable from having just created one by accident. The network log settled it: the one `GET /api/driver` request for that page load, timestamped *before* the interaction in question, already carried that row in its response body — the earlier "0" was the query's loading state rendered as a fallback, not its resolved one. **Before concluding a UI action wrote something, check the network log for the actual request, not just the next snapshot** — this is the same discipline TRACKER already keeps for a11y console warnings (§5, `Accessibility.enable`) and it generalises to any assertion made from a point-in-time render rather than the data source itself.
- **A verification script nobody schedules can be wrong for over a week with nothing red anywhere.** `docs/engineering/fixtures/golden.py` — the actual script behind the "39 of 39" claim both `CLAUDE.md` and DM §16.1 make — stopped running on 1 August 2026, the day migration `0004` added `business_id NOT NULL` to `deposit_movement`, `insurance_claim` and `incident_recovery`; its own inserts into those three tables were never updated to match. Found 9 August verifying GAP-70/71's new DM §15 queries, purely because that pass happened to re-run it. **Deliberately not wired into CI** (`docs/README.md` calls it "supporting material"; IG §9.2's branch-count ceiling is part of why), so nothing else would have caught this either. The three protected figures (`134,000` / `15,000` / `7,500`) were never actually at risk — only the ability to prove them was, silently, for eight days. Fixed with the missing column on all three inserts; re-run confirms 39/39 (DM §16.1, DM v1.1.2). **A second, smaller gap found the same pass and left as found**: the script never writes a `business_member` row at all, so DM §15's held-per-partner query has nothing to select against it — worth fixing alongside the next thing that touches this file, not urgent enough to hold this one up.
- **`api` integration test files outside the ones a change directly touches can still depend on that change** — checking impact by filename match (touch `lease.ts` → re-verify `lease.test.ts`) misses siblings that hit the same endpoint under a different name. GAP-84 (F1, 8 Aug) added an arrangement check to `POST /api/lease`; the F1 session re-verified `lease.test.ts` and `dailyLease.test.ts` and called it done, but `lease-closure.test.ts` and `mileage-assessment.test.ts` (G-3's own fixture file) both also call that endpoint through their own local helpers and neither was touched. Invisible locally — nothing runs the full `api` integration suite by default (§1) — and invisible in the F1 session itself for the same reason; only surfaced 9 Aug when `integration.yml` ran for real on the PR and failed 10 assertions, all `lease-closure.test.ts:52`, all `expected 409 to be 201`. **The search that should have run was "every call site of the endpoint", not "every file with a matching name."** `grep -rl` for the route string across `api/tests/` found both missed files in one pass; fixed by giving all 21 affected vehicles the same `setVehicleArrangement(vehicleId, "A")` the two originally-checked files already had.
- **The connection-flakiness trap above (§5, "the Neon branch has a connection ceiling") got materially worse this session, not just recurring** — full-suite runs of `trip.test.ts`, `lease.test.ts` and `business-member.test.ts` hit multi-*minute* stalls (one run: 985,847ms on a 14-test file; another: 3,044s on `trip.test.ts` alone) rather than the instant `Connection terminated unexpectedly` the original trap describes, with a **different** test failing each retry and every failure clearing on an isolated or repeat run. Same root cause, same diagnostic ("moves between runs, in files the change never touched, is the environment" — do not chase it as a regression), but severe enough on this particular branch that **`TEST_PARALLEL=1` (already documented two bullets up) is worth reaching for by default now, not just as a known option**, and a single touched-file-plus-siblings run is cheaper to trust than a full-suite one when the branch is in this state.
- **`TEST_DATABASE_URL`'s own migration bookkeeping can silently fall behind the schema its tests actually need, and the two failure modes look identical from a test's own output.** Found 9 Aug: `business-member.test.ts` showed 4 failing tests (`POST /api/business-member/invite` and `.../change-role` all 500ing) that looked exactly like a pre-existing code bug — same shape as a dozen other "flaky/pre-existing" findings this session. `npm run migrate:status` (with `DATABASE_URL` overridden to `TEST_DATABASE_URL`, never the bare command — see IG §8.3) showed migration `0010` still `PENDING` on this branch despite being merged and deployed weeks ago; `business_member_invite` genuinely did not exist (`to_regclass` returned `null`). Applying it (`npm run migrate`, same override) fixed 3 of the 4 failures instantly, leaving only a genuine connection timeout. **Check `migrate:status` against `TEST_DATABASE_URL` before spending time diagnosing a cluster of same-shaped integration failures as a code regression** — a stale disposable branch (the `.dev.vars` comment on this one records it was created fresh, "test-parallel", specifically to dodge the contention trap two bullets up) is a real, ordinary explanation and costs one query to rule out.

**Writing code**

- **A U-3 prefill that crosses a direction-of-money boundary is the one prefill that must never be written, and this project has now reached for it twice.** U-3 ("sensible defaults, pre-filled") is a good rule that becomes a money bug the moment the source field points the opposite way from the target. The live case: `driverDayFeeMinor` (`packages/shared/src/schemas/driver.ts:23`) is money **you pay him**; `dailyLeaseAmountMinor` is money **he pays you**. Prefilling the second from the first seeds a plausible, wrong, unremarked figure into every record created. **This is not a hypothetical — UC §2's UC-04 note records that v1.1 of the specification itself called both a "per-day rate", and renaming them apart was a deliberate correction.** Plan.md's B10 write-up then re-proposed exactly the merge v1.1 had been corrected for, six days after the vocabulary lock was written down; it was caught by reading the schema rather than by any check. **Standing rule: before wiring any prefill, name the direction of both fields out loud. If they differ, there is no default — leave it empty.** The vocabulary lock (UI §9.6) exists to make this visible in copy; nothing yet makes it visible in code, which is why it needs to be a habit.
- **A bare single-statement write to an audited table records `changed_by` as `NULL`.** `withActor` wraps `.transaction`, so a write that never opens one is invisible to it. P9 fixed nine such functions by wrapping them even where atomicity was not otherwise needed. **Any new write to a `posted_period_id`-carrying table must open a transaction.**
- **`payment.amount_minor` has `CHECK (> 0)`** — a full reversal can never zero it. `status = 'reversed'` carries "nothing here counts"; the wire response reports the true remaining figure separately.
- **`tailwind-merge` does not know this project's scale.** Any new `--text-*` or spacing token must be added to `theme.text`/`theme.spacing` in `web/src/lib/cn.ts` or it is silently dropped next to a real colour class. Regression test in `cn.test.ts`.
- **`ownership_share` can never hold two open (`effective_to IS NULL`) sets for one vehicle — a second `POST /api/ownership-share` for a vehicle that already has a set always 400s.** `assert_shares_total` (migration 0001) sums every row whose date range contains the *new* row's own `effective_from`; a still-open first set's `[effective_from, ∞)` range contains any later date, so the total hits 200% and the trigger rejects it before a second set can ever exist. **This means `effective_to IS NULL` alone is a correct and sufficient "current split" filter** — not a coincidence, the deferred constraint's whole job — and a test that tries to `POST` a second set to prove otherwise needs a direct `db.insert` instead (found writing A2's `listOwnershipShares`, `partner.test.ts`'s own comment records the reasoning).
- **A `GENERATED ALWAYS` column reads back nullable through Drizzle even though Postgres never actually leaves it null.** `banking_event.discrepancy_minor` (`recorded − counted`, migration 0001) is one; a raw `SELECT` of it types as `bigint | null` in Drizzle regardless of the `NOT NULL` guarantee the two source columns give it. Defaulting the gap with `?? 0n` is exactly the W-56 pattern the lint rule exists to catch (and it caught it) — compute the figure from the two real columns instead, the same way `recordBankingEvent` (domain/partner.ts) already does on write, rather than trusting the generated column on read.
- **"A row exists for this idempotency key" is not the same fact as "this action already happened", the moment something *other* than the action itself can create that row (GAP-3).** `confirmDay` was idempotent on `(daily_lease_id, business_date)` since P3, correctly, right up until P13 added a second writer (`generate-day-cards`) that inserts the same row in a placeholder `open` state before the user ever acts. The idempotency check kept matching on existence alone and started silently no-op'ing real taps against real money. The fix — and the pattern for the next table that grows a second writer — is to gate the no-op on the row's own **state**, not its presence: a row in a non-terminal/placeholder state is the write path (an `UPDATE`, guarded on that same state in the `WHERE` clause so a concurrent racer's write affects 0 rows instead of throwing), and only a row already in a terminal state is the genuine no-op. Worth checking any other `find-then-return-if-exists` idempotency guard against whether a cron can now produce that same row ahead of the user — `day_record` was one of at least two tables (`vehicle_day_allocation` is the other, cron-only, no user-facing write competes with it today) P13 turned into a second writer.
- **A new invariant trigger firing on `INSERT` "because the reference implementation does" is a copy of the wrong half of the pattern.** `assert_business_has_owner()`'s (A11/INV-31) first draft matched `assert_shares_total`'s exact shape — `AFTER INSERT OR UPDATE`, deferred — and broke roughly a dozen existing integration-test fixtures that mint a `manager` as a business's sole member, because an `INSERT` can only ever *add* a row and can therefore never by itself take an active-owner count to zero; only an `UPDATE` that sets `revoked_at` can. `assert_shares_total` genuinely needs `INSERT` (a 60/40 split is built from inserts), which is exactly why copying its trigger *shape* without re-deriving which statement types can actually violate the *new* invariant produced a check that rejected valid, unrelated writes for no protective benefit. Ask "which statement type can actually cause the bad state" before choosing `INSERT`, `UPDATE`, or both — never default to "both" because a nearby trigger does.

**Three form bug classes, fixed structurally — every new form inherits the fix**

Found in P2 and fixed once each, at the structural level rather than per-form. A new form that hand-rolls around any of them is reintroducing a solved bug:

1. **Wire/domain mismatch on any zod schema with `.transform()`** (`moneyWireSchema`, `businessDateSchema`) — its `z.infer` output type differs from its `z.input` wire type, but `MoneyField`/`DateField` produce and consume the *domain* type. The fix: a form-local schema built from `z.custom<Minor>`/`z.custom<BusinessDate>` with no transform, keeping form state in domain types throughout, converting to wire via `toWire()` **only inside `mutationFn`**.
2. **`""` vs `undefined` on optional text inputs** — a native `<input>` always yields `""` when untouched, but `z.string().min(1).optional()` only skips validation for `undefined`. Fixed once in `lib/optionalTextField.ts`'s `blankToUndefined`, spread into `register(name, …)`.
3. **A cross-field error hidden inside a collapsed `Disclosure`** — W-55's error attaches to a level-2 field, invisible on a failed submit. Fixed by `Disclosure`'s `forceOpen?: boolean`, driven by `errors.fieldName !== undefined`.

**Migrations** — forward-only and numbered. `0001` DDL · `0002` audit trigger · `0003` single active membership · `0004` `business_id` on seven audited child tables · `0005` odometer `(lease_id, read_on)` partial unique · `0006` **`assert_period_open()` only fires on `INSERT` or an `UPDATE` that actually changes `posted_period_id`** — settling a claim or obligation months later is legal and was being rejected · `0007` `expense.voided_by` · `0008` **narrows `0006`'s exception to exclude a void** (`voided_at` `NULL → NOT NULL`) — GAP-35, A9a · `0009` widens `obligation_kind_check` to admit `trip_fare` — A6 · `0010` **`business_member`'s pair-unique made partial** (GAP-52), an explicit audit trigger (GAP-53), `business_member_invite`, **INV-31** (`assert_business_has_owner()`, `AFTER UPDATE` only — see §5 above for why not `INSERT`) — A11. Run `npm run check:drift` against both databases after any of them.

---

## 6. What validation corrected

**Compacted 10 August 2026** — nine passes (4–9 August) covering roughly 65 claims across ten external documents became the table below; the full prose, including every rejected claim's own reasoning, is in git history. **The standing lessons these passes taught are what earn a permanent line** (kept in full, below the table) — the pass-by-pass narrative that produced them doesn't need to stay narrative to keep teaching it. **The tenth and eleventh passes stay in full**, further down — 9–10 August, still the most recently actioned material.

| Pass | Date | Validated | New, real | Rejected (why) |
|---|---|---|---|---|
| 1st (unlabelled) | 4 Aug | This plan's own first draft | GAP-33 (`GET /api/expense` no caller, deliberate) | Root `CLAUDE.md`'s "documentation only" claim — stale, not this file's to fix |
| 2nd | 5 Aug | A2/A3/B2/B3 before building | GAP-37 (`/more` the only door to four screens, → B0), GAP-38 (`GET /api/payment` missing, → A3) | — |
| 3rd | 6 Aug | `UI-UX-REVIEW.md` | GAP-44…48 (→ B9) | §7.3's `DialogConfirmFooter` default-text claim (doesn't compile without a value); §7.2's "excess rate per km" (the spec's own term, not a vocabulary-lock violation) |
| 4th | 7 Aug | This session's own GAP-54 draft, an hour after writing it | F-1.2 unbuilt (real) | GAP-54's original claim that `vehicle_arrangement` "never reflects reality" — the column is a standing configuration, not a derived state; the cited live data was the *correct* case (B10's own target) |
| 5th | 7 Aug | `MUST-FIX-FINDINGS.md`, web + backend editions | GAP-56…60 (GAP-56 the one real new money defect either edition produced) | §6's "`/more` only has sign-out" (deliberate, not an omission) |
| 6th | 7 Aug | "0b"'s own premise | — | The premise itself: production `business_member` was never checked, and turned out to be zero rows — nothing to fix |
| 7th | 8 Aug | The 8 Aug QA rerun | GAP-83 (worse than reported — `DateField` itself, not one screen) | "Close month opens with all-zero status" — U-7 (warn, don't block) working as designed |
| 8th | 8 Aug | `QA-COMPREHENSIVE-TEST-FINDINGS-2026-08-08.md` | GAP-84, GAP-85, GAP-86 | Sheet-clickability and keyboard-activation claims — both traced to the reporter's own broken browser-automation transport, not the app |
| 9th | 9 Aug | Four documents at once (BE/RC/QA-refresh/UI-LF, ~40 claims) | GAP-88…98 except GAP-95, plus **GAP-90** (a live 500 chased two documents stopped short of) | GAP-95 (Disclosure "More" labels — UI §6.3's own spec); re-raised keyboard/sheet claims (unchanged since 8th pass); BE-12 (reviewer's own credentials issue, not a repo defect) |

**The standing lessons, kept permanently:**
- **A gap's stated reason is a fact with a date on it.** GAP-13's "indistinguishable from an unscheduled day" expired the moment day-cards existed; GAP-23 and GAP-1 (via A11) are the same shape. Re-check a deferred gap's reason before carrying it forward, not just its existence.
- **A plausible claim stated with the confidence of a checked one is the single most common failure mode across every pass.** GAP-54's withdrawn first draft, "0b"'s unchecked production premise, and three of the ninth pass's re-raises are all this same shape: inference dressed as verification. The fix is always the same — check the thirty-second thing before trusting the inference.
- **`docs/` outranks a review.** Three separate external reviews have each proposed relabelling something the specification names on purpose (UI-UX-REVIEW's "excess rate per km" and `DialogConfirmFooter`, the QA rerun's Disclosure labels) — declined every time, on the record, so the fourth report finds the answer instead of re-litigating it.
- **A review independently rediscovering this tracker's own queue is a real signal, not a coincidence to wave off** — the fifth pass matched an existing gap or "correct to leave" row on all but five of its ~25 claims, from a completely different reading angle.
- **Reading every available document is not the same as reading the right ones.** The ninth pass's four documents found eleven real gaps; two more documents sitting in the tree unread turned out to be near-total duplicates of those four, except for one that changed the axis it looked along entirely (contracts *between* layers, not code *inside* one) — the tenth pass, below, in full.

---

### A tenth pass, 9 August 2026 — `API-CONTRACT-AUDIT-FINDINGS` in full, and it found a defect this session shipped

The ninth pass read `API-CONTRACT-AUDIT-FINDINGS` only far enough to confirm it duplicated the others, pulling one finding out of it (GAP-100). **Read in full it has two more, and the first is the most useful thing any of the six documents produced.** `BACKEND-API-QUERY-EVALUATION` was re-checked and is byte-identical to the copy validated in the ninth pass — BE-01…BE-12 stand as triaged there, no change.

24. **ACA-01 is real, and it is understated.** It reports that nine read screens branch on `query.data === undefined` without checking `query.isError`, so a failed fetch renders as loading. **The true count, verified against source 10 Aug 2026, is 30 of 32 files with a real `useQuery` call, 81 of 83 call sites** — `isError` is checked against a *query* in exactly two files in the entire client (`FirstRunGate`'s `meQuery` and `CloseMonthScreen`'s `checklistQuery`), both hand-rolled. (The "50 of 52" figure first recorded here counted every file importing anything from `react-query`, including 27 that only call `useQueryClient` for post-mutation invalidation and have no read to guard — narrower by file, worse by call site than first thought.) What hid it is that **mutation error handling is genuinely good and everywhere**: those 27 files are almost all of what a naive `grep isError` finds. So the pattern reads as present and is absent exactly where reads happen. Filed as **GAP-101**. **Three things worth stating plainly rather than burying**: this is the structural cause behind GAP-90 — the revoked-member 500 is invisible *because* of it, and fixing GAP-90's client half alone fixes one screen of thirty-two; **eight of the nine screens the audit names are B4 Wave 1's, built in this same session, gate-green, and reviewed twice without anyone noticing there was no error path** — a screen with no error branch passes every test that only ever mocks success, which is what all of them did; and **the eternal spinner turned out to be the least harmful of the three failure modes actually present** — a `?? []`/`?? 0` fallback on a failed read (`HomeScreen` renders "Nothing needs you today" while the Worker is down) and a failed read silently deleting a safety warning (`StartLeaseScreen`/`BookTripScreen`'s expired-paperwork strip, `VehicleCalendarScreen`'s INV-1 grid) are both worse, and neither was named until the 10 Aug implementation plan.
25. **ACA-08's no-caller list is now mostly closed or deliberately phase-2.** The B2 partner/banking callers, the in-app member invite screen, GAP-67's mileage package, GAP-100's advance issue/settle path, and GAP-102's vehicle-paperwork write all landed by 11 Aug. Still deliberately unbuilt from that list: phase-2 write-off and post-closure charge (GAP-15). The remaining lessons are not caller absence: GAP-1 policy scoping, ownership-share replacement after an existing current split, and management-agreement API role hardening.
26. **ACA-06's second half is real but already owned, and the audit is right that the ownership is ambiguous.** `DriverDetailScreen` never calls `GET /api/driver/{id}/view` — A5's staff-facing driver history read has no caller. TRACKER has recorded that as "rendering is B5" since 5 August, but **B5 is the driver's *own* Mine shell over `GET /api/driver-view`, a different endpoint for a different audience**, so the staff history has been sitting inside an item that was never going to build it. Clarified against B5 in `Plan.md` rather than filed as a new gap, since the work is small and genuinely belongs next to B5's own reading of the same data.
27. **Everything else in ACA maps onto an existing row**: ACA-02 = GAP-88 · ACA-03 = GAP-92 (which the audit usefully widens to `vehicleWindowQuery` and the client's own search params, both now named in that row) · ACA-04 = GAP-93 (adding that `route-defs/payment.ts` documents its 404 as "no such customer or driver", omitting partner — the documentation drifted with the validation) · ACA-05 = GAP-100 · ACA-07 = GAP-22/B6 · ACA-09 = GAP-98 · ACA-10 = GAP-89 · ACA-11 = GAP-88's empty-state half · ACA-12 = the same stale-credentials non-finding rejected as BE-12.

**What the tenth pass changes about the ninth's own verdict.** The ninth pass concluded that six documents produced one finding between them that the four did not, and that reading all six was mostly re-reading the same claims. **That was right about the other five and wrong about this one** — `API-CONTRACT-AUDIT-FINDINGS` is the only document of the six that audits *contracts between layers* rather than code in one layer, and that is exactly the seam GAP-100, GAP-101 and GAP-102 all sit in. The lesson is not "read everything" but "a document that changes the axis it looks along is worth reading even when its summary table looks familiar."

---

### An eleventh pass, 10 August 2026 — a live browser session, not a source read, and the first one with a report to check live data against

`LIVE-BROWSER-FINDINGS-2026-08-10.md`, run against `qa.fleetsettle.com` under Chrome DevTools Protocol — real Chromium, both an ordinary desktop context and, for the first time, a touch/coarse-pointer viewport emulation verified against `matchMedia('(pointer: coarse)')` before anything was tested against it, so it faithfully reproduces the condition the mobile-history bug class is actually gated on rather than just resizing a window. **Two things made this pass possible that the 8 August live sessions didn't have**: B4 Wave 2 gave it a reports catalogue to cross-check committed data against, and `MOBILE-SHEET-AND-DATE-PICKER-FINDINGS-2026-08-09.md` existed as a source-only audit nobody had yet stood up a real touch context to confirm or reject.

28. **The opening-balance finding did not come from looking for a bug — it came from checking a number the previous session had already committed live, against a report that didn't exist yet when it was committed.** The 8 August session (`LIVE-TEST-PLAN.md`'s LT-5) confirmed a `customer_due` and a `deposit_held` opening-balance entry live and noted, correctly at the time, that verifying they "appear as opening positions and never as income" was "only fully checkable once B4 exists." B4 exists now. Checking is what found **GAP-103**: neither entry appears anywhere, because `commitOpeningBalance` never wrote them into a table anything else reads. **This is the same class of lesson as GAP-13 and GAP-23** (§6 already names it twice) — a gap's standing reason is a fact with a date on it — except here the reason wasn't "not reachable yet," it was "not yet possible to check," and the moment it became possible to check, the answer was the worst one available: not a wrong number, an absent one presented as confirmed.
29. **Three claims from the 9 August mobile-sheet audit were converted from source reads into directly reproduced defects, and one could not be tested at all — for a reason worth recording as its own fact.** MP-02 (Quick Add) and MP-03 (its sibling call sites) reproduced exactly as the source audit predicted, independently, on three structurally distinct screens. MP-04 (nested pickers) reproduced too, and worse than documented — the source audit predicted a parent sheet closing; live, the entire in-progress form state (a party selection and an amount) was discarded with it, no error. **MP-08 (the `Dialog`-in-`Sheet` stacking watch item) could not be tested, and not because of a tooling limit**: reading `AdvanceSheet.tsx` to find a way to seed its precondition found that the client has no way to attach an advance to a trip at all — a gap in its own right, folded into **GAP-100** rather than filed separately, since it's the same functional area found incomplete a second, deeper way.
30. **The cash-position mislabelling is the same shape GAP-75 already taught this queue to watch for, in a screen that shipped ten days after GAP-75 closed.** `CashPositionReportScreen.test.tsx` asserts "held for customers" as the correct rendering of a number the server explicitly, deliberately, does not attribute to any party type — the test encodes the assumption instead of checking it. **Filed as GAP-106.**
31. **Everything else checked live either confirmed a prior fix or found nothing.** LT-7/GAP-3 is now closed in full — the two-visit cron test that had sat open since 6 August finally got its second visit, and the placeholder the cron produced confirmed correctly on the first tap, all three money facts written in one transaction. GAP-84's arrangement gate and GAP-85's blank-vs-zero fix both held under conditions slightly harder than their original tests — direct URL navigation for the former, a fresh trip form for the latter. Dark mode was checked across every screen this pass touched and found genuinely improved since 8 August (the voided-expense treatment now carries a badge and a border, not only strikethrough text) with nothing new wrong.

**What this pass is worth, stated fairly, the way every pass before it has been:** it produced one finding (GAP-103) more severe than anything in this tracker's 106 gap ids, and it produced it by doing the one thing eight prior source-only and live passes had no way to do — committing real data through a real write path and then checking a real report against it, weeks after both existed. The lesson is not "live-test more" in the abstract; it's the same one LT-5's own 8 August note already stated in advance, and was simply too early to act on: **a verification blocked on a screen not existing yet is not the same as a verification that happened.**

### A twelfth pass, 10 August 2026 — a consolidation document, not a review, checked the same way a review gets checked

`PENDING-REVIEW-ITEMS-2026-08-10.md` is a different shape from the first eleven passes' subjects: not a fresh external review finding new defects, but a claimed *consolidation* of the seventeen review documents accumulated since 6 August, each checked against source/tracker and re-filed as one checklist, with the seventeen originals already removed from the working tree by the time this pass opened it. **The consolidation's own claims were checked, not assumed** — the same standing rule this section has run on since the first pass, applied one layer up: a document that says "I already validated these" is itself a claim, not a fact.

32. **The file-removal claim checked out.** All seventeen paths the disposition table names as "deleted after consolidation" were confirmed absent from the working tree and present in `git show HEAD:<path>` — genuinely removed, genuinely recoverable, nothing silently lost. `git status` showed all seventeen as unstaged deletions plus the new file as untracked, meaning none of this had been committed yet; this pass is what gives it a tracker record before that happens.
33. **Nearly every checklist item mapped onto a gap id this file already carries, at more depth than the consolidation's own line.** Spot-checked against source rather than trusted: GAP-45's title-truncation claim, GAP-91's missing `variant`, GAP-89's four-entry tab map, GAP-96's missing aggregate, GAP-97's missing menu action, GAP-55's zero `autoComplete` hits, and GAP-99's leaked "presigned upload storage" copy all confirmed exactly as both this file and the consolidation described them — nothing here was stale.
34. **Five items were re-raised as active work without noting they were already filed `Recorded, unowned, and correct to leave`** (GAP-5, GAP-7, GAP-8, GAP-21, GAP-68) — the same shape GAP-95 already established a precedent for (re-raising a declined item doesn't make it live by itself), except these were never declined so much as sized and parked. Evaluated individually rather than reflexively re-parked: GAP-7's reframing ("stop implying a state exists, if not building it") is a genuine improvement worth keeping for whoever picks it up; the other four stand exactly where they were, with the re-raise now recorded against each row so a fourth report finds the answer instead of re-litigating it (§4).
35. **Nine of B9's rows were small enough, and unambiguous enough about what "done" means, to build the same sitting rather than only record.** GAP-45, GAP-46, GAP-55, GAP-89, GAP-91, GAP-96, GAP-97 and GAP-99 all closed 10 Aug — full accounts in their own §4 rows — plus M-25's vocabulary fix, which rode along with GAP-97 since both touched the same lease-term forms. **What stayed unbuilt and why**: GAP-44 needs a wire-schema change (the error-enrichment decision from the 10 Aug B9 unblock, §0) and GAP-47/GAP-48/GAP-83/GAP-105 each touch a shared primitive or need their own sitting — none of the five are a same-pass extension of what nine straightforward call-site fixes already covered.
36. **One test failure surfaced a real race in the new test, not in the code under test — worth recording as its own trap.** GAP-46's first draft asserted `aria-label` on a calendar cell immediately after `await screen.findByText("July 2026")` — but the month label renders synchronously while the day grid waits on the calendar query, so the assertion sometimes ran before real day data replaced the empty default. Fixed by warming up on the first cell's own glyph first (`findByText`, async), the same pattern the neighbouring "seven day-states" test already used. Filed in §5 as its own trap.

**What this pass is worth:** the consolidation held up — seventeen files' worth of findings really did reduce to one file without losing anything this tracker didn't already have a home for, and the five re-raised items are now harder to lose a second time. The nine builds are the more ordinary kind of progress this tracker mostly records; the validation is what makes them safe to trust without re-reading seventeen deleted files first.

---

## Alongside this tracker

- **[LIVE-TEST-PLAN.md](LIVE-TEST-PLAN.md)** — the eight checks that can only be answered by a real browser against a real environment, collected in one runnable queue instead of scattered as prose across this file and [Plan.md](Plan.md). Ordered so no test contaminates the state a later one needs, with the write cost of each stated up front — two are one-way and one is irreversible. Plan.md schedules the whole file as **V1**. **Findings go through the same validation every external review gets**: confirmed ones become §4 rows with a gap id, rejected ones become §6 paragraphs.
- **[ROLES-AND-CAPABILITIES.md](ROLES-AND-CAPABILITIES.md)** — the five actors, the enforced capability matrix, and what is actually reachable in the client today, with the three put side by side. A reference, not a record: **if it ever disagrees with `api/src/auth/policy.ts`, `policy.ts` is right.** It exists because those three drifted once already and the drift was invisible until someone hit it in production (GAP-42).
- **External review documents — superseded 10 August 2026.** The five listed in this bullet's own prior edition, plus twelve more accumulated since (`API-CONTRACT-AUDIT-FINDINGS`, `ATTACHMENT-UPLOAD-IMPLEMENTATION-PLAN`/`-PENDING`, `B4-REPORTS-DESIGN`/`-REVIEW`, `FLOW-INVENTORY-AUDIT`, `GAP-101-READ-ERROR-CONTRACT-PLAN`, `LIVE-BROWSER-FINDINGS`, `LIVE-TEST-PLAN`, `MOBILE-SHEET-AND-DATE-PICKER-FINDINGS`, `MUST-FIX-FINDINGS`, `STATIC-SOURCE-AUDIT-FINDINGS`, `UI-UX-REVIEW`) — were consolidated into one file, `PENDING-REVIEW-ITEMS-2026-08-10.md`, and the seventeen originals removed from the working tree (recoverable via `git show HEAD:<path>`, since all were committed before removal). **That consolidation was itself validated here, the twelfth pass (§6)**: its claims cross-checked against source and against this file's own §4/§6, its closeable items built the same sitting (nine of B9's rows, §4/§0), and five items it re-raised without noting they were already `Recorded, unowned, and correct to leave` got that context restored in place rather than silently reopened. `PENDING-REVIEW-ITEMS-2026-08-10.md` itself is now kept as an input, the same standing this bullet already gave its five predecessors — the gap ids in §4 are the record, not the file.

## Not in this tracker

UC §9.1 phase Third and UI §15 phase Third, listed so their absence is a decision: depreciation and disposal · driver retainers and spare-vehicle reassignment · loan and lease schedules · tax, if it applies · offline capture of photos · the desktop analytical dashboard beyond UI §14's three changes.

## Blocked

| Blocked | Waiting on |
|---|---|
| **P14** — messaging | Twelve Meta template approvals (6 messages × 2 languages), minutes to ~2 days each. `dispatch-messages`, the fifth Cron Trigger, waits with it |

Nothing else on either track waits on anyone outside this repository.

**Read this before writing "blocked externally" against anything again.** Real Asgardeo sat in this table for months described as "~10 minutes; blocks nothing else." The console change *was* ten minutes. The client half — SDK, PKCE flow, callback, sign-in screen, a real token getter — was unbuilt, unsized, and blocked **everything**: nobody could log in to a deployed build at all. The label described the blocker's cost and said nothing about the work behind it, and it kept the item ranked last for exactly that reason. P14 carries the same label today; its Meta approvals are genuinely external, but `dispatch-messages` and six message templates are not, and no one has sized them either.

## One open question, still unresolved

UC §9.1's phase lists name incidents nowhere — not First, Second or Third — yet **G-2, "one accident", is a golden fixture that must reproduce**. This tracker assumes incidents are phase one, since a fixture no scheduled phase can satisfy is a gap rather than a plan. If that is wrong, UC §9.1 is the document to change, deliberately, with the reason recorded.

## Adding an entry

A finished item becomes **one row** in §2 or §3 — what it delivers, its proof with real counts, its gap ids — not a section. Anything it did not build becomes a row in §4 **with a track**. Anything that will cost a future session an hour to rediscover goes in §5. The narrative belongs in the commit message; this file is the index.
