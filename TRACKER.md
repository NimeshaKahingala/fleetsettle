# Build tracker

**Not a specification.** `docs/` says what to build and why; this says what is done. Where the two disagree, `docs/` is right. [Plan.md](Plan.md) says what remains and in what order; where *those* two disagree, this one is right, because it is the record and that one goes stale.

**Rewritten 4 August 2026**, from `b2cf367`. The previous edition was 760 lines of narrative in phase order; this is the same content reorganised so a finished phase is a row, every unfinished thing is one table with a track against it, and anything that costs a session to rediscover has its own section.

**Validated this pass** — route-def by route-def against `api/src/route-defs/`, screen by screen against `web/src/`, both against a real `npm run check`. Corrections in [§6](#6-what-validation-corrected).

**Updated 5 August 2026**, `e7efa71` — CI's integration workflow, blocked since P0 on missing repository secrets, is now configured and verified end to end: a real PR run applied all seven migrations from scratch, passed the DM §13 drift check, and ran all 328 integration tests, green, in 12m49s. One live bug found and fixed along the way, recorded in [§5](#5-the-traps).

**Updated 5 August 2026**, `2822193` — a second validation pass, this time scoped to the four items about to be built (A2, A3, B2, B3). Three more corrections and two settled decisions, in [§6](#6-what-validation-corrected); two new gaps, **GAP-37** and **GAP-38**, both blocking.

**Updated 5 August 2026 — A2 done.** Partner, banking and cash reads: `GET /api/business-member` (GAP-31), five list endpoints (`ownership-share`, `capital-contribution`, `management-fee-agreement`, `banking-event`, `partner-payout`), and the composed `GET /api/partner/{userId}` (F-7.6/F-7.3, closing GAP-9 and GAP-4 by derivation, never a write). 52 new integration tests, all green together and against the full suite. Two real defects found and fixed while writing them, both recorded in [§5](#5-the-traps): `listOwnershipShares`' first draft, and a nullable-generated-column default that the W-56 lint rule caught. A third finding, **GAP-39**, is a live gap the endpoint works around rather than fixes — no generator ever turns a `management_fee_agreement` into a period obligation, so `sumVehicleCostsForPeriod`'s own `management_fee` branch has been silently reading zero since P7.

**Updated 5 August 2026 — A3 done.** Period, write-off and payment reads: `GET /api/accounting-period` (list, open and closed alike) · `GET /api/write-off` (list, gated the same as recording one) · **`GET /api/payment`** (GAP-38 — `POST /api/payment/{id}/correct` has had a caller since P9 with nowhere to reach it from; this is F-8.2's "open the receipt" step, and carries the linked-driver 403 class since a payment names the driver it moved against) · the close checklist's **`unconfirmedDays`** row (GAP-13 — one `COUNT(*)` on `day_record.state = 'open'` scoped to the open period, cheap and exact since P13, UI §7.7's first row). No new table, no new write, no domain-layer function needed — every one of these is a straight filtered read, the same shape A2's list endpoints already established (list capability mirrors the corresponding write capability). 13 new integration tests across the three existing files; full suite 31 files / 366 tests, 366/366 green (one file's known Neon connection-flake under parallel load ruled out by re-running it alone — 16/16 clean, unrelated to this change).

**Updated 5 August 2026 — A4 done.** Customer-scoped reads, GAP-22's backend half: `GET /api/customer/{id}/obligation` (outstanding dues only, oldest due first — reuses `findOutstandingObligationsForParty` rather than a second party-scoped query, so this screen can never disagree with what `recordPayment` actually allocates against; `obligation` carries `party_customer_id` directly, so no three-way source reassembly was needed the way the lease hub required) and `GET /api/customer/{id}/payment` (reuses `listPaymentsForBusiness` scoped by `partyCustomerId`). Both `dailyOperations`, 404 cross-tenant, both proven against the W-49 linked-driver 403 class. No new table, no new write, no domain-layer function. 8 new integration tests in the existing `customer.test.ts`; full suite 31 files / 374 tests, 374/374 green (three files' known Neon connection-flake under full-parallel load — `accounting-period.test.ts`, `dailyLease.test.ts`, `vehicle.test.ts`, none of them touched by this change — ruled out by re-running all three alone: 47/47 clean).

**Updated 5 August 2026 — B8 done: real Asgardeo auth.** `96301f8`. `@asgardeo/auth-react` ^5.6.2, PKCE, an `/auth/callback` exchange and a sign-in gate — 12 new tests, web now 76 files / 276 tests. **Until this landed nobody could log in to a deployed build**: `auth-stub.ts` issues an unsigned token `verifyAccessToken` was always going to refuse, and there was no SDK, no PKCE flow and no login screen. §12.1's inject-the-token-getter rule paid for itself — the swap touched `main.tsx` and nothing else, no screen and no query. **Three defects surfaced, none of them auth code:** the client id in `api/wrangler.jsonc` was stale in all three environments (checked as `aud`, so every real token would have 401'd with nothing in the message to say why) · `deploy:qa` ran plain `vite build`, whose default mode is `production`, so QA would have silently shipped production's client id · the callback cannot use `history.replaceState`, because the router is built once at module scope over browser history. Verified against the live tenant: discovery `issuer`/`jwks_uri` match the Worker's vars exactly, S256 PKCE supported, JWKS serves one RS256 key. **Still untested: one real browser round trip** — everything either side of it is verified.

**Updated 5 August 2026 — A6–A10 re-validated against the code, ahead of building them.** The same kind of pass that preceded A2/A3, and it found more: **A6 makes `bookTrip` period-dependent for the first time** (it has never called `resolvePeriodLinkage`, so booking becomes refusable with `PERIOD_CLOSED`) · **a trip need not have a customer**, so a trip fare cannot always be posted · **A8's "real decision" was already settled four times** by `lease.ts`/`mileage.ts`/`trip.ts`, and its borne-by lookup already exists — what is missing is a *preview* read, not a lookup · **GAP-35's fix cannot name `NEW.voided_at` unguarded**, because `assert_period_open()` covers 19 tables and only 13 have that column. And the largest finding: **GAP-23 is one of three identical silent zeros**, the other two being GAP-39 and GAP-10, both unowned until now and both now **A10**. Plan.md carries the full findings and the re-ordering — **GAP-35 (now A9a) goes first**, because A6 and A10 each add another place it can fire.

**Updated 5 August 2026 — A5 done.** Driver history reads, closing GAP-24 and GAP-29: `GET /api/driver/{id}/view`, the staff-facing twin of P12's `GET /api/driver-view`. **It reuses `getDriverOwnView` unchanged** — one domain function, one wire shape (`toDriverViewResponse`, lifted out of the driver-view handler and shared), so the two screens can never disagree about a driver's money. What differs is only the gate and where the id comes from: `dailyOperations` and an explicit business-scoped `{id}`, rather than `viewOwnData` and `requireDriverId`. INV-25 is untouched — it forbids a `driverId` slot on *the driver's own* route, and that route still has none; this is a separate route with a separate gate, and a linked-driver token 403s it for **any** id including their own (proven, both cases). 4 new integration tests in the existing `driver.test.ts` (10 total). GAP-29's `GET /api/advance` was **not** built — this composed read returns the driver's advances already, so a second endpoint would have been the half-built increment Web-P8b deliberately avoided.

**Updated 5 August 2026 — deployed.** Both environments are live and verified: **`fleetsettle.com`** (production) and **`qa.fleetsettle.com`** (QA), each two Workers on one hostname — the client on a Cloudflare Custom Domain, the API on a `/api/*` route Cloudflare matches first. Same origin, so this needed **no application code at all**: every route already sat under `/api/*` and the client already defaulted `VITE_API_BASE_URL` to `""`. Neon `main` and `qa` both carry migrations `0001`–`0007` with the DM §13 drift check clean. `develop` → QA and `main` → production both deploy automatically on merge. [DEPLOYMENT.md](DEPLOYMENT.md) is the runbook and carries the full state; TS §8/§9/§10 and IG §9 are updated to match. **Three things worth carrying forward, each of which cost real time:**

- **Exit 134 from `npm run lint` in CI is a crash, not a lint failure.** The first full gate run since the codebase grew died at ~2 GB with "Ineffective mark-compacts near heap limit". Type-aware `typescript-eslint` holds the whole program graph, so the ceiling rises with the project. Fixed with `NODE_OPTIONS: --max-old-space-size=4096` in `checks.yml`. It passes locally on the same default cap, which is exactly what makes it confusing.
- **A freshly deployed route has not propagated for a few seconds, and during that window `/api/*` answers 200 with `index.html`** — identical to the one failure mode this routing design has. Observed on the first QA deploy, diagnosed by reading the *body* rather than the status. Every smoke assertion now retries (`.github/scripts/smoke.sh`).
- **There is no approval gate on the production path.** Required reviewers are a GitHub environment protection rule and those do not exist on a private repository on the free plan. Deploy-on-merge was chosen anyway, so **the pull request into `main` is the only human decision** — nothing pauses after it.

**Updated 5 August 2026 — B0 done: the `/more` hub and sign-out.** Closes GAP-37 and GAP-40, the first item in Plan.md's "The order, end to end". `/more` no longer renders `NotBuiltYetScreen` — it is a real screen, currently one row (**rows for what exists only**: Cash arrives with B2, Reports with B4, Close the month with B3, each adding its own row rather than one pointing at a placeholder). The row it carries today is **sign-out** (GAP-40), which needed its own small piece of plumbing: `useAuthContext().signOut` is a hook value built after React mounts, and the composition root builds `getToken`/`signOut` before it — so `auth-asgardeo.ts` gained a second registration slot (`registerAsgardeoSignOutSource`/`createAsgardeoSignOutGetter`) alongside the token one B8 already had, filled by `AuthGate` the same way. A new `AuthActionsContext` (mirrors `ApiContext` exactly) is what lets `MoreScreen` reach it without importing the SDK directly, and its `signOut()` **clears the query cache before calling the raw sign-out, never after** — the trap recorded when this item was filed: TanStack Query holds one person's money on screen, and the next sign-in on the same device must not paint it before the first fetch returns. The confirm is a `Sheet`, not `Dialog` — `Dialog.tsx`'s own docstring reserves it to INV-1/INV-17/M-10's three call sites, and sign-out is neither destructive nor one-way. 20 new tests (`auth-asgardeo.test.ts` +2, `AuthGate.test.tsx` +1, `AuthActionsContext.test.tsx` new file +2, `MoreScreen.test.tsx` new file +4, `App.test.tsx` +1, plus the pre-existing files' assertions unaffected); web now 79 files / 290 tests, `npm run check` clean end to end.

**Updated 5 August 2026 — A9a done, closing GAP-35.** Migration `0008` narrows `0006`'s "an `UPDATE` that leaves `posted_period_id` untouched skips the closed-period check" exception: it now excludes the one case that is actually a new posting in disguise — a void (`voided_at` `NULL → NOT NULL`). `to_jsonb(OLD/NEW) ->> 'voided_at'` rather than a direct field reference, because only 13 of the 19 `assert_period_open()`-covered tables carry that column; a direct `NEW.voided_at` would have thrown on the other six. `voidExpense` — the one void-and-replace path that exists today (GAP-12/A9b covers the rest) — gained the `isPeriodClosedViolation` → `PeriodClosedError` mapping every other write already had, and its own regression test, which previously *asserted the bug* ("succeeds even after the expense's own period has closed"), now asserts 409 `PERIOD_CLOSED`. Two stale comments corrected in the same commit — both `domain/expense.ts` and `queries/expense.ts` described the old behaviour as intentional design.

**Verified against a fresh ephemeral Neon branch** (`write-migration` skill's own prescribed path — apply, drift-check, test, discard), not the shared dev branch: migration applied clean, DM §13 drift check clean, and the full suite ran **378/378 green** in one pass with none of the connection flakiness the shared branch has produced repeatedly this project (documented in [§5](#5-the-traps) and again above at B8/A4/A5) — a fresh branch has no contending session on it. **This also surfaced that the `388` this row has carried since A5 was already wrong** before A9a touched anything; corrected above.

**Updated 6 August 2026 — GAP-3 closed, out of sequence: the daily-confirmation flow was silently discarding taps in production.** An independent UI/UX review (`UI-UX-REVIEW.md`, run against the built client with Playwright, not against this tracker's own accounting of what's finished) found that `confirmDay`'s idempotency check — "a row already exists for this `(daily_lease_id, business_date)`" — had been keyed on existence alone since P3, when its own doc comment was still true: nothing produced a plain `open` row yet. **P13 made that false in production and nothing re-checked it.** `generate-day-cards`, a live Cron Trigger on both environments since 5 Aug, pre-inserts an `open` day_record for every scheduled day up to 90 days out; `confirmDay` treated that placeholder exactly like a genuinely confirmed row and returned it unmodified, writing no obligation, no payment, no allocation — the user's actual tap silently discarded. The client compounded it: `ConfirmDayCard` mapped `state: "open"` to the label `"Confirmed"` and rendered zero buttons, so the failure was invisible rather than merely wrong. This was GAP-3, sitting in §4's "correct to leave" table on a judgment made before P13 existed and never revisited — the third instance of "a gap's reason is a fact with a date on it" (§6 already names two, GAP-13 and GAP-23).

**The fix is in the write path, not just the read of it.** `confirmDay.ts` now only treats a row already in a *terminal* state as the no-op; an `open` row is the write path, filled in by `confirmOpenDayRecord` (`queries/day-record.ts`) — an `UPDATE` guarded on `state = 'open'` in the `WHERE` clause itself, never a second `INSERT` (the unique index would reject that). That guard is also the concurrency fix: two confirms racing the same placeholder have the loser's `UPDATE` affect 0 rows once the winner commits, read back as the ordinary no-op rather than a thrown constraint violation. Both `posted_period_id` and `belongs_to_period_id` are re-resolved at confirm time rather than trusted from generation time, in case a day sat unconfirmed long enough for its period to have since closed — the same `resolvePeriodLinkage` a fresh row already used. `ConfirmDayCard.tsx` no longer treats a fetched `open` row as settled, and renders the three real buttons off that row's own `expectedMinor` instead of the lease's current rate. One new integration test seeds the exact shape the cron leaves behind before confirming (nothing in `day-record.test.ts` had ever done this); one new web test covers the client half. `day-record.test.ts` 17/17, `trip.test.ts` and `scheduled.test.ts` re-run in full since both touch `day_record` (32/32), web 79 files/291 tests, all against the live suites, `npm run check` clean.

**A process finding worth recording alongside the fix itself: nothing this session has actually run through CI.** `checks.yml` and `integration.yml` both trigger only on `pull_request` into `develop`/`main` — every commit from A2 onward has gone straight to `build/p0-foundation` via `git push`, which nothing listens for. All verification since has been local, substituting for a gate that was designed to run in CI instead specifically because the integration suite is the expensive part. Decided going forward: PR into `develop` (QA auto-deploys on merge there; `main` has no such gate and deploys production on push with nothing pausing after) — run only the tests actually touched locally, and let `integration.yml`'s fresh-branch-per-PR run carry the full-suite cost, in parallel with the next item rather than blocking it.

**Updated 6 August 2026 — A6 done, closing GAP-23.** The trip receivable: `bookTrip` now posts a `trip_fare` `obligation` (`direction: owed_to_us`, `party_type: customer`) when a charter has a customer and a nonzero agreed amount — the design settled and recorded in [Plan.md](Plan.md), unchanged here. Migration `0009` widens `obligation_kind_check` (unnamed in `0001`, confirmed against the live branch rather than assumed) to admit the new literal; every existing row already satisfies the widened constraint. `cancelTrip` voids it (`voidObligationBySource`, `queries/obligation.ts` — a source-scoped void generalised for reuse, not `trip`-specific), reusing `input.cancelReason` as the void's own reason when given, falling back to `"Trip cancelled"` when not.

**Scoped to where money is actually posted, not the whole endpoint.** `resolvePeriodLinkage` is called only inside the `customerId !== undefined && agreedAmountMinor > 0n` guard, not unconditionally at the top of `bookTrip` — an owner-driven charter with no customer touches no period-scoped table (the allocation and day-record pause carry no `posted_period_id`) and is refused nothing new, matching CLAUDE.md's "only validate at system boundaries" over Plan.md's own broader-sounding phrasing. `due_on`/`effective_due_on` are the trip's `endDate` (the fare falls due once the trip is over), while `posted_period_id` comes from `resolvePeriodLinkage(businessId, bookingDate)` — today, per W-35, never the trip's own travel dates.

**Found while fixing the golden fixture's own test, not in the domain code:** `accounting-period.test.ts`'s G-1 test (134,000) and `trip.test.ts`'s §7.1 close test both had an ad-hoc query — `WHERE source_type = 'trip' AND source_id = tripId` — that implicitly meant "the driver-fee obligation" only because nothing else with that source had ever existed. A6 gave `trip_fare` the identical `source_type`/`source_id`, and both queries silently started summing a receivable into what they called "costs." **Production's own report query was never at risk** — `sumVehicleCostsForPeriod` (`queries/reports.ts`) already filters `direction = 'owed_by_us'`, which a receivable never matches — this was a latent coupling in two tests' own shortcuts, not a defect in the code they were testing. Both fixed by adding the same `direction`/`kind` scoping the real query uses; G-1 still lands on **134,000**.

7 new integration tests in `trip.test.ts` (26 → 33): raises the receivable (customer + amount) · raises none (no customer) · needs no period (no customer, no open period, still 201) · 409 `PERIOD_CLOSED` booking a customer charter with no period open, transaction fully rolled back · cancel voids it, with and without an explicit `cancelReason` · 409 `PERIOD_CLOSED` cancelling into a since-closed period (A9a's own rule, proven end to end). `npm run check` clean; `trip.test.ts` 33/33, `accounting-period.test.ts` 10/10 (G-1 unmoved), `vehicle.test.ts` 27/27 and `post-closure-charge.test.ts` 6/6 re-run since both reference `POST /api/trip` — api integration now 31 files / 386 tests.

**Updated 6 August 2026 — Track B validated before building it, and three of its items were not buildable.** The same kind of pass that preceded A2/A3 and A6–A10, this time screen by screen against `web/src/`. **B4 and B5 were both recorded here as needing nothing, and neither could have started**: `RootLayout` hardcodes `shell="operate"`, `FirstRunGate` exposes only a `renderOperate` branch (`owner` and `driver` both fall through to `NotBuiltYetScreen`), the role is unreadable outside that one file's local `MeResponse` interface, and UI §12.4's `lib/capabilities.ts` + `<Can>` do not exist at all — which also left B3's "the close action is **absent** for a manager" (M-22/W-49) with nothing to gate on. `AppShell` itself was ready the whole time (`shell="review"`/`"mine"` both built and tested); nothing could reach either. That plumbing is now **B0b** in [Plan.md](Plan.md), ahead of all three, on the same reasoning that made B0 its own item. Two further findings: the **UI §11.2 chart palette is eight raw hex pairs** and this project forbids raw hex, so they become `--color-chart-*` tokens (and each must reach `cn.ts` or tailwind-merge drops it); and `GET /api/audit-log/{tableName}/{recordId}` is **per-record, not a feed**, so B3's `Timeline` attaches to a corrected payment rather than to the month. One new gap, **GAP-41**, below. Plan.md now carries a box-by-box implementation checklist for every remaining Track B item.

**Updated 6 August 2026 — A0, GAP-42: `createBusiness` assigned every signup the one role with no interface, and nothing could reach any other role either.** Reported from the deployed app: signing in showed "Review — Not built yet" and nothing else, for every account. Traced to `domain/setup.ts`, not the client: `insertBusinessMember` hardcoded `role: "owner"` for the business creator. UI §1.1 routes `owner` to the Review shell; Review is unbuilt (B4), so `FirstRunGate` renders `NotBuiltYetScreen`. **The backend never restricted this account at all** — `owner` sits in `STAFF` in `auth/policy.ts` and holds every capability except `viewOwnData` — so the fix is one word: the creator is now `owner_manager`, matching F-0.1 step 3 ("lands on an empty home screen with one action: Add a vehicle") and F-0.2's own actor for the very next flow over the same business. `business.test.ts` had asserted `role: "owner"` — the same "regression test pins the bug" shape A9a found in `voidExpense` — now asserts `owner_manager`. **Why no test caught it sooner: every integration test mints its users via `mintUser(db, ctx, businessId, "owner")`, an explicit role argument, so nothing ever exercised the role `POST /api/business` itself assigns.** `business.test.ts` 5/5, `npm run check` clean.

**This does not fix the account already stuck on it — if one exists.** The fix governs new signups; the claim that an existing production `business_member` row still reads `owner` was written as fact and was never actually checked against the database. **Checked 7 August 2026: it was wrong.** A direct read-only query against Neon `main` (`br-odd-cherry-afx5394i`, confirmed live production by its `_migrations` table — 0001 through 0007, applied 5 Aug 2026, timestamps matching DEPLOYMENT.md exactly) found `business_member`, `app_user` and `business` all at **zero rows**. Nobody has ever signed up in production. "0b" — the queued data-correction step — is **moot, not done**: there is no row to correct, and none was written, since guessing at one is exactly the "bare `WHERE role = 'owner'`" the step itself warned against. §6 records the correction.

**And it surfaced a second, larger gap: no second person can ever join a business.** `GET /api/business-member` is the only method that route has — no way to add a member, change a role, or link a driver's login (`driver.linked_user_id` is read by `queries/identity.ts` and written by nowhere). So the passive owner partner (B4's entire audience), a `manager`, and every driver (B5's entire audience) are all currently unreachable **by anyone**, not merely unbuilt. **GAP-43**, Plan.md's **A11**.

**Updated 6 August 2026 — GAP-43 has a specified mechanism now: `docs/product/use-cases.md` W-57.** UC-03 ("share a vehicle with a manager") had always said "pick the user" without saying from where — invisible on a solo business, which is exactly why nobody had specified it. **W-57** ⚑ (use-cases v1.2.3) settles it as the same shape W-42 already used for a driver: the owner/owner-manager generates an invite code scoped to a role, hands it over out of band, the invitee redeems it at their first sign-in. `user-flows.md` v1.1.3's **F-1.4** carries the steps; **F-1.8** (link a driver's own account) moves from phase 2 to phase 1 alongside it, since UC-03 and UC-07 turned out to be the identical unanswered question. **A11 is no longer unscoped** — it is a real build (an invite-code table, two endpoints, a `FirstRunGate` branch), sized L, ready to plan.

**Updated 6 August 2026 — `UI-UX-REVIEW.md`'s remaining findings validated against the current source, one by one, and triaged.** GAP-3 was the one already absorbed; the other eight were not, so each was re-checked directly against `web/src` and `docs/` rather than taken on the review's word — this repository's own standing rule. **Five confirmed, real, and now scheduled as GAP-44 through GAP-48** (below), bundled as Plan.md's new **B9**, independent of everything else on either track. **Two do not hold up and are corrected, not scheduled** — recorded in [§6](#6-what-validation-corrected) rather than silently dropped:

- §7.3's claim that `DialogConfirmFooter` defaults its button to `"Confirm"` is false against the code as it stands: `confirmLabel: string` ([Dialog.tsx:65](web/src/design/primitives/Dialog.tsx#L65)) is a required prop with no default value — omitting it is a TypeScript compile error, not a silent fallback to the forbidden word.
- §7.2's claim that "Excess rate per km" ([StartLeaseScreen.tsx:370](web/src/features/vehicles/StartLeaseScreen.tsx#L370), [RenewLeaseSheet.tsx:96](web/src/features/leases/RenewLeaseSheet.tsx#L96)) violates M-25 misreads the rule. §9.6's vocabulary lock bans "rate" only as a synonym for two *specific* words — "Daily lease amount" and "Driver day fee" — because those two mean opposite directions of money and both risk collapsing to "rate." "Excess rate per km" is a third, unrelated concept (the mileage-overage charge) that **the specification itself names this way throughout** — `use-cases.md` W-16/W-19, `user-flows.md` F-2.1, and the official happy-path test script (`docs/testing/suites/01-arrangement-a-monthly-rental/happy-path.md`: *"VERIFY: Daily limit = 100, excess rate = 25"*). Renaming it would contradict `docs/`, not obey it.

One finding is neither confirmed nor rejected — §4's nested-`Sheet`/`AmountPad` interaction can't be settled by reading source; it needs the two-minute manual check the review itself asked for, on a real browser, before anyone builds against it either way. Recorded under B9 rather than dropped.

**Updated 6 August 2026 — the first real browser round trip, live against `qa.fleetsettle.com`.** Every prior review of this client — including `UI-UX-REVIEW.md` — ran against `page.route()` mocks with `VITE_AUTH_MODE=stub`, which never once rendered `AuthGate` or exercised a real sign-in. This session did: Chrome DevTools MCP driving a real browser, a human entering real Asgardeo credentials, live QA data. **It found four real things in about twenty minutes**, three of them bugs no amount of mocked review could have caught, because the two screens involved are structurally unreachable with auth stubbed out.

1. **GAP-49 — the sign-in screen, and two of three role placeholders, render with no page background at all, anywhere in the DOM.** Confirmed by walking the computed-style chain from `<h1>` to `<html>`: every ancestor is `background-color: rgba(0,0,0,0)`. `Screen.tsx` — used directly by `AuthGate`'s loading/sign-in states and by `NotBuiltYetScreen` (`FirstRunGate`'s fallback for `owner`/`driver`) — sets no background of its own; only `AppShell.tsx`'s root div carries `bg-page`, and none of these three render inside one. In a light-mode browser this is invisible by coincidence (the browser's default white canvas happens to sit close to the light-mode `--color-page` token); in a **dark**-mode browser — the automated Chrome instance's default, and a common device default — `--color-ink-primary` resolves to `#f5f5f0` and renders against that same white canvas: near-invisible white-on-white text on literally the first screen every user sees. Confirmed structural, not cosmetic, and confirmed **not** a whole-app bug: once past `FirstRunGate` into a real `AppShell`-wrapped screen, dark mode is genuinely solid — `UI-UX-REVIEW.md` §5 was right about that part, because stub mode never left `AppShell` in the first place.
2. **GAP-50 — closing a `Sheet` can leave `aria-hidden` on an ancestor of a still-focused element**, a live console warning (`Blocked aria-hidden on an element because its descendant retained focus`), reproduced twice independently: once closing a single sheet (Add a vehicle), once closing a nested one (Add → Add a driver). This confirms and generalises `UI-UX-REVIEW.md` §4's "low confidence" nested-`Sheet`/`AmountPad` finding — it isn't one nested case, it's `Sheet.tsx`'s vaul-based focus/inert handling on close, full stop. A real WCAG violation (hidden focus is explicitly against the ARIA spec), and it fires on the single most-used interactive primitive in the client.
3. **GAP-51 — F-1.7, "Set up the daily lease," has no implementation anywhere, client or spec-wireframe.** Confirmed three ways: `grep` for `F-1.7`/`SetUpDailyLease`/`DailyLeaseForm`/`StartDailyLease` across `web/src` returns nothing; `docs/design/ui-ux-guidelines.md` has zero mentions of F-1.7 at all — it was never wireframed; and the vehicle calendar's free-day tap-through is confirmed correct per F-1.5's own spec (`onSelectFreeDay`/`onSelectFreeDayForTrip` only, matching F-2.1/F-5.1 — daily-lease setup was never supposed to live there). Meanwhile `api/src/route-defs/dailyLease.ts`'s own doc comment names it outright — *"F-1.7 / UC-05 — starting arrangement B"* — and `startDailyLeaseRequestSchema` (`packages/shared/src/schemas/arrangement.ts:46`) is a complete, ready request shape (`vehicleId`, `driverId`, `patternType`, `patternWeekdays`, `effectiveFrom`/`effectiveTo`, `dailyLeaseAmountMinor`). The backend was built and labelled for a client screen that never got built. **This blocks arrangement B — the daily-lease model this project's own running example (the bus) runs on, where the driver pays you (UC §1.2) — from ever being usable by a real business**, and it's what stopped this session from reaching a day-confirmation card to verify GAP-3 live.
4. **Not a code gap — an environment gap.** This QA account's `business_member.role` was still `owner`, landing on the exact dead end A0 fixed. `git log` confirms why: A0 (`17da20d`) is the tip of `build/p0-foundation` and has never been merged into `develop`, so QA has been running the pre-A0 signup path this whole time. Fixed for this session with one scoped `UPDATE` against the `qa` Neon branch (`business_member.id = '019fd4c9-0398-7712-b234-62daf2225a37'`, by explicit request); **the branch itself is still unmerged**, so every other environment-dependent fix since A0 (A6, A9a, GAP-3) is equally unverified on QA until it lands.

**What this changes about how validation passes get run, not just what they find:**

- **Stub-mode/mocked review has now missed real bugs on the same boundary twice** — B8's own writeup flagged "one real browser round trip still unexercised" as an open risk, and it stayed open long enough for GAP-49/50 to ship. Standing rule going forward: **anything touching `AuthGate`, `FirstRunGate`'s non-operate branches, or any component that can render outside `AppShell`, gets one real-browser check, both colour schemes, before being called done.** Mocked review is not sufficient at this specific boundary — it structurally cannot be, since the mock replaces the exact code path in question.
- **Nobody had been reading the browser console as part of any review.** It surfaced GAP-50 in under a minute. Added to the standing checklist below.
- **A route-def-by-route-def, screen-by-screen validation pass checks that what exists matches spec — it does not notice a flow that was never wireframed *and* never built**, because there's nothing on either side to compare. GAP-51 fell through every prior pass for exactly this reason. The gap is in the method: none of the validation passes to date have enumerated `user-flows.md`'s full phase-1 flow list top-down against both `ui-ux-guidelines.md` and `web/src`. That audit hasn't been run yet and should be, soon — F-1.7 slipping through whole is reason to suspect it isn't alone.
- **QA is only a meaningful live-verification target when it's current.** Testing against a stale QA branch nearly produced a false "the product is unreachable" reading for the second time in two days, for a bug that's already fixed on `build/p0-foundation`. Before the next round of live verification, merge to `develop`.

Plan.md's queue is re-ordered below this finding: **GAP-49 (trivial, huge blast radius) and B10 (F-1.7 — arrangement B is unusable without it) now sit ahead of A11**, not behind it — A11 blocks two roles from existing at all, but B10's absence blocks the *one role that already works* from doing its actual job, which is the more urgent failure of the two now that A0 has shipped.

**Updated 7 August 2026 — B11 done, closing GAP-49 and GAP-50. Both were worse than reported, in opposite directions.** GAP-49's fix is a `@layer base` rule painting `--color-page` and `--color-ink-primary` on `html` itself, so the three screens that render outside `AppShell` — and any future one — are covered at the cause rather than at the call site. It also surfaced that **`color-scheme` was declared nowhere in the client**, leaving the UA canvas, scrollbars and native form controls light under a dark theme: the same bug one layer down, fixed in the same block. Verified in a real browser in both schemes, which is the only way it can be verified.

**GAP-50's write-up had the mechanism backwards, and the correction is the useful part.** The violation fires on **open**, not on close: `vaul` ships `autoFocus: false` and preventDefaults Radix's open-auto-focus so a drawer does not summon a phone keyboard, but the cost is that focus never enters the drawer — it stays on the trigger, which Radix has just marked `aria-hidden` along with the whole background. One prop (`autoFocus` on `Drawer.Root`) closes it. **Testing that fix then exposed a second, independent bug nobody had reported:** `Sheet.tsx`'s doc comment credited vaul with focus restore, and it had never worked. Radix's modal `DialogContent` always redirects close-focus to its own `triggerRef`, which is only set by a `Dialog.Trigger`; every sheet here is driven by an external button, so the ref was always null and closing a sheet dropped a keyboard user at `<body>`. Both are covered by `e2e/sheet-a11y.spec.ts`.

**And the test for it was a false green on the first attempt, which is the finding with the longest reach.** The first version of that spec passed against known-broken code. Headless Chromium computes the accessibility tree lazily: with no client asking for it, the `aria-hidden` check never runs and the warning is never emitted, so the assertion had nothing to see. `Accessibility.enable` over a CDP session is what makes it observable — proven both ways against a deliberate violation before the real test was trusted. **Any a11y assertion that works by watching for browser warnings is worthless without it**, and it fails silently, in the passing direction. §5 carries this as a standing trap.

**Updated 7 August 2026 — the access model for A11 settled, and two prerequisites found that would have made it quietly wrong.** A design pass proposed multiple roles per user, a role switcher, a super-admin role and a "no role yet" empty state. **Settled: one role per user per business, no switcher, no fourth role — but yes to the empty state and yes to real member administration**, which is the need underneath "super admin". Three facts in the code decided it: `owner` and `owner_manager` have *identical* capabilities (every `MATRIX` entry resolves to `STAFF`, `OWNERS` or `LINKED_DRIVER`); `driver` is not a membership role at all, so **a partner who also drives is already representable** with no multi-role machinery; and one user belongs to at most one business by `0003`'s partial index, which `resolveMembership` takes on faith once per request. The reasoning, and the four alternatives declined, are in [Plan.md](Plan.md)'s A11 section — kept per this repository's convention of recording what was not taken.

**The switcher is the one worth restating here, because it is a money argument rather than a UX one.** `audit_log.changed_by` comes from the token's `sub` via `withActor`. Either the audit row is identical whichever role was picked — so the switch is cosmetic and must never gate a capability — or it differs, and *who did this* becomes something the client asserted. In a ledger whose whole promise is being believed about money, that is the last field to make ambiguous.

**Two new gaps, GAP-52 and GAP-53, both prerequisites for A11 being correct rather than merely complete.** GAP-52: F-1.4's own "revoke, then re-invite" alternate is blocked by `0001`'s `UNIQUE (business_id, user_id)`, which no later migration touches — and **`0003`'s own header comment asserts the opposite**, being right about its own index and wrong about the conclusion. GAP-53: `business_member` has no `posted_period_id`, so `0002`'s discover-by-column trigger loop never covers it, and **who granted or revoked whom is recorded nowhere**. Both found by reading migrations rather than by any test, because nothing has ever written `revoked_at` — three readers, no writer. §5 carries the reusable half of GAP-52's lesson.

**GAP-1 also widened rather than duplicated.** `ROLES-AND-CAPABILITIES.md` names a half of it that was previously unstated: `viewReports` is the same flat stand-in seen from the other side — UC-70/71/72 say a `manager` sees only shared vehicles, and every P11 report reads the whole business. Same missing scoping, same root, so one gap — but the *manager* over-sees there, not the owner-manager, and that was not written down.

**Updated 7 August 2026 — B10 done, closing GAP-51: arrangement B can be started at last.** `StartDailyLeaseScreen.tsx`, a `/vehicles/$vehicleId/daily-lease/new` route, and a "Start a daily lease" row in the vehicle-actions menu — the exact menu this session opened looking for the flow and found empty. Built from F-1.7's own four steps, with **no vehicle field**, because F-1.7 has no vehicle step: the flow is entered from a vehicle, so `vehicleId` comes from the route the way `StartLeaseScreen` already takes it. Offered for arrangement **B or no active arrangement** — the latter being precisely the vehicle you would start one on — and hidden for A and C, asserted both ways.

**The amount field starts empty, and that is the most load-bearing decision in the item.** An earlier draft of Plan.md specified it as pre-filled from the driver's default per-day rate, citing U-3. That field is `driverDayFeeMinor`: money *you pay him* on a charter, where the daily lease amount is money *he pays you*. UC §2's UC-04 note records that v1.1 of the specification called both a "per-day rate" and that naming them apart was a deliberate correction — so the plan had re-proposed the exact merge the docs had already been fixed for, six days after the vocabulary lock was written down. Caught by reading the schema, not by any check. §5 now carries the general rule: **before wiring any prefill, name the direction of both fields; if they differ, there is no default.**

**Still outstanding, and it is the one thing B10 could not finish:** confirming against a live environment that `generate-day-cards` then produces a real day-record placeholder, confirmable through F-4.2. That is the check **GAP-3's fix has been waiting on since 6 Aug**, and it needs `build/p0-foundation` merged to `develop` first — a deploy decision, not a build step.

**One thing fixed in passing that was not in scope: `npm run typecheck` has been broken on this branch since A0.** `NewBusinessMember.role` was typed as the literal `"owner"`, so A0's one-word change to `owner_manager` did not compile. It is now `BusinessMemberRole` from `packages/shared` — the set DM §3's CHECK constraint actually allows, `driver` correctly excluded. **This is the second time a commit has shipped with a broken typecheck** (§5 records `2822193`), and both times the same way: the gate was not run before pushing.

**Updated 7 August 2026 — A11 done, closing GAP-43, GAP-52 and GAP-53: a second person can join a business at last.** Migration `0010`: `business_member`'s pair-unique replaced by a partial index scoped to active rows (GAP-52 — F-1.4's own "revoke, then re-invite" alternate was unreachable before this), an explicit `business_member_audit` trigger (GAP-53 — the table carries no `posted_period_id`, so `write_audit_log()`'s discovery loop has never covered it, and who granted or revoked a role was recorded nowhere), a new `business_member_invite` table (the F-1.4 counterpart to the existing, previously-unused `driver_link_invite`), and **INV-31**: a deferred constraint trigger refusing to leave a business with no active owner/owner-manager. Six endpoints: `POST /api/business-member/invite`, `.../{id}/revoke`, `.../{id}/change-role` (revoke-and-grant, never an in-place edit), `POST /api/invite/redeem` (one mechanism for both F-1.4 and F-1.8 — the code alone decides the destination), `POST /api/driver/{id}/link-invite`, `.../unlink`. A new `manageMembers` capability (OWNERS) gates the three `business-member` endpoints; the two driver ones use the existing `manageEntities` (STAFF) — see the correction below for why. 39 new integration tests across 3 files (`business-member.test.ts`, `driver.test.ts`, the new `invite.test.ts`); full suite **32 files / 406 tests, 406/406 green** against a fresh ephemeral Neon branch (write-migration skill's own prescribed path). Web: `RedeemInviteForm`, folded into `FirstRunGate` alongside `CreateBusinessForm` rather than replacing it — one screen, two equally first-class options, matching Plan.md's own "don't distinguish never-had-access from revoked" call. A global `QueryCache.onError` in `main.tsx` now invalidates `["me"]` on any 403, so a demoted user stops seeing affordances without a reload, without every mutation needing to remember it. 81 files / 306 tests, `npm run check` clean.

**Two corrections found building this, both caught before they shipped rather than after:**

1. **UC-03 itself was wrong.** v1.2.3's invite-role list named only "manager, or a second owner-manager" — the plain, passive `owner` role this project's own two-partner example (CLAUDE.md) is built around had no invite path at all, the identical shape of bug A0 fixed for the business creator. `owner` and `owner_manager` already carry identical capabilities (the 7 Aug access-design pass's own finding #1), so this cost no new capability logic — only the flow text (`use-cases.md` v1.2.4, `user-flows.md` v1.1.4) and the endpoint's accepted role values, which already admitted `owner` in the schema and would have quietly accepted it as an invite role even before the docs caught up.
2. **`assert_business_has_owner()`'s first draft fired on `INSERT OR UPDATE`, matching `assert_shares_total`'s shape by rote — and broke roughly a dozen existing test files that mint a manager as a business's only member.** An `INSERT` can only ever add a row; it can never by itself reduce a business's active-owner count to zero, so firing on it added no real protection — every actual removal path (`revokeBusinessMember`, and `changeBusinessMemberRole`'s revoke half) is an `UPDATE`. Narrowed to `AFTER UPDATE ON business_member`, which fixed every affected fixture with no change to the fixtures themselves and no loss of coverage: the same behavioural tests (revoking or demoting the last owner, both 409) still pass. Migration `0010` was only ever applied to a disposable branch at the point this was found, so the file itself was corrected rather than superseded by `0011` — the forward-only rule protects a migration once something shared depends on it, and nothing did yet.

**Not built, and correctly not — Plan.md's own A11 client checklist scoped this to the entry point only.** There is no in-app "invite a member" screen yet (an owner generating a code for someone from inside the product); `POST /api/business-member/invite` and `POST /api/driver/{id}/link-invite` exist and are tested but have no caller. That screen needs a role-aware shell to live in, which is **B0b**, not built yet either — the same reasoning that kept `GET /api/expense` (A1) callerless until a screen was actually scheduled for it.

**Updated 7 August 2026 — `build/p0-foundation` merged to `develop`, QA redeployed, and a real-browser pass against `qa.fleetsettle.com` confirmed the fixes since 6 Aug and found one new gap.** The merge itself had already happened by the time this session checked — five PRs (#5–#9), `deploy-qa` green on each, `origin/develop` at `ba389ff` — this session's own first read of it was wrong because it diffed against a stale *local* `develop` ref instead of `origin/develop`; fetching updates the remote-tracking ref, not the local branch, and local `develop` hadn't been checked out in days. Corrected by fast-forwarding local `develop` to match.

**Live verification, real browser, both colour schemes, the existing signed-in session (token still valid) rather than a fresh sign-in:** `role: "owner_manager"` via `/api/me` lands correctly on the Operate shell (A0's fix, confirmed on the account this session's 6 Aug entry recorded as manually corrected) · **GAP-49** solid in both themes — dark `#0d0d0c`/`#f5f5f0`, light `#f1f1ec`/`#14140f`, computed straight through from `html` · **GAP-50** solid on both a single sheet (vehicle actions) and a stacked one (People → Add → Add a driver) — focus returns to the exact trigger each time, zero `aria-hidden`/focus console warnings either way · **GAP-51** — "Start a daily lease" is live in the vehicle-actions menu, exactly where B10 put it · `/more` (B0) matches spec exactly, one row, "Sign out," no placeholder rows for the three items that haven't shipped.

**One new gap, GAP-54 — and the way it was found is a caution, not a credit.** Trying to run GAP-3's outstanding live check against QA's bus, its calendar showed no day markers despite the overview badge reading "Arrangement: Daily lease." A direct read against the `qa` branch showed `vehicle_arrangement` open at `'B'` and `daily_lease` empty, **which this session first wrote up as a stale-figure bug and then had to withdraw**: `vehicle_arrangement` is a standing configuration, not a live state, so a vehicle configured for daily lease that hasn't started one yet is *exactly* the vehicle B10's flow serves. Badge right, calendar right, gating right — the live data was evidence of correct behaviour, misread as a defect. What survives is a `grep` finding made while checking it: **F-1.2 ("Change a vehicle's arrangement," UC-94, phase 1) has no implementation on either side**, so a vehicle's arrangement is fixed at creation for life — and since the client gates hard on it, a vehicle created as `A` can never book a charter and one created as `B`/`C` can never start a lease. GAP-51's exact shape. Full account, including what was withdrawn and why, in [§4](#4-every-open-gap).

**GAP-3's live confirmation is still open, and it is not blocked by any of this** — QA's bus simply has no daily lease yet. Someone starting one through B10's new flow, then letting `generate-day-cards` run, is the whole check; this session held off because it is a real write to a live environment, not because anything stands in the way. **GAP-55**, minor, same session: the "Add a driver" form's Name field has no `autocomplete` attribute.

**One methodological trap worth keeping, filed in [§5](#5-the-traps):** an accessibility-tree snapshot taken immediately after a click can render before the async data it depends on resolves. This session nearly reported a real driver record as accidentally created by a `Close` button click, based on exactly that race — the immediate post-click snapshot showed a driver that hadn't been there moments before. The network log settled it: the one and only `GET /api/driver` request for that page load, timestamped before any of the "Add a driver" interaction, already carried that driver in its response body. Nothing was written; the earlier "0" was the query's loading state, not its resolved one.

**Updated 7 August 2026 — A12 done, closing GAP-56 and GAP-57: the only new money defect either review pass produced, and the trip receivable's own screen made honest.** GAP-56: `resolveBorneByDefault` resolved arrangement, active lease and active daily lease as of *today* regardless of the expense's own `spentOn` — a real charge could land on whoever holds the vehicle now rather than whoever held it when the cost was incurred, reachable with one lease and no arrangement change at all. Fixed by threading `spentOn` through all three lookups: a new `findVehicleArrangementAsOf` alongside the existing current-only query (kept — the badge and client gating genuinely want "now"), and `findActiveLeaseForVehicle`/`findCurrentDailyLeaseForVehicle` changed from `status`/`effective_to IS NULL` to a real date-range test. **Verified the fix actually catches the bug, not just that it's plausible**: all 4 new tests were run against the pre-fix code first and failed exactly as expected, then re-verified green after restoring it (`git stash` on the fix commit, this repository's own "make it fail on purpose" rule applied to a money defect rather than an a11y one this time).

GAP-57: `TripDetailScreen`'s "Received" row had been `NotAvailable` since Web-P7 for a reason A6 (6 Aug) quietly made false — a real `trip_fare` obligation exists and nothing surfaced it. `tripResponseSchema` gained `receivable: leaseObligationRowSchema.nullable()`, the identical row shape a lease's own dues already use, so the two screens can never disagree about what a due looks like. `findObligationBySource` — one function, six existing call sites, all of them reading a row back immediately after writing it — widened to the full row and gained a `voidedAt IS NULL` filter that changes nothing for any of them but makes a cancelled trip's voided receivable correctly read as none rather than its last live value. `bookTrip` returns the new obligation's own id from inside the same transaction that wrote it, so the booking response can show the real state without a second guard re-deriving whether one was raised.

**`CollectPaymentSheet` was lease-only for a reason that turned out to be an accident, not a design choice**: its `leaseId` prop was used solely for query invalidation, never sent in the actual `POST /api/payment` request, which has always been party-level. Generalised to take `onCollected: () => void` instead — the caller owns its own invalidation — so `TripDetailScreen` reuses the identical sheet a lease's dues already open, rather than a second payment-collection UI. A new `web/src/lib/obligationStatusLabel.ts` replaces three separate copies of the same five-word status vocabulary that were starting to exist across `LeaseHubScreen` and this change (U-6's vocabulary lock applies to consistency between screens, not just within one).

5 new integration tests (booking raises a pending receivable, a real payment settles it, no customer means none — never a fabricated zero per W-56, cancelling voids it, cross-tenant 404) and 4 new/updated web tests. `expense.test.ts` 28/28, `trip.test.ts` 38/38, web 81 files/309 tests, `npm run check` clean end to end across all three workspaces.

**Updated 8 August 2026 — A12 merged to `develop`; a flow-inventory audit run against both tracker documents, finding nine gaps neither had recorded.** `develop` is now at `3794e93` (PR #10), carrying A12 — the item flagged 7 Aug as "unmerged, so every environment-dependent fix since is unverified there" is resolved; `origin/develop` and `build/p0-foundation` agree.

**The audit is the one Plan.md's own 7 August entry recommended and left unsized:** every phase-1 `F-x.y` in `user-flows.md`, checked against `ui-ux-guidelines.md` (wireframed?) and `web/src` (built?), independent of what either tracker already claims — the same top-down pass that would have caught GAP-51 and GAP-54 before either shipped unbuilt rather than after. Full account, with file:line evidence for each, in `FLOW-INVENTORY-AUDIT.md`. **Nine findings, all one shape: a phase-1 flow whose backend shipped in P2–P9 and nothing has ever called it — a write endpoint with no scheduled caller, in neither tracker.** Two of the nine (GAP-61, GAP-62) have no backend either. Formalised here as **GAP-61 through GAP-69** (full rows in §4):

- **GAP-61 — F-0.2, going live mid-stream, has no screen at all.** `PUT`/`GET`/`POST .../commit` on `opening-balance` are all built; `ui-ux-guidelines.md` never wireframed it and `web/src` never called it. The flow's own text: *"Without this flow the first month is a lie and the whole ledger inherits it."* The largest of the nine — no business with pre-existing arrangements, drivers or dues can go live on this product honestly today. Now **B12**.
- **GAP-62 — F-4.7, changing a daily lease's driver, has no backend.** `dailyLease.ts` carries only list/start/get; no reassignment endpoint exists on either arrangement's route-defs. Now **A15**.
- **GAP-63 — F-6.1, pay the driver, has no frontend — and the code already says so.** `QuickAddSheet.tsx`'s own doc comment: *"Payment made (F-6.1, 'pay the driver') has no frontend anywhere yet — not even from a driver's own page."* Known to the code, absent from both trackers until now. Now **B13**.
- **GAP-64 — F-6.3, recording a driver advance, has no caller.** `POST /api/advance` exists; only its *settlement* is reachable (`CloseTripSheet`'s own enforcement copy cites F-6.3 without anything ever calling the record endpoint). GAP-29 (closed, A5) covered the *list* side only and says nothing about this. Now **B13**.
- **GAP-65 — F-6.6, the printed slip's core mechanism, doesn't exist.** The spec marks this **"Not optional"** — a signed, expiring, no-login share link. Zero hits anywhere for a signing/expiry mechanism. B5's "statement link" checklist item is the driver's *authenticated* in-app view, a different requirement that does not satisfy this one even once B5 ships. Now **A14**.
- **GAP-66 — F-6.7, recording a driver's deposit, has no caller.** `POST /api/deposit` exists; B5 only reads the held figure. Now **B13**.
- **GAP-67 — F-1.9, mileage packages, has no create screen.** Create/list/archive all exist server-side; `web/src` only ever selects from existing ones (`StartLeaseScreen`). A real business cannot create a package through the product. Folded into **B2**'s checklist.
- **GAP-70 through GAP-74 came from a second, unrelated pass the same week** — `B4-REPORTS-DESIGN.md`'s verification of every report contract against its own owning-document row, rather than of flows against screens. **A different shape from the nine above: not "a flow with no caller" but "a report whose endpoint cannot produce the screen its use case specifies."** Two of the four are **phase-1 reports** (UC-75, UC-76), which is what made the pass worth doing — the assumption going in was that GAP-41 was the only such case. One (**GAP-72**) is a live wrong number in deployed code rather than a missing capability, and is the only item here that waits on nothing. Two need a `doc-change` *before* their query moves — DM §15 for GAP-70, UI §11.1 for GAP-71 — because in both cases the owning document is where the requirement went missing, not the code. **GAP-74 was added 8 Aug after the owner confirmed what §7.8's "What I'm owed" row means, and it is the sharpest of the five**: `GET /api/partner/{userId}` explicitly declined, in its own schema comment, to compute the figure UC-67 calls *"the line he actually reads"*. Full rows in §4.

- **GAP-68 — F-3.5, scheduled maintenance's predictive half, is unbuilt.** The base action is genuinely covered (`RecordExpenseSheet`'s `servicing`/`repairs` categories); only "system uses history + odometer to prompt next time" is missing, and that's a feature, not a form. **Recorded, unowned, correct to leave** — closer to GAP-19's shape than to the other eight here.
- **GAP-69 — F-7.2, recording a partner payout, has no caller.** `POST /api/partner-payout` exists; B2's own checklist never named a payout screen among its six. Folded into **B2**.

**Why these went unnoticed through every prior validation pass:** every pass to date — A2/A3/B2/B3's, A6–A10's, `UI-UX-REVIEW.md`'s, both `MUST-FIX-FINDINGS.md` editions' — compared what exists against what's specified *for the same flow*, screen-by-screen or route-def-by-route-def. None enumerated the full phase-1 flow list top-down against both sides at once, exactly the blind spot GAP-51 and GAP-54 already demonstrated once each and §5 named as a standing gap in method rather than in code. **Six of the nine share a second, more specific cause: B2's and B5's own checklists name screens by noun** ("partners, banking, cash" / "Mine shell") **rather than by flow id**, so nothing forced every `F-x.y` under that noun onto the list — a payout, a mileage package, a deposit and an advance all fell through the same gap in the same two checklists. Worth carrying forward: any item scoped by noun rather than by flow id should be re-cut against the actual `F-x.y` list before it starts, not discovered mid-build.

**Updated 8 August 2026 — a live browser pass against `qa.fleetsettle.com` (`QA-BROWSER-TEST-FINDINGS-2026-08-08.md`), validated against source the same way every prior external review has been rather than absorbed on trust.** Signed-in read/write smoke test, a responsive pass and a CRUD/state-transition review; no console errors anywhere. Ten claims, checked one by one: six are real and newly recorded (**GAP-75 through GAP-80**), three are real but already covered by an existing item, one needs a live re-check a source read cannot settle.

- **GAP-75 — `TripDetailScreen`'s receivable row shows the wrong field, not just unclear copy.** [TripDetailScreen.tsx:132-138](web/src/features/trips/TripDetailScreen.tsx#L132-L138) renders `receivable.settledMinor` beside the status label — for a `pending` receivable that is `0` **by definition**, so every unpaid trip reads "Due · Rs 0" regardless of the real amount owed. `LeaseHubScreen.tsx:247` establishes the correct pattern for the identical schema (`leaseObligationRowSchema`): it renders `amountMinor`. **A regression inside A12's own work** (GAP-57, closed 7 Aug) — the status logic A12 added is right; the amount next to it never was. A confident, plausible, wrong number on real money, the exact failure CLAUDE.md names first. Now **B14**.
- **GAP-76 — driver and vehicle creation fall back to generic `Invalid input`.** [CreateDriverForm.tsx:30](web/src/features/people/CreateDriverForm.tsx#L30) and [CreateVehicleForm.tsx:41-42](web/src/features/vehicles/CreateVehicleForm.tsx#L41-L42) give their required fields no custom Zod message; `CreateCustomerForm`'s contact validation already shows the right pattern. Folded into **B9**.
- **GAP-77 — no way to see a vehicle's trip history, and the read endpoint doesn't exist either.** Confirmed worse than the report suggested: `vehicle.ts`'s route-defs carry calendar/document/expense/lease/incident/daily-lease, no trip. A charter/hire vehicle's own trip history — booked, closed, cancelled — is unreachable from its detail screen because nothing can produce it. Now **A16**.
- **GAP-78 — driver detail renders the driver's name twice.** [DriverDetailScreen.tsx:40](web/src/features/people/DriverDetailScreen.tsx#L40) sets it as the `Screen` title; [TwoBalances.tsx:39](web/src/components/TwoBalances.tsx#L39) renders `driverName` again as an `<h2>` in the body. Folded into **B9**.
- **GAP-79 — an incident with no description reads as "No description recorded" with no other context.** Literal string match, [VehicleOverviewScreen.tsx:284](web/src/features/vehicles/VehicleOverviewScreen.tsx#L284). Folded into **B9**.
- **GAP-80 — internal phase references leak into end-user copy, three places.** `"no advance list read exists yet (Web-P8b)"` ([TripDetailScreen.tsx:203](web/src/features/trips/TripDetailScreen.tsx#L203)) and `"photo capture needs upload support, not built yet"` (both [RecordExpenseSheet.tsx:184](web/src/features/costs/RecordExpenseSheet.tsx#L184) and [FuelFillSheet.tsx:155](web/src/features/costs/FuelFillSheet.tsx#L155)) — a `NotAvailable` `reason` prop is user-facing text, and these three read as commit messages rather than product copy. Folded into **B9**.
- **GAP-81 — `voidExpense` has had a real backend endpoint since A9a, and nothing in the client calls it.** Plan.md's A9b section already noted this in passing ("Track B's half… belongs to whichever screen owns the record") but never gave it an id or an owner; live testing re-confirmed it — a manager who fat-fingers an expense has no way to correct it from the product itself, only the API. Distinct from A9b's other twelve tables, which need backend work first — this one is client-only and ready now. Folded into **B9**.

**Three real findings, already covered, not double-filed:** no archive/edit/unlink path for a driver/customer/vehicle is **GAP-36 → A9b**; Quick Add omitting payment made/received is **GAP-63 → B13**, found the day before; no central list for overhead expenses overlaps **GAP-41 → B4** (a report-endpoint gap, not a UI-list gap — related, not identical).

**One claim left open rather than guessed at either way: the mobile trip-form layout at 360px.** `Screen.tsx:83-91` already implements a proper `position: sticky` CTA per M-24, the same pattern every other form uses correctly — nothing in the code looks wrong, so this may be a real device-only rendering issue a source read can't confirm or rule out. Filed as unresolved, the same treatment B9's own nested-`Sheet`/`AmountPad` finding got on 6 August.

**Updated 8 August 2026 — B14, B0b, B12, B13 and B3's core all shipped in one session, closing GAP-75, GAP-61, GAP-63, GAP-64 and GAP-66.** Full accounts in Plan.md against each item; the headline findings, because two of the five turned out to need more than the queue entry that scheduled them:

- **B14 — the wrong-field fix, plus the two tests that had pinned it inverted** (the `/Due.*Rs 0/` assertion the same "regression test asserts the bug" shape as A9a's `voidExpense` and A0's `business.test.ts`).
- **B0b — the shells, plus a fail-closed correction to `<Can>` found testing sign-out.** `<Can>` was built reading through `useMe()`, which throws if `["me"]` is missing — correct for a screen that cannot function without a role, wrong for a capability gate. Sign-out clears the query cache *before* the real navigation completes, and in that window a still-mounted `<Can>` crashed instead of hiding. Fixed to read the cache directly and degrade to "hide" — the same fail-closed instinct M-22 already applies to the capability check itself, now applied to the read that feeds it.
- **B12 — opening balances, scoped narrower than the flow's own prose reads, deliberately.** F-0.2's text lists "per-vehicle: current arrangement, current odometer, current lease terms" as one of its steps; the actual `opening-balance` request schema carries no such fields, because F-1.1/F-2.1/F-1.7 already accept an original start date and are where that belongs. Building a second vehicle-setup path inside this form would have duplicated an existing one rather than filled a real gap — caught by reading the schema before building the wireframe's full step list literally, the same discipline that caught GAP-56/B10's own U-3 prefill trap.
- **B13 — the one that changed scope mid-build.** GAP-63 ("pay the driver has no frontend") was filed as a client gap; it wasn't. `POST /api/payment` had never accepted anything but `direction: "received"`, so there was no backend path to a driver's `owed_by_us` balance at all — `createOffset` requires both sides nonzero and can't stand in for a clean bonus or retainer. `recordPayment` and `recordPaymentRequestSchema` generalised to carry `direction`, defaulting to `"received"` so nothing existing moved; two new integration tests prove the new direction settles only what it should and that a payment with nothing owed is held as credit, not dropped. **This is the same shape as GAP-51/54 one layer down** — a gap recorded from the client side alone read as smaller than it was, because nobody had checked whether the backend actually supported the other direction until building against it forced the question.
- **B3 (core) — the close screen and payment correction, `WriteOffSheet`/`PostClosureChargeSheet` deliberately not built.** Both need a specific obligation/lease/trip as an entry point that "opened from the close screen" never had; recorded as its own remainder rather than a half-finished box ticked. **One capability gap found and fixed before shipping, not after:** `CorrectPaymentSheet`'s trigger wasn't gated on `reverseReceipt` (owners only, narrower than the `dailyOperations` that gates reading the list) — a `manager` reaching the screen by direct URL could have opened the sheet and hit a 403 on submit. Fixed to render the row as information, not a button, for a role without the capability.

**Two naming corrections against the shipped implementation, both cosmetic, recorded so Plan.md's own checklist text isn't trusted over the code again:** the checklist endpoint is `GET /api/accounting-period/checklist`, not `.../close-checklist`; the capability gating close is `closePeriod`, not `closeAccountingPeriod`.

`npm run check` clean across all three workspaces after every commit; `web` 85 files/349 tests, `api` integration 32 files/417 tests (two files individually re-verified after the shared Neon branch's own documented connection flakiness — [§5](#5-the-traps) — not a real regression either time), `packages/shared` 78/78. G-1 unmoved at 134,000.

**Merged and deployed 8 August 2026 — PR #11, `acce227`.** All three checks green on the PR (`checks / gate` 5m42s, `integration / verify` 5m28s — drift check clean, 32 files against a fresh branch — and SonarCloud), then `deploy-qa` green on the merge. **`qa.fleetsettle.com` now carries B14, B0b, B12, B13 and B3-core**; `develop` and `build/p0-foundation` agree again.

**Updated 8 August 2026 — a triage pass over the queue itself rather than over the code, and it moved four things.** Nothing new was built; what changed is which document was believed. **Three of the four corrections have one cause: a shipped backend was read as evidence that its screen was due.** This tracker has now recorded that cause three times in three days — P11's nine report endpoints (the B4 scope decision, "backend existence is not phase ownership"), P2's `opening-balance` step list (B12's own narrowing), and now P10's write-off and post-closure endpoints.

- **`WriteOffSheet` and `PostClosureChargeSheet` are withdrawn from the queue, and GAP-15 leaves §4's scheduled table with them.** `user-flows.md` marks **F-8.3** and **F-8.4** *Phase: 2* on their own headers and `use-cases.md` §9.1 names **UC-90 and UC-91 under Second** — so B3's checklist had asked for two phase-2 screens, and B3's remainder inherited them as the head of a phase-1 queue. `docs/` outranks Plan.md; deferred with the reason recorded rather than left as an unchecked box.
- **GAP-81 is promoted out of B9 to take that slot.** It is **F-8.5's** client half — *Phase: 1*, UC-96 phase **First** — with a backend live since A9a and no caller, and it had been sitting at rank 15 inside a bucket of UI nits *below* the two phase-2 sheets. A manager who mis-enters an expense currently cannot correct it anywhere in the product.
- **GAP-74 gets a row for the first time.** It was named as a hard prerequisite inside B4's own Wave 1 checklist and scheduled nowhere — the largest remaining item silently depending on unscheduled work. It now sits ahead of B4 with GAP-41 and GAP-72, all three in `queries/reports.ts`: one visit to one file instead of three.
- **GAP-72 stops being "outside both orderings."** That phrasing has meant *unscheduled* in practice since the day it was written. A one-predicate fix with no owner is deferred, not flexible — it rides with its two neighbours, not on any claim that a report nothing renders is urgent.

**One new gap, GAP-82, and it is a triage failure rather than a build one.** `QuickAddSheet` ships **3 of M-4's 5 actions**; §3's Web-P8b row has said so honestly since it shipped, with no id and no track against it, so nothing ever scheduled the other two. The 8 Aug QA pass then filed "Quick Add omits payment made" as *already covered by GAP-63/B13* — true of the flow, not of M-4 ⚑ itself — and GAP-63 closed over it the same day. **A finding folded into an existing item is only covered if that item's own checklist names it**; B13's named `DriverDetailScreen` and nothing else. Now **B15**.

**Also standing, and unblocked for the first time: one live verification pass (Plan.md's V1).** GAP-3's live confirmation has been open since 6 August, and B0b's Review/Mine shells sit exactly on the `FirstRunGate` boundary [§5](#5-the-traps)'s own standing rule says gets a real-browser check in both colour schemes before being called done — the rule that exists because mocked review shipped GAP-49 and GAP-50 on this code. PR #11 put both on QA; neither has been looked at in a browser.

**Updated 8 August 2026 — a QA rerun re-confirmed most of that session's findings and surfaced one new one; then GAP-81 and the Track A trio (GAP-72, GAP-41, GAP-74) all shipped, clearing every item ahead of B4.** `QA-BROWSER-TEST-FINDINGS-2026-08-08-RERUN.md`, validated the same way every external review here is — source-checked, not absorbed on trust (§6's seventh pass has the full account). B14's fix confirmed live; GAP-76/78/79/80/81 all re-opened exactly as filed; one claim ("Close the month can be opened with all-zero counts") turned out to describe U-7 working correctly, not a defect. **One new finding, GAP-83** — every `DateField`, not only the screen it was reported on, exposes a 1×1 focusable native date input in the tab order.

- **GAP-81** — `ExpenseCostRow` + `VoidExpenseSheet`, one shared pair replacing three screens' identical inline cost-row block. Found and fixed two more stale duplicate `EXPENSE_CATEGORY_LABEL` maps along the way.
- **GAP-76/78/79/80** — closed in the same session, folded out of B9 (full accounts already in §4 above).
- **GAP-72** — the fix *written down when the gap was found* was itself wrong: `date AT TIME ZONE tz` resolves to a different Postgres overload than `timestamp AT TIME ZONE tz`, off by the full UTC+5:30 offset. Verified directly against Postgres rather than assumed; caught because the "make it fail on purpose" rule was applied twice — the new tests failed pre-fix as expected, then one **kept failing against the first-draft fix**, which is what exposed the wrong overload.
- **GAP-41, GAP-74** — both landed clean. GAP-74's `balanceMinor` is a new, separate all-time field; the existing period-scoped `earned` is untouched.

`npm run check` clean across all three workspaces; `web` 87 files/359 tests (up from 85/349 — two new files, `ExpenseCostRow.test.tsx` and `VoidExpenseSheet.test.tsx`), `api` unit unaffected, `api` integration touched files individually re-verified (`reports.test.ts` 24/24, `partner-summary.test.ts` 6/6, `expense.test.ts` 24/24) rather than a full local run — this repo's own standing rule (§5) after the shared branch's documented connection flakiness, re-confirmed today when a full local run stalled past 90 minutes and was killed rather than trusted.

---

## 1. Where things stand

**Backend is complete through P13. Frontend is complete through Web-P8b. Both environments are deployed, and a real person can now log in to them.** As of 5 August 2026 the only thing still blocked on anyone outside this repository is **P14 messaging**, waiting on twelve Meta template approvals.

**"Complete" in the row above means built, not reachable — those turned out to be different claims.** Until A0 (6 Aug), the account the product's own signup flow created held `role: "owner"`, the one role with no screen — every real signup landed on `NotBuiltYetScreen` with nothing else to do. The Operate rows in §3 below were never wrong about what was *built*; they were silent about whether anyone could *become* the role that reaches it. A0 fixed new signups; **A11 (7 Aug) fixed the other half** — a second owner, a manager, or a linked driver can now actually get an account (`POST /api/business-member/invite` + `POST /api/invite/redeem`, `POST /api/driver/{id}/link-invite`). **B0b (8 Aug) narrowed the gap further without closing it**: an `owner`/`driver` token now lands inside a real Review/Mine shell (redirect-to-default-tab, `<Can>`-gated affordances, the whole plumbing) — but every screen inside it is still `NotBuiltYetScreen`. Reachable, still not usable; B4/B5 are what change that.

That changes what "remaining work" means again. Until 5 Aug the product could not be reached at all — no deployment, and an auth stub whose token the Worker was always going to refuse. Until 6 Aug, the one role reachable through it was the one with nothing built. As of 8 Aug, every role can get an account and land inside its own real shell; **what is left is the two screens two of those roles actually need** (B4, B5), plus three money facts that still read as zero (A10) — the fourth and fifth defects of that same family, a money fact that read as the wrong *party* (GAP-56/A12) and a payment direction that silently didn't exist (GAP-63/B13), are both closed. [Plan.md](Plan.md) sequences it.

**One thing the 7 August review pass did *not* change, recorded because the absence is the finding: `docs/` needs no edit for any of it.** All five new gaps are the code failing a specification that is already correct and already specific — GAP-56 is a straight violation of **UC-94** ("a month reported before the change keeps the arrangement that was in force then, **including its cost-ownership defaults**") read against **U-8** ("any record can be entered for a past date… no screen ever assumes it is being used on the day the thing happened"); GAP-54's flow is fully written as F-1.2/UC-94; GAP-57's is F-5.3; GAP-1's scoping is UC-70/71/72 and W-49. **The two that could arguably want a document — GAP-59 (a cross-entity invariant) and GAP-60 (a `replaces_id` column) — are design decisions, and this pass deliberately did not smuggle either into `docs/` as a side effect of a review.** They get a `doc-change` pass of their own or they do not happen; that is the rule this repository already runs on.

CI runs the full gate and the integration suite on every PR, `develop` deploys to QA and `main` to production, both on merge.

| Gate | Result |
|---|---|
| `npm run check` | clean across `api` / `web` / `packages/shared` |
| `web` | 85 files / 349 tests |
| `packages/shared` | 11 files / 78 tests |
| `api` unit | 3 files / 10 tests |
| **`api` integration** | 32 files / 417 tests — **not in `check`** locally ([§5](#5-the-traps)); re-verified 8 Aug 2026 (B13's `payment.test.ts` additions and B3's `accounting-period.test.ts` re-run), each file re-run alone after a shared-branch connection drop on the first pass — clean both times; runs on every PR via `integration.yml` |
| `npm run lint` | 0 errors, 26 warnings — all `react-refresh/only-export-components`, expected ([§5](#5-the-traps)) — the count keeps rising with every new route/context, correctly |
| **Deployed** | `fleetsettle.com` and `qa.fleetsettle.com` live since 5 Aug 2026; Neon `main` and `qa` both at migration `0007` |
| **Login** | Real Asgardeo, PKCE, JWT access tokens — one browser round trip still unexercised by a human |

**Updated 8 August 2026 — the client reaches 22 routes**, up from 15: B0b added `/review`, `/review/vehicles`, `/review/money`, `/reports` and `/me` (all four still `NotBuiltYetScreen` placeholders behind their real shells — B4/B5's own job); B12 added `/opening-balances`; B3 added `/period/close`. §3.3's map names 19 paths; still with no screen are `/cash`, `/partners/:id`, `/reports/:key`, plus `/settings/*` (level-3 config, unscoped). `/people/customers/:id` still renders a placeholder — `/more` now carries three real rows (sign-out, opening balances, close the month) rather than one, and has somewhere to send two of the four remaining doors once B2/B4 land.

### The five rules that decided the order

Still binding on everything in [Plan.md](Plan.md):

1. **`packages/shared` is the root of the dependency graph.** A bug in it is structural, not local.
2. **The schema is the synchronisation point, and the main lever for parallelism.** Once a resource's Zod schema lands in `packages/shared`, the two tracks separate: the Worker builds against a real Neon branch, the client against mocks derived from *the same schema object*. Hand-mirrored types drift and what drifts is a money field. **This rule is what makes the plan's Track A / Track B split legal.**
3. **Auth blocks the client, not the Worker.** `tests/support/auth.ts` mints a user per W-49 role, so endpoints are tested before Asgardeo works.
4. **No cron is ever a prerequisite for a user action.**
5. **Reports come after the writes that feed them.**

### The golden fixtures

The regression suite. Any change that moves one is breaking and must fail loudly (FL §9.1).

| Fixture | Figure | Reproduces at | Status |
|---|---|---|---|
| **G-3** Mileage on an open-ended rental | **7,500** | P5 | ✅ end to end through real endpoints |
| **G-2** One accident | **15,000** | P8 | ✅ against the real period lifecycle |
| **G-1** One month of the bus | **134,000** | P9 | ✅ open July → write → close → open August |

---

## 2. Backend — P0 to P14

Each row is complete unless its Gaps column says otherwise. Gap ids resolve in [§4](#4-every-open-gap).

| Phase | Delivers | Proof | Gaps |
|---|---|---|---|
| **P0** Foundation | Money codec (`Minor = bigint`, largest-remainder split), business-date helper, error codes · Hono + Drizzle + Neon skeleton, per-request db middleware (`neon()` reads / `Pool` transactions) · migration runner (advisory lock + SHA-256 per filename) · `0001` (DM §16.0 DDL) and `0002` (`audit_log` trigger) · `/api/health`, `/api/ready`, `/api/docs` 404 in prod · Vite + React 19 + Tailwind v4 tokens, both dark selectors diffed declaration-by-declaration · PWA shell precache · CSP/HSTS via a native `public/_headers` | codec covers U+2212, M-16 cents, split-sums-to-whole | GAP-28 |
| **P1** Identity + access boundary | `jose` against a **local** JWKS through KV, refetch-once on unknown `kid`, one undifferentiated 401 otherwise · `sub → app_user → business_member` **or** `driver.linked_user_id` in one query · `auth/policy.ts` per W-49 row · **cross-tenant 404, missing capability 403** · the linked-driver test class · rate limiting on `CF-Connecting-IP` · `GET /api/me` | full matrix through the real middleware chain; a real ES256 keypair signs test tokens — only the IdP is mocked | GAP-1 |
| **P2** Entities + setup | CRUD for business/vehicle/driver/customer · three arrangements with the original start date preserved · opening balances (owners-only `manageOpeningBalances`) · paperwork expiry dates · **opens the first accounting period** as part of business creation · the creator is granted `owner_manager` (GAP-42, A0 — was `owner`, the one role with no screen) | | GAP-17, GAP-31, ~~GAP-42~~ |
| **P3** The daily loop | `domain/confirmDay.ts` — **four inserts, one transaction**; one endpoint serves all three F-4.2 buttons via a discriminated union; `paid_in_full` resolves the rate **in force on that date** server-side · idempotent on `(daily_lease_id, business_date)` **and on row *state*, not just existence** (a P13 placeholder is filled in via `UPDATE`, never mistaken for a confirmed fact — GAP-3, fixed 6 Aug 2026) · **`PERIOD_CLOSED` mapped from the trigger, no pre-check** · `earned`/`received` never collapsed | 14 integration | GAP-2, ~~GAP-3~~, GAP-30 |
| **P4** Costs, adjustments, driver money | `POST /api/expense` with **`borne_by` and `paid_by` as two fields**; absent `vehicleId` is a valid overhead cost · UC §6.7's default-owner matrix resolved **server-side** from the vehicle's current arrangement · adjustments/waivers — a waiver raises `waived_minor` only, `amount_minor` stays the charge · `computeObligationStatus()` is the one place status is derived · advances + settlement · deposits as money held · `offset_record`, the only thing that moves both balances · `GET /api/driver/{id}/balances` — two `SUM`s, no subtraction anywhere | 30 integration + 13 shared unit; §6.4's walkthrough lands 0 / 4,000 | GAP-4, GAP-16 |
| **P5** Rent, billing periods, mileage · **G-3** | Billing periods idempotent on `(lease_id, seq)`, boundaries from the lease's own anchor via `addCalendarMonths`, never chained off the previous period · `POST /api/lease` writes lease + handover odometer + **first billing period** in one transaction · **rent fixed per period, mileage allowance per day × days** (W-25) · one-directional excess; multi-period assessments split by days and marked `isEstimated`; at-or-below-threshold auto-waives · F-2.5 renew · F-2.2 payment, oldest-`due_on`-first, surplus returned as `unallocatedMinor` | 22 integration + 18 shared unit; **G-3 = 7,500** split 152/148 | GAP-5 |
| **P5+** F-2.6 close the lease | Four endpoints on `active → closing → closed`: `close` (stop the clock, final period via `applyAdjustmentTx`, final mileage through the unmodified `assessMileage`), `closure-summary` (INV-18), `settle-deposit` (refund / retain / `hold_window`), `close-out` (frees future occupancy). The covering period's `period_end`/`allowance_km` shorten on early closure **regardless of `finalPeriodMode`**; `rent_amount_minor` is never touched | 17 integration, through the real chain start → close-out | GAP-6, GAP-16 |
| **P6** Trips and charters | Booking **pauses** existing `day_record` rows (bulk `UPDATE`, never a loop) · close in one transaction — **INV-17 blocks unconditionally** on an unsettled advance; closing odometer and driver-fee obligation both written **at close, never at booking** (W-41/INV-30); trip P&L degrades distance/litres/km-per-litre to `null` · cancel reconciles the advance via `advance_settlement`, resumes paused days, **deletes** allocation rows (that table has no `voided_at`) · vehicle calendar | 19 + 5 integration; UC §7.1 reproduces **26,000** | GAP-7, GAP-8 |
| **P7** Partners, banking, cash | Ownership shares as **one bulk insert sharing one `effectiveFrom`** — `assert_shares_total()` is a *deferred* trigger, so 60/40 lands as one legal multi-row write; violation → 400, never pre-checked · capital kept distinct from ownership (W-52) · management agreements, `EXCLUDE` overlap → 409, revoke sets `effective_to` · banking events; the request schema offers **only** `absorbed`/`unattributed` · partner payouts | 27 integration | GAP-9, GAP-16 |
| **P8** Incidents · **G-2** | The container: open · off-road with three treatments (`continue` / `credit_days` via `applyAdjustmentTx` / `extend` writing `lease_extension`) · repairs reuse `expense.incident_id` · customer contribution agreed then received · insurance claim and its paired recovery in one transaction · bottom line as a **live snapshot** · closed manually | 14 integration; **G-2 = 15,000** | GAP-10, GAP-11, GAP-16 |
| **P9** Period close, corrections, audit · **G-1** | **`audit_log.changed_by` actually populated** — `withActor` wraps `.transaction` once in `db/client.ts` · close opens its successor in the same transaction; the checklist **warns and lists, never blocks** (U-7) · payment correction where `bearer` decides everything: `back_to_arrears` unwinds allocations newest-first (INV-22), `absorbed_loss` touches no obligation · void-and-replace for `expense` · opening balance locked once the first period closes | 26 integration; **G-1 = 134,000** | GAP-12, GAP-13, GAP-14 |
| **P10** Write-offs, post-closure charges | `write_off` flips the obligation straight to `written_off`, never touching `settled_minor`/`waived_minor` — the bucket stays separate from a waiver (INV-14) · recovery recorded as an ordinary `payment` **deliberately never allocated**; the `write_off_recovery` row is what marks it · post-closure charge posts to the *currently open* period and never checks whether the source is closed — being closed is the point | 13 integration | GAP-15 |
| **P11** Reports | Nine `GET /api/reports/*`, all read-only. DM §15's six ported close to verbatim; UC-71/72/77/79 designed this phase. `profitPerKm`/`kmPerLitre` **degrade to `null`, never zero** · `listAgeingBuckets` takes `asOfDate`, never `CURRENT_DATE` · `viewReports` (STAFF) gates seven, `viewOwnerOnlyReports` (OWNERS) gates UC-77/79 · **a linked-driver token 403s all nine**, proven in one loop | 17 integration | GAP-18, GAP-19 |
| **P12** Driver-view (backend half) | `GET /api/driver-view` — both balances never netted, every day **including excused ones**, closed trips and fees, advances, offsets, held deposit. **INV-25 is structural**: `driverId` is never a request parameter anywhere in the route, so no request shape can ask for another driver · new `viewOwnData` capability, `driver` role alone | 4 integration; INV-25 proven with two drivers on two vehicles | frontend half → Track B |
| **P13** Scheduled work | `generate-day-cards` + the arrangement-A calendar extension — **this is what finally enforced INV-1 for lease and daily lease** · `generate-billing-periods` reusing P5's exact function, catching up in a bounded 24-period loop · both **unscoped by `business_id` deliberately** (a cron is not a request), each unit in its own try/catch · idempotency via `.onConflictDoNothing()` on a **separate insert path** from the one a real booking uses, where a conflict must stay a 409 · real Cron Trigger via `Object.assign(app, { scheduled })`, preserving `app.request()` · `GET /api/home/paperwork-warnings` + `/deposit-releases` — reads, not jobs | 15 tests | GAP-20, GAP-21 |
| **P14** Messaging | 🔴 **Blocked** — twelve Meta template approvals (6 messages × 2 languages), minutes to ~2 days each. `dispatch-messages`, the fifth Cron Trigger, waits with it | | |
| **A2** Partner, banking and cash reads | `GET /api/business-member` (GAP-31) · five list endpoints — `ownership-share` (current split only, `effective_to IS NULL`, safe because `assert_shares_total` structurally forbids two open sets per vehicle), `capital-contribution`, `management-fee-agreement` (**revoked agreements returned, not filtered**), `banking-event`, `partner-payout` · the composed `GET /api/partner/{userId}` (F-7.6/F-7.3) — `putIn`/`takenOut` all-time, `earned` scoped to the open period only and named as such, `holding` reused from P11's cash-position query. No new table, no new write | 25 new integration tests (business-member 4, partner +16, partner-summary 5) in 2 new files; full suite 31 files / 353 tests, 353/353 green (one unrelated file's known connection-flake ruled out by re-running alone) | GAP-4 ✅, GAP-9 ✅, GAP-31 ✅, GAP-39 |
| **A3** Period, write-off and payment reads | `GET /api/accounting-period` (list, newest first, `viewReports`) · `GET /api/write-off` (list, every filter optional, gated `writeOffOrWaiveAboveThreshold` to mirror recording one) · **`GET /api/payment`** (GAP-38 — `dailyOperations`, mirroring `recordPayment`; carries the W-49 linked-driver 403 class) · close checklist gains **`unconfirmedDays`** (GAP-13 — `day_record` rows still `state = 'open'`, scoped to the open period's `posted_period_id`, one `COUNT(*)`, honest under-report if the cron hasn't run yet). No new table, no new write, no domain layer — three straight filtered reads and one added count | 13 new integration tests across 3 existing files; full suite 31 files / 366 tests, 366/366 green (`day-record.test.ts`'s known connection-flake under parallel load ruled out by re-running alone, 16/16) | GAP-13 ✅, GAP-38 ✅ |
| **A4** Customer-scoped reads | `GET /api/customer/{id}/obligation` (GAP-22 backend half — outstanding dues only, oldest due first, reusing `findOutstandingObligationsForParty` rather than a second party-scoped query so the screen can never disagree with what `recordPayment` allocates against; row shape reused from the lease obligation hub's own schema) · `GET /api/customer/{id}/payment` (reuses `listPaymentsForBusiness` scoped by `partyCustomerId`, and its response mapper via a handler-to-handler import, the same pattern `vehicle.ts` already uses for incidents). Both `dailyOperations`, 404 cross-tenant. No new table, no new write, no domain layer | 8 new integration tests in the existing `customer.test.ts`; full suite 31 files / 374 tests, 374/374 green (three files' known connection-flake under full-parallel load — `accounting-period`, `dailyLease`, `vehicle` — ruled out by re-running all three alone, 47/47 clean) | GAP-22 ✅ (backend half) |
| **A5** Driver history reads | `GET /api/driver/{id}/view` — the staff-facing twin of `GET /api/driver-view`, **reusing `getDriverOwnView` unchanged** and sharing one wire mapper (`toDriverViewResponse`), so two screens cannot disagree about one driver's money. `dailyOperations` + explicit business-scoped `{id}`, vs `viewOwnData` + `requireDriverId`. INV-25 intact — the driver's own route still has no id slot; a linked-driver token 403s this one for any id, their own included. GAP-29 closed **without** a separate `GET /api/advance`: the composed read already returns advances | 4 new integration tests in the existing `driver.test.ts` (10 total), green in isolation and inside the full suite | GAP-24 ✅, GAP-29 ✅ |
| **A11** Member and driver access | Migration `0010`: `business_member`'s pair-unique made partial (GAP-52), an explicit audit trigger (GAP-53), `business_member_invite`, **INV-31** (deferred, `AFTER UPDATE` only — see §5 for why not `INSERT` too) · `POST /api/business-member/invite`/`.../{id}/revoke`/`.../{id}/change-role` (new `manageMembers`, OWNERS) · `POST /api/invite/redeem` (one mechanism, two destinations — `business_member` or `driver.linked_user_id`, decided by the code alone; mounted on `verifyTokenMiddleware`, no business resolved yet) · `POST /api/driver/{id}/link-invite`/`.../unlink` (`manageEntities`, matching F-1.6/F-1.8's own "Manager" actor — **not** `manageMembers`, corrected before shipping, see above) | 39 new integration tests across 3 files; full suite **32 files / 406 tests, 406/406 green** against a fresh ephemeral Neon branch | GAP-43 ✅, GAP-52 ✅, GAP-53 ✅ |

---

## 3. Frontend — Web-P1 to Web-P8b

| Phase | Delivers | Tests |
|---|---|---|
| **Web-P1** Router + first-run | Code-based TanStack Router from a factory (`createAppRouteTree(today, history?)`) so tests inject memory history — no `routeTree.gen.ts`, same reason migrations are hand-written · `FirstRunGate` with `retry: false`; 404 → `CreateBusinessForm`; role → shell · `auth-stub.ts` behind `VITE_AUTH_MODE=stub`, set on the **build**, because env vars inline at build time | 4 e2e |
| **Web-P2** Home reads (backend) | `GET /api/daily-lease`, `/api/day-record`, `/api/trip` — the three lists Home needed and no earlier phase's tests had asked for. `GET /api/trip` filters `status = 'booked'`, not a literal `'in_progress'` that would have returned nothing forever | full matrix each |
| **Web-P3** The Home tab | §3.2's ordered stack from six independent reads · new `AlertStrip` + `EmptyState` · `DayCard`/`ConfirmDayCard` gained `elevated?: boolean` | 50 / 154 |
| **Web-P4** Driver detail | `DriverDetailScreen` + a real working `OffsetSheet`. **W-2 by construction** — `TwoBalances` gets two independent totals and derives its own sentence; the screen never computes a net | 52 / 158 |
| **Web-P5** Vehicle overview + calendar | Backend: four vehicle-scoped reads (`/document`, `/expense`, `/lease`, `/daily-lease`) · frontend: Paperwork/Costs/History sections, a **voided expense stays visible struck through with its reason** · calendar with colour **plus glyph** per day; `findVehicleCalendar` extended with a `LEFT JOIN` onto `day_record` so "ran" and "lost" are distinguishable | 54 / 171; api 24/24 |
| **Web-P6a** Two gaps first | F-1.9 mileage packages, entirely unbuilt (the table existed since `0001` and nowhere else) — **archive, never delete** · `GET /api/lease/{id}/obligation`, a **three-way reassembly** because `obligation` has no `lease_id`: rent → `billing_period`, excess → `mileage_assessment`, post-closure → `lease` directly | api 46/46 |
| **Web-P6b** The lease hub | `/leases/:id` — Terms card + dues + `CollectPaymentSheet` (client-side `AllocationPreview`; the server still performs the real allocation and is the only authority), `AdjustObligationSheet`, `RenewLeaseSheet` · `Timeline` gained per-entry `onClick` | 57 / 185 |
| **Web-P6c** Start a lease | `/vehicles/:id/lease/new` — seven steps, six level-1 fields, **U-2 verified against the actual POST body** having no optional keys at all · mileage-package chips · calendar tap-through, arrangement A half | 58 / 194 |
| **Web-P6d** Odometer + closure | `GET /api/lease/{id}/deposit` (returns `null` when none was taken — that is not a W-56 gap) · `ReadOdometerSheet` · `CloseLeaseScreen`, an in-screen stepper on one route. **Back is removed once step 0 succeeds** — the lease is really `closing`, a posted fact, not a draft | 60 / 206; api 20/20 |
| **Web-P7** Trips | `tripResponseSchema` fixed — `closingDate`/`cancelReason`/`advanceDisposition` were fetched but never projected · `GET /api/trip/{id}/expense` · `BookTripScreen`, `TripDetailScreen`, `CloseTripSheet` (**INV-17 blocks with a `Dialog`**, its first real caller), `CancelTripSheet` (never blocks; a 409 reveals a Refund/Keep-as-fee chooser inline) · calendar tap-through, B/C half | 64 / 226; api 26/26 |
| **Web-P8a** Incidents | Three wire gaps closed: recoveries + claim now reach `GET /api/incident/{id}`; `GET /api/vehicle/{id}/incident`; `GET /api/incident/{id}/expense` · `IncidentScreen` as a **container, not a wizard** — every action stays available regardless of `status` · five single-purpose sheets · `VehicleOverviewScreen`'s one app-bar action became a menu | 71 / 254; api 18/18, 27/27 |
| **Web-P8b** Costs + quick-add | **`GET /api/expense`** — every filter optional, newest first, voided rows included; no 404 for a filter naming another business's row, since the `businessId` AND already excludes it · `RecordExpenseSheet` + `FuelFillSheet` — **`BorneByPaidBy`'s first real caller** · `QuickAddSheet` on `AppShell`'s `＋`, shipping 3 of M-4's 5 actions — **that shortfall carried no gap id until GAP-82 (8 Aug), which is why nothing ever scheduled the other two**. **`borne_by` is never recomputed client-side** — omitted from the request unless explicitly overridden to "Us", so the server's §6.7 matrix always wins. **No `ExpenseListScreen`**, correcting the plan's own guess: §3.3's route map has no business-wide costs route, and `docs/` outranks the plan | 74 / 264; api 20/20 |

**Web-P8b's own open gaps** — GAP-29 (`GET /api/advance` skipped for want of a caller), GAP-30 (`expense.odometer_reading_id` never wired), GAP-31 (no business-member list, so paid-by has one option), GAP-32 (borne-by overrides only to "Us"), GAP-33 (`GET /api/expense` has no caller), GAP-34 (fuel fill's U-3 prefill degrades to the first vehicle).

---

## 4. Every open gap

One table, because scattering these is what let GAP-22 sit unowned through four phases. **Track** resolves in [Plan.md](Plan.md); `—` means nothing schedules it yet.

### Scheduled

| id | Gap | Track |
|---|---|---|
| ~~**GAP-42**~~ | ✅ **Closed 6 Aug 2026 (A0).** `createBusiness` assigned the creator `role: "owner"` — the one role with no built screen, so every real signup reached `NotBuiltYetScreen` and nothing else. Backend never restricted the account (`owner` is `STAFF`, every capability but `viewOwnData`); the fix is one word, to `owner_manager`, matching F-0.1 step 3's own stated landing screen. `business.test.ts` had asserted the wrong role, the same "regression test pins the bug" shape as GAP-35's `voidExpense` test. **No production row needed correcting** — checked directly 7 Aug 2026 and production has never had a signup at all; the "already-affected row" this entry originally flagged was an unverified assumption, not a checked fact. Full account above §1 | — |
| ~~**GAP-43**~~ | ✅ **Closed 7 Aug 2026 (A11).** `POST /api/business-member/invite` + `POST /api/invite/redeem` (F-1.4) and `POST /api/driver/{id}/link-invite` (F-1.8) — the invite code scoped to a role and this business, redeemed at sign-in, the same shape W-42 already used for a driver, now built rather than merely specified. The passive owner partner, a `manager`, and every driver can all get an account; **B4/B5 still render `NotBuiltYetScreen`** once they arrive, which is a separate, already-tracked gap (their rows, not this one) | A11 |
| ~~**GAP-52**~~ | ✅ **Closed 7 Aug 2026 (A11).** Migration `0010` drops `business_member_business_id_user_id_key`, replaced by `business_member_active_pair` — a partial unique index `WHERE revoked_at IS NULL`. F-1.4's "revoke, then re-invite the same person" alternate now works, proven both ways (same role and a different role) against a real branch | A11 |
| ~~**GAP-53**~~ | ✅ **Closed 7 Aug 2026 (A11).** Migration `0010`'s `business_member_audit` trigger, one explicit `CREATE TRIGGER` attaching the existing `write_audit_log()` — `business_member` carries no `posted_period_id`, so `0002`'s discovery loop was never going to find it on its own. Verified rows land in `audit_log` for both an insert and a revoke | A11 |
| **GAP-1** | Per-vehicle capability scoping. `managePartnerCapital`/`viewOwnerOnlyReports` are flat, business-wide stand-ins — an `owner_manager` shared one vehicle reaches every vehicle's capital, payouts and reports. **`viewReports` is the same stand-in from the other side**, named 7 Aug 2026: UC-70/71/72 say a `manager` sees only *shared* vehicles, and every P11 report reads across the whole business regardless of role. Same missing `WHERE`-clause scoping and the same root, so it stays one gap rather than a second id — but it is the *manager* who over-sees here, not the owner-manager, and that half was previously unnamed. `ownership_share` has existed as data since P7; what is unbuilt is the scoping on top of it, and which table (ownership or management) decides "his" vehicles for which action. Recorded in `auth/policy.ts` since P7. **Do not build UI implying the scoping exists.** **Status changed 7 Aug 2026 — this stops being theoretical, and the reason is A11.** Every prior edition of this row rested, unstated, on the fact that **no `manager` could exist**: `business-member` was GET-only until 7 Aug, so "a manager sees every vehicle's reports" described nobody. A11 shipped `POST /api/business-member/invite`, `businessMemberRoleSchema` admits `manager`, and `manager` sits in `STAFF` — so **a real manager can now be invited today and will read the whole business's reports, capital and payouts**, against UC-70/71/72's own "only shared vehicles". **This is the fourth instance of the pattern §6 names three times** (GAP-13, GAP-23, GAP-3): a gap's standing reason is a fact with a date on it, and A11 is the date this one expired. It remains genuine design work (which table decides "his" vehicles), so it is not reclassified as a build item here — but "correct to leave" was a judgment about *reachability*, and that judgment no longer holds. **Practical guard until it is scoped: do not invite a `manager` to a real business.** Re-raised, at P0, by `MUST-FIX-FINDINGS.md` §7 — right that it matters, though it did not name the A11 interaction that is what actually changed | **needs re-triage** |
| ~~**GAP-4**~~ | ✅ **Closed 5 Aug 2026 (A2).** `paid_by_user_id` is recorded but raised no partner current-account entry — F-3.1's "no extra step" is unmet no longer: `GET /api/partner/{userId}` sums `expense.paid_by_user_id` at read time. A current-account *entry* would have been a new money table — a new `assert_period_open()` array entry, a new void path, a row that can disagree with the expense it came from. A `SUM` cannot drift from its own source | — |
| ~~**GAP-9**~~ | ✅ **Closed 5 Aug 2026 (A2), F-7.6/F-7.3 half.** F-7.1/F-7.5 already had endpoints (`GET /api/reports/vehicle-month`/`cash-position`, P11, not duplicated); F-7.6 and F-7.3 are now the composed `GET /api/partner/{userId}` — `putIn`, `takenOut`, `earned`, `holding`, one page per partner. B2 still owns rendering it | B2 |
| **GAP-10** | **An agreed customer contribution to an incident never becomes a receivable.** `recordCustomerContribution` writes `incident_recovery` with an `agreedAmountMinor` and leaves `obligation_id` NULL, so the money appears in no receivable, no ageing bucket and no payment allocation — `0001` documents the intent on the column itself (`-- customer contributions become receivable`). Re-scoped 5 Aug 2026 out of "correct to leave": this half is the same defect class as GAP-23 and GAP-39 and is now **A10**. The other half — `incident.status` never auto-advancing to `repairs_recorded`/`recovery_pending` — stays unowned and correct to leave, since the container design (P8) makes every action available regardless of status | **A10** (receivable half) |
| ~~**GAP-40**~~ | ✅ **Closed 5 Aug 2026 (B0).** `MoreScreen`'s one row today. `useAuthActions().signOut()` clears the query cache, then calls the Asgardeo SDK's `signOut` through a registration slot `AuthGate` fills, mirroring the token-getter slot B8 already had. Confirmed via a `Sheet`, not `Dialog` (reserved for INV-1/INV-17/M-10) | B0 |
| **GAP-12** | **Void-and-replace exists for `expense` only.** The other twelve W-50 tables carry the `voided_*` trio structurally, so the mechanism is proven — not each instance of it. A record created by mistake in any of the twelve cannot currently be reversed | **A9b** |
| ~~**GAP-13**~~ | ✅ **Closed 5 Aug 2026 (A3).** The close checklist's `unconfirmedDays` is a `COUNT(*)` on `day_record` where `state = 'open'` and `posted_period_id = periodId` — the same scoping convention `pendingObligations` already used. Cheap and exact since P13 (a `day_record` only exists for a scheduled pattern day), UI §7.7's first row, under-reports honestly if the cron hasn't run for a date yet (U-7) | B3 |
| ~~**GAP-3**~~ | ✅ **Closed 6 Aug 2026, out of sequence.** `confirmDay`'s idempotency check was keyed on "a row exists", not "this day is confirmed" — correct when written at P3, wrong once P13's `generate-day-cards` started pre-inserting `open` placeholders in production. A tap against a pre-generated day silently discarded (no obligation, no payment) and the client rendered it as `"Confirmed / Rs 0"`. Found by an independent UI/UX review, not by this tracker's own process — filed as "correct to leave" on a judgment made before P13 existed. Fix: `confirmOpenDayRecord` (`queries/day-record.ts`), an `UPDATE` guarded on `state = 'open'` that fills in the placeholder in place, re-resolving the period fields at confirm time; `ConfirmDayCard.tsx` no longer treats a fetched `open` row as settled. One new integration test, one new web test, `trip.test.ts`/`scheduled.test.ts` re-run in full since both touch `day_record` | — |
| **GAP-83** | **Every `DateField` in the product exposes a 1×1 focusable control — not just the one screen it was found on.** [DateField.tsx:80-88](web/src/components/DateField.tsx#L80-L88) renders the native `<input type="date">` as `sr-only` with `aria-label={label + " (calendar picker)"}` — visually hidden, **still in the tab order**, so keyboard focus lands on an invisible 1×1 box with no focus indicator (WCAG 2.4.7), and the field ends up with **two** controls for one value: a visible `min-h-tap` button whose accessible name is only the formatted date ("Sat 8 Aug", no indication it opens anything), and the labelled input nobody can see. The component's own docstring records the hidden input as deliberate — `showPicker()`'s overlay does not depend on the input's box size, which is true — but that reasoning covers the *pointer* path and never addressed the focus one. **Reported against Opening balances at 320px; it is `DateField` itself, so it is every date form in the client** — the same "fix at the cause, not the call site" shape as GAP-49. Likely fix is the standard overlay: drop `sr-only`, position the input over the visible button at full size with `opacity-0`, leaving one properly-sized labelled target for pointer and keyboard alike. Found by the 8 Aug QA rerun (`QA-BROWSER-TEST-FINDINGS-2026-08-08-RERUN.md`), confirmed against source the same day | **B9** |
| **GAP-82** | **`QuickAddSheet` ships 3 of M-4's 5 actions — both payment directions are missing.** M-4 ⚑ names all five (fuel, expense, payment received, payment made, new trip); the sheet renders fuel, expense and new trip. §3 has recorded "3 of M-4's 5" honestly since Web-P8b, but it has never had a gap id or a track, so nothing scheduled the other two. **Payment received** has never had a caller here — it needs a party picker, since `CollectPaymentSheet` is reached from a lease's own hub where the party is already known. **Payment made now exists**: B13 built `PayDriverSheet`, reachable only from `DriverDetailScreen`. So what remains is one picker, not two forms, and `EntityPicker` already exists. **This is the half of GAP-63 that B13 did not close** — the 8 Aug QA browser pass filed "Quick Add omits payment made" as *already covered by GAP-63/B13*, which was true of the flow and not of M-4's own requirement, and GAP-63 then closed over it. Found 8 Aug 2026 re-reading the trackers against the shipped code | **B15** |
| ~~**GAP-15**~~ | **Moved out of Scheduled 8 Aug 2026 — it belongs to a phase-2 flow and always did.** F-8.4's "deduct it from his fee" is `POST /api/offset` applied afterward, not a code path, and a combined one-tap endpoint is UI-shaped work — unchanged. What changed is the reading: **`user-flows.md` marks F-8.4 *Phase: 2*** and **`use-cases.md` §9.1 lists UC-91 under Second**, so this sat scheduled against B3 (phase 1, now done) on nothing but the P10 backend's existence. Same correction as the two sheets it travelled with; see the unowned table below | — |
| **GAP-16** | **R2 presigned uploads.** Blocks condition photos at lease start (F-2.1 step 6) and close (F-2.6 step 5), incident damage photos, expense receipts, and the side-by-side comparison. `PhotoCapture` has **0 real callers**; `attachment` (DM §12) is already generic and polymorphic — one small endpoint unblocks all five | A7 |
| ~~**GAP-22**~~ | ✅ **Closed 5 Aug 2026 (A4), backend half.** `GET /api/customer/{id}/obligation` (outstanding dues, reusing `findOutstandingObligationsForParty` — the same query `recordPayment` allocates through, so the screen can never disagree with the allocator) and `GET /api/customer/{id}/payment` (this customer's payment history, reusing `listPaymentsForBusiness` scoped by party). Both `dailyOperations`, 404 cross-tenant. `/people/customers/:id` itself still renders `NotBuiltYetScreen` — that placeholder is B6's own item, not a backend gap | B6 |
| ~~**GAP-23**~~ | ✅ **Closed 6 Aug 2026 (A6), backend half.** `bookTrip` posts a `trip_fare` obligation (`direction: owed_to_us`, `kind: 'trip_fare'`, migration `0009` widens `obligation_kind_check`) when there is a customer and a nonzero agreed amount — `listReceivables` and `POST /api/payment` pick it up with no change to either, the same allocator every other receivable already goes through. `cancelTrip` voids it via the new `voidObligationBySource`. Scoped to where money is actually posted: a charter with no customer needs no accounting period at all, since it touches no period-scoped table. `due_on`/`effective_due_on` are the trip's `endDate`; `posted_period_id` resolves off the booking date (W-35). **`TripDetailScreen`'s "Received" row still renders `NotAvailable`, and its reason string is now false** — `POST /api/trip/{id}` already returns nothing to derive it from client-side; no Track B item currently claims wiring the real figure through, so it stays as `NotAvailable` with a stale reason until one does | — |
| ~~**GAP-24**~~ | ✅ **Closed 5 Aug 2026 (A5), backend half.** `GET /api/driver/{id}/view` returns days (excused ones included), closed trips and fees, advances, offsets and the held deposit for an explicit business-scoped driver id — the breakdown `driverBalancesResponseSchema` never carried. Reuses `getDriverOwnView` unchanged, so the staff view and the driver's own view are the same figures by construction. Rendering the sections is B5's own item | B5 |
| ~~**GAP-29**~~ | ✅ **Closed 5 Aug 2026 (A5) — and still no `GET /api/advance`.** The driver's advances now have a caller: A5's composed read returns them alongside days, trips, offsets and deposit. A separate advance-list endpoint would have been a second way to reach the same rows with no screen asking for it — the same reasoning that withdrew `ExpenseListScreen` (B1). Revisit only if a screen ever needs advances *without* the rest of a driver's history | — |
| **GAP-30** | **`expense.odometer_reading_id` has never been wired through any layer** — a DB column since P3, unlike `trip.opening_odometer_id` which P6 wired. Blocks fuel-fill's odometer and trip-link fields (both level 2). Wiring it means deciding whether a fuel fill creates its own `odometer_reading` row transactionally | A8 |
| ~~**GAP-31**~~ | ✅ **Closed 5 Aug 2026 (A2).** `GET /api/business-member` lists a business's own active owners/owner-managers/managers, gated `dailyOperations` (not `managePartnerCapital`) since its caller is a manager-facing form. `BorneByPaidBy`'s paid-by picker still needs wiring to it — that's B2's own item | B2 |
| **GAP-32** | Borne-by can only be overridden to **"Us"**. Overriding to a *specific* driver or customer other than the vehicle's current party needs either a live preview endpoint or a second "who currently holds this vehicle" lookup | A8 |
| **GAP-33** | **`GET /api/expense` has no caller.** Shipped deliberately ahead of any screen (report-adjacent, real value regardless), but §3.3's IA calls for none in phase 1. Revisit if a costs view is ever specified | — |
| ~~**GAP-35**~~ | ✅ **Closed 5 Aug 2026 (A9a).** Migration `0008` narrows `0006`'s exception: an `UPDATE` that leaves `posted_period_id` untouched still skips the closed-period check, unless it is also the `voided_at` `NULL → NOT NULL` transition, via `to_jsonb` so the six trigger tables with no `voided_at` column stay legal. `voidExpense` gained the same `isPeriodClosedViolation` → `PeriodClosedError` mapping every other write already had. Its own regression test (previously asserting the bug — "succeeds even after the period has closed" — as if it were correct) now asserts 409 `PERIOD_CLOSED` | A9a |
| **GAP-36** | **`driver` and `customer` have no soft-delete column at all**, so a row created by mistake is permanent. `vehicle.lifecycle` has existed since `0001` but no endpoint ever moves it off `'active'`. `mileage_package`'s `archived_at` + archive endpoint is the reference pattern | **A9b** |
| ~~**GAP-37**~~ | ✅ **Closed 5 Aug 2026 (B0).** `/more` is a real screen now — no longer `NotBuiltYetScreen`. It has nothing to route B2/B3/B4 to yet since none of them have landed (**rows for what exists only**), but the hub itself, and the door it opens, both exist | B0 |
| ~~**GAP-38**~~ | ✅ **Closed 5 Aug 2026 (A3).** `GET /api/payment` — every filter optional, newest first, gated `dailyOperations` to mirror `recordPayment`. `POST /api/payment/{id}/correct` (built since P9) finally has somewhere to reach a payment id from. Carries the W-49 linked-driver class: `dailyOperations` is a STAFF-only capability, so a linked-driver token 403s before it ever sees another driver's payment row — proven, not assumed | B3 |
| ~~**GAP-41**~~ | ✅ **Closed 8 Aug 2026.** `GET /api/reports/overheads?periodId=` — a dedicated endpoint, not a tri-state expense filter. `sumOverheadsForPeriod` reuses `sumVehicleCostsForPeriod`'s `expense` filter set with `vehicle_id IS NULL` in place of a specific vehicle; no `obligation` half, since `driver_fee`/`management_fee` obligations are always raised against a vehicle's own arrangement and cannot carry a null one. `viewReports` (STAFF) — same audience as `vehicle-month`, the block it sits beneath. A real `0` when nothing was recorded, never `NotAvailable` (W-56 governs an unknown, not an absent one). 5 new integration tests: sums correctly and excludes a same-period vehicle-attributed cost, a genuine zero, a voided expense excluded, 404 for no such period and for another business's period | — |
| **GAP-39** | **W-53's "management fee reduces vehicle profit" has never actually fired.** `sumVehicleCostsForPeriod` (queries/reports.ts) reads `obligation WHERE kind = 'management_fee'` — the enum value exists, the query is ready — but nothing in this codebase ever inserts one; there is no generator turning a `management_fee_agreement` into a period obligation the way `generate-billing-periods` does for rent. Every vehicle with a managed agreement has been reporting a management fee cost of **0** since P7, silently. Found building A2's `GET /api/partner/{userId}`, which reads the fee from `management_fee_agreement.monthly_amount_minor` directly instead — correct for that one endpoint, but `sumVehicleCostsForPeriod`/UC-70's vehicle profit is still wrong wherever a management fee applies. **Scheduled 5 Aug 2026 as A10**, alongside GAP-10 — the same defect class as GAP-23: an agreed amount that never becomes an obligation | **A10** |
| **GAP-44** | **INV-1 (double-booked vehicle-day) has no `Dialog` anywhere in the client.** `Dialog.tsx`'s own doc comment reserves it to three call sites — INV-1, INV-17, M-10 — and `grep`ing for real `<Dialog` usage (not the doc comments referencing it) finds exactly one: `CloseTripSheet.tsx`, for INV-17. Nothing renders it for INV-1. A double-booking conflict today surfaces as a bare line of `mutation.error.message` in both `StartLeaseScreen.tsx` ([:448](web/src/features/vehicles/StartLeaseScreen.tsx#L448)) and `BookTripScreen.tsx` ([:307](web/src/features/trips/BookTripScreen.tsx#L307)) — confirmed, `VEHICLE_DOUBLE_BOOKED` ([app-error.ts:97](api/src/errors/app-error.ts#L97)) is never referenced anywhere under `web/src`. Not merely a component swap — §9.3 requires "the fix offered inline," and that copy doesn't exist yet either. Found in `UI-UX-REVIEW.md` §2.1, confirmed against source 6 Aug 2026 | **B9** |
| **GAP-45** | **Trip detail's app-bar title truncates at 360px.** `TripDetailScreen.tsx`'s `formatShortDate` ([:45-51](web/src/features/trips/TripDetailScreen.tsx#L45-L51)) sets `year: "numeric"` unconditionally, and the title concatenates two of them ([:122-124](web/src/features/trips/TripDetailScreen.tsx#L122-L124)) — `"1 Aug 2026 – 4 Aug 2026"`, clipping mid-digit next to the cancel-trip icon. §8.3 says show the year only where the date is otherwise ambiguous; §7.5's own wireframe leads with an identifier instead ("Trip #21 · Bus · 28–30 Jul"), which today's title has none of. Found in `UI-UX-REVIEW.md` §2.2, confirmed against source 6 Aug 2026 | **B9** |
| **GAP-46** | **Vehicle calendar's occupied-day cells are invisible to a screen reader.** `VehicleCalendarScreen.tsx`'s occupied-day branch ([:200-207](web/src/features/vehicles/VehicleCalendarScreen.tsx#L200-L207)) renders a plain `<div>` with the state glyph inside `aria-hidden` and no `aria-label` on the container — confirmed by reading the component directly. A sighted user reads state from colour plus glyph; a screen-reader user gets only the bare day number. The free-day case is already correct (`aria-label={"Start a rental from " + cellDate}` etc.) — this is specifically the occupied case, and it's the same pattern `TwoBalances` gets right elsewhere in the product (§10: colour is never the only channel). Found in `UI-UX-REVIEW.md` §2.3, confirmed against source 6 Aug 2026 | **B9** |
| **GAP-47** | **M-26 (landscape) is entirely unbuilt, and it wasn't on any list before this.** `grep -rn "landscape" web/src` returns nothing; confirmed. M-26 (`ui-ux-guidelines.md:111`) requires the app bar to collapse to 44px and the tab bar to go icon-only at 44px below `md` in landscape — a deliberate, specified requirement (WCAG 2.1 SC 1.3.4 forbids locking orientation instead, and the spec's own §16.4 records that rejection explicitly), not an aspirational one. `AppShell.tsx`'s tab bar and `Screen.tsx`'s app bar are both hardcoded to the 56px portrait height with no orientation variant. At 640×360, Home's day card is reachable but its three action buttons sit below the fold — the exact failure the spec's ~192px budget was designed to avoid. The largest of this batch: it touches two shared primitives every screen is built on top of. Found in `UI-UX-REVIEW.md` §2.4, confirmed against source and spec 6 Aug 2026 | **B9** |
| **GAP-48** | **M-11's undo toast has no component rendering into it.** `AppShell.tsx`'s toast host ([:84-85](web/src/design/primitives/AppShell.tsx#L84-L85)) is a bare `<div id="toast-root" />` with a comment stating outright that nothing renders into it yet. M-11 requires a 5-second undo toast for any write that "sent no message and settled no obligation" — no such write currently offers one. Needs both a `Toast` primitive and a decision, call site by call site, about which writes qualify. Found in `UI-UX-REVIEW.md` §7.1, confirmed against source 6 Aug 2026 | **B9** |
| ~~**GAP-49**~~ | ✅ **Closed 7 Aug 2026 (B11).** `AuthGate` and `FirstRunGate`'s fallback screens rendered with no page background anywhere in their DOM chain — computed styles walked from `<h1>` through `<html>` were `background-color: rgba(0,0,0,0)` at every level. `Screen.tsx` sets no background of its own and only `AppShell.tsx`'s root div carried `bg-page`, so `AuthGate`'s loading/sign-in states and `NotBuiltYetScreen` were uncovered: invisible by coincidence in light mode, and in dark mode `--color-ink-primary` (`#f5f5f0`) on the browser's default white canvas, making the sign-in screen's own heading and body text illegible. Fixed structurally rather than at the three call sites — a `@layer base` block in `tokens.css` paints `--color-page` and `--color-ink-primary` on `html` itself, so any future screen built outside `AppShell` inherits it. **`color-scheme` was missing entirely** (grepped: only `prefers-color-scheme` media queries existed), leaving the UA canvas, scrollbars and native controls light under a dark theme — added with the same two-selector shape as the token blocks so the in-app toggle still beats OS dark. Verified in a real browser, both schemes: dark `#0d0d0c`/`#f5f5f0`, light `#f1f1ec`/`#14140f`. `tokens.test.ts` asserts the rule survives, since jsdom cannot render and no component test can catch its absence | — |
| ~~**GAP-50**~~ | ✅ **Closed 7 Aug 2026 (B11), and the diagnosis in the original report was wrong in a way worth keeping.** The violation fires on **open**, not close. `vaul` ships `autoFocus: false` and calls `preventDefault()` on Radix's open-auto-focus (so a drawer does not summon a phone keyboard); the cost is that focus never enters the drawer at all — it stays on the trigger, which Radix has just marked `aria-hidden` along with the rest of the background, and Chrome refuses. Fixed by passing `autoFocus` on `Drawer.Root`. **A second, independent bug surfaced while testing the first: focus was never restored on close either.** `Sheet.tsx`'s own doc comment credited vaul for "focus restore", but Radix's modal `DialogContent` unconditionally preventDefaults close-auto-focus and focuses its own `triggerRef` — which only exists if a `Dialog.Trigger` opened it, and every sheet in this product is driven by an external button. Focus landed on `<body>`. Fixed with an `onCloseAutoFocus` that returns focus to the opener, recorded from a `focusin` listener (React runs a child's effects before its parent's, so reading `activeElement` when `open` flips is already too late). `ActionSheet` is built on `Sheet`, so the nested case is covered by the same fix. Three tests in `e2e/sheet-a11y.spec.ts`, each verified to fail without the fix | — |
| ~~**GAP-51**~~ | ✅ **Closed 7 Aug 2026 (B10).** F-1.7, "Set up the daily lease," had no implementation anywhere — not in the client, not in the UI spec. `StartDailyLeaseScreen.tsx` now builds it from F-1.7's own four steps (driver · pattern · amount · effective date, optional end date), reached from a new "Start a daily lease" row in the vehicle-actions menu — the exact menu this session opened looking for it and found empty. **No vehicle field**, because F-1.7 has no vehicle step: the flow is entered from a vehicle and `vehicleId` comes from the route, as `StartLeaseScreen` already does. The action is offered for arrangement **B or no active arrangement**, hidden for A and C, asserted both ways. **The amount is deliberately not pre-filled from the driver's `driverDayFeeMinor`** — that is money you pay him, this is money he pays you, and UC §2's UC-04 note records that v1.1 merged the two and that separating them was a deliberate correction; an earlier draft of Plan.md re-proposed exactly that merge and it was caught by reading the schema. 409 `DAILY_LEASE_OVERLAPS` is surfaced as an ordinary outcome, never pre-checked. 4 unit tests + 5 gating tests + a full-flow e2e at 360×640 and 320px. **Still outstanding: the live-environment confirmation** that `generate-day-cards` then produces a real day-record placeholder, confirmable through F-4.2 — that is the check GAP-3's own fix has been waiting on, and it needs the `develop` merge first | — |
| **GAP-54** | **F-1.2, "Change a vehicle's arrangement," is unbuilt — so a vehicle's arrangement is fixed at creation for the rest of its life.** `insertVehicleArrangement` (`api/src/queries/vehicle.ts:34`) has exactly one call site, `createVehicle` (`api/src/domain/vehicles.ts:48`), and **that is by design, not an oversight** — `createVehicle`'s own doc comment says so outright: *"its opening `vehicle_arrangement` row (effective from today, open-ended — **F-1.2 is what ever closes it**)."* The gap is that F-1.2 was never built: no route-def, no endpoint, no client screen, against a phase-1 flow (`user-flows.md:356`, UC-94) with its own effective-dated accept criteria. **This is GAP-51's exact shape** — a spec'd phase-1 flow with nothing on either side — and the same reason it went unseen: no validation pass can diff a flow that exists in neither place. **Practical effect, and it is a real functional wall:** the client gates hard on `vehicle.arrangement` — `canStartLease` requires `A` ([VehicleCalendarScreen.tsx:166](web/src/features/vehicles/VehicleCalendarScreen.tsx#L166)), `canBookTrip` requires `B` or `C` ([:168-170](web/src/features/vehicles/VehicleCalendarScreen.tsx#L168-L170)), `canStartDailyLease` requires `B` or none ([VehicleOverviewScreen.tsx:163](web/src/features/vehicles/VehicleOverviewScreen.tsx#L163)) — so **a vehicle created as `A` can never book a charter and one created as `B`/`C` can never start a lease, permanently.** UC-94's own motivating example is precisely this ("the car stops being rented monthly and starts taking charters; the bus comes off its daily lease"), and `use-cases.md` calls it a founding premise §1 relied on and nothing implemented. **Correction to this row's own first draft (7 Aug 2026):** it originally claimed the field "never reflects what actually happens to the vehicle" and cited QA's bus — `arrangement: 'B'`, `daily_lease` empty — as evidence of a stale/wrong figure. **That was a misreading and is withdrawn.** `vehicle_arrangement` is a *standing configuration* ("this vehicle's business model is daily lease"), not a derived state ("a daily lease is running right now"); it is supposed to persist across leases starting and closing, and a vehicle configured `B` with no daily lease yet is exactly the vehicle B10's flow exists to serve. The badge was right, the empty calendar was right, and B10's gating offering "Start a daily lease" there was right. The live data proved correct behaviour; only the `grep` finding survives | — |
| ~~**GAP-56**~~ | ✅ **Closed 7 Aug 2026 (A12).** `resolveBorneByDefault` now takes `spentOn` and resolves the vehicle's arrangement, active lease and active daily lease all as of that date — a new `findVehicleArrangementAsOf` ([queries/vehicle.ts](api/src/queries/vehicle.ts)) alongside the existing current-only `CURRENT_ARRANGEMENT` (kept, since the badge/gating genuinely want "now"), and `findActiveLeaseForVehicle`/`findCurrentDailyLeaseForVehicle` changed from `status = 'active'`/`effective_to IS NULL` to a real date-range test (`startDate`/`endDate` and `effectiveFrom`/`effectiveTo`). 4 new integration tests, each verified to fail against the pre-fix code before the fix was restored (this repo's own "make it fail on purpose" rule) — a lease starting after `spentOn` no longer matches, a since-closed lease's own customer still resolves correctly for a date inside its window, the daily-lease equivalent, and the arrangement lookup itself resolved as of `spentOn` rather than today. `expense.test.ts` 28/28, full `npm run check` clean | — |
| ~~**GAP-57**~~ | ✅ **Closed 7 Aug 2026 (A12).** `tripResponseSchema` gained `receivable: leaseObligationRowSchema.nullable()` — the same row shape a lease's own dues use, so `TripDetailScreen` and `LeaseHubScreen` can never disagree about what a due looks like. `findObligationBySource` ([queries/obligation.ts](api/src/queries/obligation.ts)) widened to return the full row (`kind`/`dueOn`/`waivedMinor` alongside what it already returned) and gained a `voidedAt IS NULL` filter — a cancelled trip's voided `trip_fare` now correctly reads as no receivable, not its last live state; every other caller reads a row immediately after creating it, so the filter changes nothing for them. `bookTrip` returns the new obligation's own id in the same transaction rather than a second guard re-deriving whether one was raised. Client: `TripDetailScreen`'s "Received" row shows the real settled amount and status, tappable while outstanding to open `CollectPaymentSheet` — generalised to take `onCollected` instead of a hardcoded `leaseId`, since it's no longer lease-only, with `LeaseHubScreen` updated to the same shape. A new shared `lib/obligationStatusLabel.ts` replaces three separate copies of the same five-word status vocabulary (U-6). 5 new integration tests (booking raises it pending, a payment settles it, no customer means no receivable — never a fabricated zero, cancelling voids it, cross-tenant 404) and 4 new/updated web tests; `trip.test.ts` 38/38, web 81 files/309 tests, full `npm run check` clean | — |
| **GAP-58** | **`docs/testing/test-manifest.yaml` is 178 cases, every one `not_started`, and it has never been run.** `last_full_run: null`, `last_updated: null`, and `grep` returns `178 × status: not_started` with **zero** in any other state; 38 of them are P0. The file's own header calls itself "the single source of truth for test execution progress" and says "the AI agent reads it before each run" — no run has ever happened. This is not the same claim as "the code is untested" (406 integration + 306 web tests are real and green); it is that **the acceptance spine that maps use-cases to evidence is declared and empty**, so nothing currently proves a *flow* works end to end across UI and API — which is precisely how GAP-51 and GAP-54 both reached production-adjacent QA unbuilt. Untracked anywhere before now. Found by `MUST-FIX-FINDINGS.md` (web edition) §13, confirmed by counting 7 Aug 2026 | — |
| **GAP-59** | **Related ids on one request are each checked against the business, never against each other — so an expense can name a vehicle and a trip that have nothing to do with one another.** [handlers/expense.ts:38-46](api/src/handlers/expense.ts#L38-L46) fetches the trip and the incident purely to prove they belong to this business, **then discards both rows** — `trip.vehicleId` is never compared to `body.vehicleId`, and neither is `incident.vehicleId`. Two same-business ids that describe different events are accepted as one coherent fact. The row then lists under `GET /api/trip/{id}/expense` (filtered by `trip_id` alone, [queries/expense.ts:309](api/src/queries/expense.ts#L309)) *and* under the other vehicle's costs, so one cost is attributed to a trip on one vehicle and to the profit of another. **Not reachable from the client today** — no screen sends both ids (the fuel-fill trip link is GAP-30, unbuilt) — which is what keeps this latent rather than live, the same standing as GAP-1 before A11. **`post-closure-charge.ts` has the same shape** (source, party and vehicle each validated independently). Worth deciding deliberately whether the fix is request-level or a DB constraint; CLAUDE.md's "validate at boundaries" argues for the handler, and DM's preference for constraints-over-code argues for the schema. Found by `MUST-FIX-FINDINGS.md` (backend edition) §6, confirmed against source 7 Aug 2026 | — |
| ~~**GAP-61**~~ | ✅ **Closed 8 Aug 2026 (B12).** `OpeningBalanceScreen` + `AddOpeningBalanceEntrySheet`, reached from a new `/more` row gated `<Can cap="manageOpeningBalances">`. Deliberately excludes per-vehicle setup — the request schema never had those fields; F-1.1/F-2.1/F-1.7 already carry an original start date for that. Full account in Plan.md's B12 section | — |
| **GAP-62** | **F-4.7 — no way to change a daily lease's driver.** `dailyLease.ts` carries only list/start/get; no reassignment endpoint exists on either arrangement's route-defs, no client screen either. Found by the flow-inventory audit (AUDIT-2) | **A15** |
| ~~**GAP-63**~~ | ✅ **Closed 8 Aug 2026 (B13), and it was bigger than filed.** "No frontend" undersold it — `POST /api/payment` had never accepted anything but `direction: "received"`, so there was no *backend* path to a driver's `owed_by_us` balance at all. `recordPayment`/`recordPaymentRequestSchema` generalised to carry `direction`, defaulting to `"received"` so every existing caller is unaffected; `PayDriverSheet` reached via a new "Driver money" `ActionSheet` on `DriverDetailScreen`. Two new integration tests prove the new direction never touches arrears | — |
| ~~**GAP-64**~~ | ✅ **Closed 8 Aug 2026 (B13).** `AdvanceSheet`, reached the same way as GAP-63/66. No trip picker — F-6.3's own text names none, and `tripId` is optional on `issueAdvanceRequestSchema` | — |
| **GAP-65** | **F-6.6 — the printed slip's "not optional" mechanism doesn't exist**: a signed, expiring, no-login share link, per the spec's own words. B5's "statement link" checklist item is the driver's own authenticated view, a different requirement it does not satisfy. Found by the flow-inventory audit (AUDIT-5) | **A14** |
| ~~**GAP-66**~~ | ✅ **Closed 8 Aug 2026 (B13).** `DepositSheet` — take only, no movement action (refund/apply/top-up stays "never automatic," unbuilt by design) | — |
| **GAP-67** | **F-1.9 — no screen creates a mileage package.** Create/list/archive all exist server-side (`mileage-package.ts`); `web/src` only ever selects from existing ones (`StartLeaseScreen`). Found by the flow-inventory audit (AUDIT-7) | **B2** |
| **GAP-69** | **F-7.2 — recording a partner payout has no caller.** `POST /api/partner-payout` exists; not named among B2's own six checklist screens. Found by the flow-inventory audit (AUDIT-9) | **B2** |
| **GAP-70** | **UC-75 "where is our cash" cannot say where the cash is — banked money leaves the report entirely.** `listPartnerCashPositions` computes `held = received − banked − advanced` ([queries/reports.ts](api/src/queries/reports.ts)); every subtraction is correct, but `cashPositionResponseSchema` is `{ partners[], depositsHeldMinor }` and has **no field for either subtrahend**. Bank 100,000 and it leaves the partner figure and appears nowhere. **UC-75 and FL §9.2 both require "held by partner *and account*" plus driver advances, at phase 1** — but DM §15's own query is headed *"UC-75 where is our cash — held per partner"* and the account block was never carried down from the use case. The implementation follows DM faithfully, so this is a **documents-travel-together break, and the `doc-change` on DM §15 comes before the endpoint changes.** Material exists: `banking_event.destination` is `text NOT NULL`, `advance.issued_by_user_id` already drives the subtraction. Target shape adds `banked: [{ destination, heldMinor }]` and `driverAdvances: [{ driverId, driverName, outstandingMinor }]`. **Until it lands, B4 titles the screen "Cash partners are holding"** — narrow but true. Found 7 Aug 2026 by the `B4-REPORTS-DESIGN.md` verification pass (§8.1) | **B4 Wave 2** |
| **GAP-71** | **UC-76's reason breakdown is phase 1, unbuilt, and UI §11.1 dropped it too.** UC-76: *"Shows: the count, the money it represents, **the reason breakdown**, and the weekday distribution"*; FL §9.2 phase **1**, *"with reasons and weekday distribution"*. `listLostDays` groups by `driverId` and weekday only and never reads the column. The data is **guaranteed present, not merely available** — `day_record.lost_reason` is `CHECK (lost_reason IN ('breakdown','driver_day_off','driver_ill','public_holiday','no_passengers','other'))` with `CHECK (state <> 'did_not_run' OR lost_reason IS NOT NULL)`. This is the report UC-06 calls *"your only protection"*, and reasons are what separate "a bus that breaks down often" from "a driver who takes Fridays off" — weekday distribution catches only the second. **UI §11.1's UC-76 row omits reasons and needs the same `doc-change`**, so fixing the query alone would leave the screen unspecified. Proposed shape: a sibling `reasons: [{ driverId, reason, lost, lostValueMinor }]` array rather than a denser cube (breaking change to `lostDaysResponseSchema`, cheap now — the only consumer is a test). Found 7 Aug 2026 by the `B4-REPORTS-DESIGN.md` verification pass (§8.2) | **B4 Wave 2** |
| ~~**GAP-72**~~ | ✅ **Closed 8 Aug 2026, and the first-draft fix was itself wrong — verified directly against Postgres, not assumed.** The suggested one-predicate fix (`created_at >= $from::date AT TIME ZONE tz`) resolves to a completely different overload than intended: `date AT TIME ZONE tz` upcasts the date to `timestamptz` at the *session's* zone first, then reinterprets that instant's wall-clock time in `tz` — `pg_typeof` even confirmed the result was `timestamp`, not `timestamptz`. Landed on `2026-01-01T11:30:00Z` for a Colombo midnight, not the `18:30:00Z` the fix needed. **Correct form casts to a bare `::timestamp` first** (genuinely naive, no zone attached) so `AT TIME ZONE tz` reads it as *that* wall-clock time *in* `tz`: `created_at >= $from::timestamp AT TIME ZONE tz AND created_at < ($to::timestamp + interval '1 day') AT TIME ZONE tz`. Caught because this repo's own "make it fail on purpose" rule was applied twice — the two new tests failed against the pre-fix code as expected, then **one kept failing against the first-draft fix too**, which is what surfaced the wrong overload rather than a passing test hiding it. 2 new integration tests: a waiver given at 8pm Colombo on the last day of a window, and one given 2am Colombo on 1 January counting toward that year rather than 31 December UTC | — |
| ~~**GAP-74**~~ | ✅ **Closed 8 Aug 2026.** `getPartnerSummary` gains `balanceMinor` — a new, separate all-time figure alongside the existing period-scoped `earned` (unchanged, still what "this month" reads). Computed by `sumAllTimeEarnedForUser`: a bounded loop over `listAccountingPeriodsForBusiness` (one row per month a real business has run, already built for A3), calling the same two reads the open-period `earned` figure already calls per period, summed. **The naive fix, exactly as scoped when this gap was sized** — not the eventual period-close snapshot, which needs a migration and a backfill and was deliberately left for later, on the condition (also met) that it must not gate this. `balanceMinor = putIn.contributionsMinor + putIn.outOfPocketMinor + allTimeEarned − takenOut.payoutsMinor − takenOut.settlementsMinor`, `holdingMinor` never netted in (W-2). 2 new integration tests, one verified to fail against the pre-fix code first (a closed June's profit share plus an open July's, proving the loop actually crosses a period boundary — the whole point of the gap) | — |
| **GAP-73** | **Goodwill windows on the wrong date entirely, and returns no breakdown.** Two halves of the same UC-77 work, both deferred with its screen. **(a) Wrong date basis:** `created_at` is when the row was *inserted*, not when the waiver was *given* — U-8 is explicit that "any record can be entered for a past date", so a waiver given 15 June and entered 2 July lands in July, and in the wrong year if it straddles one. **Same class as GAP-56**, the borne-by date bug already treated as a real money defect. `adjustment` has no business-date column (only `created_at` and `posted_period_id`), so the fix is a forward-only migration plus a backfill decision — or windowing on `posted_period_id`, which is consistent with W-40 but turns UC-77's "per year" into a set of period ids. **(b) No breakdown:** UI §11.1 asks for "Single number + a table by reason"; `goodwillResponseSchema` is `{ totalMinor }` and the query has no `GROUP BY`. Group by `adjustment_type` (a real enum) rather than the sparse free-text `reason`. Also unenforced: nothing in the DDL ties `adjustment.sign` to `adjustment_type`, and the sum ignores `sign`. **Distinct from GAP-72**, which is the comparison being wrong for the date it already uses. Found 7 Aug 2026 by the `B4-REPORTS-DESIGN.md` verification pass (§8.3b) | — |
| ~~**GAP-75**~~ | ✅ **Closed 8 Aug 2026 (B14).** `receivableRow` now renders `amountMinor`, matching `LeaseHubScreen.tsx:247`'s own pattern for the identical schema. Both tests that had pinned the bug (asserting `/Due.*Rs 0/` as correct) inverted to assert the real figure | — |
| ~~**GAP-76**~~ | ✅ **Closed 8 Aug 2026.** `driverFormSchema.name` and `vehicleFormSchema.registration`/`vehicleType` all gained a `.min(1, "…")` message — "Name is required", "Registration is required", "Vehicle type is required" — matching `CreateCustomerForm`'s existing pattern. Re-confirmed by the 8 Aug rerun before the fix, then fixed the same session. A test per form asserts the exact copy and that zod's generic fallback never appears | — |
| **GAP-77** | **A vehicle's trip history is unreachable, and the read endpoint doesn't exist.** `vehicle.ts`'s route-defs have calendar/document/expense/lease/incident/daily-lease — no trip. An arrangement-C vehicle's booked/closed/cancelled trips have nowhere to be seen from its own detail screen. Found by the 8 Aug QA browser pass (P1), confirmed worse than reported | **A16** |
| ~~**GAP-78**~~ | ✅ **Closed 8 Aug 2026.** `TwoBalances`' `driverName` prop is now optional and the heading renders only when given; `DriverDetailScreen` stops passing it, since `Screen`'s own title already names the driver. Its own test had asserted the duplication as correct ("appears twice… the same header/body duplication every screen already has") — corrected to assert once, and to name why `VehicleOverviewScreen`'s superficially similar "Registration: CAB-1234" is a different, legitimate shape (a labeled data row beside Type/Arrangement, not a repeated bare heading) rather than a second instance of the same bug | — |
| ~~**GAP-79**~~ | ✅ **Closed 8 Aug 2026.** "No description recorded" → "Incident with no description", both call sites (`VehicleOverviewScreen`, `IncidentScreen`) — no richer field exists on `incidentResponseSchema` to surface as extra context; date and status are already rendered alongside as siblings, which is the operational context the finding asked for | — |
| ~~**GAP-80**~~ | ✅ **Closed 8 Aug 2026.** The three `NotAvailable reason` strings replaced: `"advances aren't shown here yet"` and `"photo capture isn't available yet"` (×2), matching the primitive's own doc-comment voice ("no closing odometer" — a lowercase fragment, not a sentence). **Not fixed, and recorded rather than silently expanded into**: `StartLeaseScreen.tsx:427` and `CloseLeaseScreen.tsx:375` carry the same shape with worse copy — `"condition photos need presigned upload storage, not yet built (W-30)"` — found while grepping for every `NotAvailable reason=` in the client to confirm GAP-80's own three were the only matches. `W-30` is a citation to `use-cases.md`, not an internal phase codename, so it isn't the same defect GAP-80 named, but "presigned upload storage" is bare implementation vocabulary in user-facing copy either way. No gap id yet | — |
| ~~**GAP-81**~~ | ✅ **Closed 8 Aug 2026 (Plan.md 10b).** One shared `ExpenseCostRow` (`web/src/features/costs/`) replaces the identical inline `<Card>` block `VehicleOverviewScreen`, `TripDetailScreen` and `IncidentScreen` each carried independently — a live row is tappable, opening `VoidExpenseSheet` (reason required, `POST /api/expense/{id}/void`); a voided row stays in place, struck through, un-tappable (INV-21, "never voided twice"). Gated on `dailyOperations` server-side, the same capability that already gates every screen these rows render on, so no client-side `<Can>` was needed — matches `createExpenseHandler`'s own doc comment ("same capability expenses are already grouped under"). **Wiring three screens at once surfaced two more stale copies**: `TripDetailScreen.tsx` and `IncidentScreen.tsx` each carried their own hand-duplicated `EXPENSE_CATEGORY_LABEL` map, byte-identical to the shared `lib/expenseCategoryLabels.ts` — both deleted in the same commit rather than left to drift. 7 new tests across the two new files, `npm run check` clean | — |
| **GAP-60** | **A void and its replacement are not linked, so "what corrected this?" has no answer in the data.** `expense` carries the `voided_*` trio and W-50's void-and-replace is the prescribed correction path, but **there is no `replaces_id`/`corrects_id` column anywhere in the schema** (`grep`ped `api/src`, `api/migrations`, `packages/shared` — the only hit is [queries/expense.ts:67](api/src/queries/expense.ts#L67)'s own comment saying the column does not exist). So the corrected expense and its correction are two unrelated rows that happen to be near each other in time. UC-97/F-8.6 ("who changed what, readable from the record itself") is satisfiable for a *payment* — `payment_correction` is a real table — and not for the twelve other money tables W-50 covers. **Distinct from GAP-12**, which is about void endpoints not existing; this is about the linkage being absent even where voiding works. Relevant to A9b, which will build ~15 void paths and would otherwise build all fifteen without it. Found by `MUST-FIX-FINDINGS.md` (backend edition) §11, confirmed 7 Aug 2026 | **A9b** |
| **GAP-55** | **The "Add a driver" form's Name field has no `autocomplete` attribute.** Flagged live by Chrome's own accessibility/best-practice check (`msgid=8`, "An element doesn't have an autocomplete attribute") while re-verifying GAP-50 on `qa.fleetsettle.com`, 7 Aug 2026. Minor — doesn't block submission, doesn't fail a screen reader — but real and cheap; fits the same bucket B9 already collects small, independently-found UI nits into | B9 |

### Recorded, unowned, and correct to leave

Each is unreachable, unbacked by the schema, or genuinely out of scope. Listed so the absence stays a decision.

| id | Gap |
|---|---|
| **GAP-2** | F-4.3's effective-dated rate change and F-4.6's bulk week-confirm — real features the size of F-4.2 itself. Several screens' "current rate" simplifications are safe **only** while this stays unbuilt |
| **GAP-5** | `recordPayment`'s surplus comes back as `unallocatedMinor` but is never applied forward against a future due |
| **GAP-6** | F-8.4's deposit-apply — reusing `allocateAgainstOldest` verbatim would wrongly create a `payment` row for money already held |
| **GAP-7** | Trip `hold` (ST-5) and `in_progress` are unreachable — `bookTrip` writes `booked` outright. `VehicleCalendarScreen`'s hold-outline cell and `T?` glyph render and are tested but cannot occur |
| **GAP-8** | A genuine concurrent double-close race is unguarded (status-based idempotency only), the same accepted gap billing-period's wrapper has |
| **GAP-11** | W-11's "hidden unless enabled" has no backing settings field anywhere in the schema — the claim action is always offered |
| **GAP-14** | Stacked partial corrections against one payment are allowed by schema and implementation; only the single-correction path has a test |
| **GAP-17** | The photo pipeline still runs on the main thread — no Worker + 3s-timeout wrapper |
| **GAP-18** | UC-73 (yearly) and UC-99 (export) — both product-phase Second |
| **GAP-19** | UC-79's `revenuePerAvailableDayMinor` — needs its own date-range-scoped earned calculation |
| **GAP-20** | `isPatternDay`'s "alternate" reference point is a judgment call (lease `effective_from` = day zero); F-1.7's skippable-individual-days has no column to hold an exception |
| **GAP-21** | One business timezone for every business — `scheduled.ts` calls `businessToday()` once |
| **GAP-25** | Nothing writes `effectiveTo` on a `daily_lease` — no closure flow for arrangement B the way F-2.6 exists for A |
| **GAP-26** | "Off the road" is not rendered on the calendar — no write path marks a vehicle-day unavailable outside an allocation |
| **GAP-27** | Arrangement C's orange reuses `--color-serious` — a colour match, not a semantic one. Worth a real token, not a raw hex |
| **GAP-28** | Error monitoring deliberately deferred. Tail Worker needs a real alert destination; Sentry needs an account and DSN. Workers Logs (`observability.enabled`) stays the only observability **until there is an on-call person and a channel to page** — revisit then, rather than build an alert that pages no one |
| **GAP-34** | Fuel fill's U-3 prefill ("the vehicle that has something pending") degrades to "first vehicle in the business's list" — nothing in this schema tracks which vehicle a manager last touched, the same simplification `HomeScreen`'s most-recently-used already made |
| **GAP-15** | F-8.4's "deduct it from his fee" — `POST /api/offset` applied afterward, never a combined one-tap path. **Moved here 8 Aug 2026 from Scheduled/B3**, where it sat on the P10 backend's existence rather than on any phase reading: F-8.4 is *Phase: 2* and UC-91 is phase **Second**. Travels with `WriteOffSheet`/`PostClosureChargeSheet` (F-8.3/UC-90, same phase), withdrawn from the queue the same day |
| **GAP-68** | F-3.5's predictive half — "system uses history + odometer to prompt next time" for scheduled maintenance. The base recording action is already covered by the generic expense form (`servicing`/`repairs` categories, GAP-30/A8 covers the odometer link); this is the missing prompt engine specifically, a real feature rather than a form. Found by the 8 Aug flow-inventory audit (AUDIT-8) |

---

## 5. The traps

Things that cost a session each when rediscovered.

**Testing and environment**

- **`npm run check` does not run the api integration suite.** `api`'s `test` is `vitest run --project unit`; `test:integration` is separate and needs a live Neon branch. A green `check` says nothing about endpoint behaviour.
- **`mintUser(db, ctx, businessId, role)` takes the role as an explicit argument everywhere it is called — which means no test in this codebase has ever exercised the role a real signup actually gets.** GAP-42's bug (`createBusiness` granting `owner`, the one role with no screen) sat behind 386 green integration tests for exactly this reason: every one of them minted its `owner`/`manager`/`driver` directly rather than going through `POST /api/business` and reading back what it assigned. `business.test.ts`'s own happy-path test does call the real endpoint — and had asserted the wrong role as correct, so even the one test positioned to catch this had been pinned to the bug (the same shape as A9a's `voidExpense` regression test). Worth a standing question on any new setup/provisioning path: is there a test that reads back what the system decided, or only tests that assume a value and mint it directly?
- **The Neon branch has a connection ceiling.** Every integration file opens its own `Pool` and holds it to `afterAll`, so file-level parallelism opens all of them at once — `vitest.config.ts` sets `fileParallelism: false` for exactly this. On one long-lived interactive branch it still surfaces as `Connection terminated unexpectedly` at a **different, random** test each run. **A failure that moves between runs, in files the change never touched, is the environment.** Re-run the touched file alone before believing a regression. Web-P6d, Web-P7, Web-P8a and Web-P8b each hit it; P8a's session saw 15 failures across 7 untouched files, P8b's saw 2 in `auth.test.ts` that passed 9/9 alone.
- **A dedicated Neon branch plus `TEST_PARALLEL=1` cuts a full local `test:integration` run from ~29 minutes to ~105 seconds** — a ~16x speedup, measured both ways on the *same* branch (`test-parallel`, `br-lingering-violet-afr5li7f`, forked from `main`) so nothing else differs. `api/.dev.vars`'s `TEST_DATABASE_URL` now points at it; the old shared `test` branch is unused, not deleted. The env var sets `fileParallelism: true` with `poolOptions.threads.maxThreads: 4` (`vitest.config.ts`) — bounded well under the ~14-file ceiling above, so it doesn't reintroduce that failure mode. Opt-in and off by default; CI and anyone who hasn't set up a personal branch are unaffected.
- **The connection flakiness above can also surface as a foreign-key violation mid-cleanup, not just a raw timeout.** Found running `TEST_PARALLEL=1` twice in a row: run one failed 6 tests in `mileage-assessment.test.ts` (`billing_period_lease_id_fkey`), run two failed 2 in `day-record.test.ts` (`day_record_daily_lease_id_fkey`) — different file, different constraint, different count, both files 25/25 re-run alone. Same "moves between runs" signature as above, not a cleanup-ordering bug — checked `trackCreatedLease`'s teardown before concluding that, and it's correctly written (re-queries and deletes child rows before the parent, one sequential closure, no bug found). A dropped connection mid-closure can still leave a delete half-done; the fix is the one already established above, not a change to test support.
- **`audit_log` no-ops every `DELETE`** (`DO INSTEAD NOTHING`, migration `0001`). An `app_user` is permanent from the moment anyone acts. Test cleanup must not try — it will compile, run, and silently do nothing. The fix lives in `tests/support/auth.ts`/`factories.ts`; production only ever revokes `business_member`.
- **17 ESLint warnings are expected**, all `react-refresh/only-export-components` in `router.tsx` (15, one per route component — 14 until B10 added `StartDailyLeaseRoute`), `ApiContext.tsx` (1) and `AuthActionsContext.tsx` (1, added in B0 — the same context-with-a-hook shape as `ApiContext.tsx`). A direct consequence of the code-based-router decision plus that shared pattern. **The count rises by one with every new route and that is correct**; 0 *errors* is the bar. Do not "fix" these by splitting the route tree or the contexts.
- **`eslint-plugin-jsx-a11y` is deliberately absent** — its published peer range caps at ESLint 8/9 and this repo is on ESLint 10. **axe-core in Playwright is the a11y gate** until it catches up.
- **Neon's Free plan rejects an explicit `suspend_timeout` on branch creation outright** — a `412` with no error body surfaced by `create-branch-action`, even though the value passed (300s) matched the plan's own fixed default exactly. Confirmed against Neon's own plans doc: *"Can I disable scale-to-zero? Free: No, it's always enabled (5 min idle timeout)"* — the setting isn't configurable via the API on this plan at any value, matching or not. Cost three failed CI runs before the parameter itself was suspected (the first three failures were a separate, missing-secrets cause). Fixed by omitting it from `integration.yml` entirely; Free already suspends at 5 minutes regardless, so nothing is lost.
- **The free plan caps a project at 10 branches and starts failing branch creation around nine open PRs**, on *someone else's* PR, for a reason that looks unrelated (IG §9.2) — why `integration.yml` is one workflow, not two. Delete a hand-created scratch branch (a `migrate-check-*` from testing a migration manually) once its job is done rather than leaving it as permanent headroom loss.
- **`2822193` shipped with `npm run typecheck` broken** — `api/vitest.config.ts`'s `process.env.TEST_PARALLEL` trips `noPropertyAccessFromIndexSignature` (`tsconfig.base.json`), TS4111, since the index-signature access needs bracket notation (`process.env["TEST_PARALLEL"]`). `TEST_PARALLEL=1` had been run and verified, but plain `npm run check` apparently hadn't, on this one file. One-line fix; found and fixed at the start of A2 before `check` could pass at all.
- **Nothing pushed straight to a feature branch (`build/p0-foundation` included) has ever run through CI.** `checks.yml` and `integration.yml` both trigger only on `pull_request` into `develop`/`main`; a bare `git push` to any other branch triggers neither. Every gate result recorded in this document through B0 was a local run substituting for that, not a CI-confirmed one. **Changed 5 August 2026 (A9a):** stop running the full local integration suite at all — run only the touched file locally, push, and open a PR into `develop` so `integration.yml`'s fresh-ephemeral-branch-per-PR run carries the full-suite cost instead, in parallel with the next item. It is also strictly better at the one thing the shared branch keeps failing at: A9a's own verification, run against a throwaway branch created via the Neon MCP tools rather than `api/.dev.vars`'s long-lived one, came back **378/378 in one pass with zero connection flakiness** — the failure mode above is specifically a contention problem, and a branch nobody else is using has none.
- **The `api` integration count recorded in §1's gate table had drifted to a stale `388`** before A9a touched anything — corrected to the verified **378** (ground-truthed two ways: the full CI-equivalent run above, and a plain `grep -c` across every `tests/integration/*.ts` file). Worth a reminder that a number in this document is only as good as the run that last produced it; re-verify rather than carry one forward on trust when a full run is available anyway.
- **Mocked/stub-mode review cannot see `AuthGate` or `FirstRunGate`'s non-`AppShell` states, because the mock replaces the exact code path in question.** `VITE_AUTH_MODE=stub` — used by every test file and by `UI-UX-REVIEW.md`'s own Playwright pass — skips straight past sign-in. GAP-49 (6 Aug, live testing) lived on precisely that boundary and had already cost one near-miss: B8's own writeup flagged "one real browser round trip still unexercised" as an open risk and left it open. **Standing rule: anything touching `AuthGate`, `FirstRunGate`'s non-`renderOperate` branches, or any component that can render outside `AppShell`, needs one real-browser check, both colour schemes, before being called done.** Not a preference — mocked review is structurally incapable of catching this class.
- **Nobody had been reading the browser console as a standing step of any review, automated or manual.** It surfaced GAP-50 (a real `aria-hidden`-on-focused-ancestor WCAG violation in `Sheet.tsx`, live and reproducible) in under a minute, on the second sheet interaction tried. Cheap enough that there is no excuse to skip it — check console warnings/errors alongside any screenshot-based review from now on.
- **A route-def-by-route-def, screen-by-screen validation pass proves what exists matches spec — it cannot notice a flow that was never wireframed *and* never built**, because there is nothing on either side to diff. That is exactly how GAP-51 (F-1.7, "Set up the daily lease") went unbuilt through every prior pass: it has zero mentions in `ui-ux-guidelines.md` and zero implementation in `web/src`, so no comparison ever flagged a mismatch. None of this project's validation passes to date have enumerated `user-flows.md`'s full phase-1 flow list top-down against both documents at once. That audit hasn't run yet; F-1.7 falling through whole is reason to suspect it isn't the only one.
- **A migration comment can be right about its own object and wrong about the conclusion drawn from it — and it reads as authoritative either way.** `0003`'s header says revoked rows are excluded "so a revoked manager (F-1.4) can be re-granted". True of `0003`'s own index; false of the outcome, because `0001`'s `UNIQUE (business_id, user_id)` one migration above still blocks exactly that (GAP-52). The comment never looked up. **This is the third instance of the pattern §6 already names twice** — a stated reason that was accurate about the thing it examined and wrong about what it concluded. The habit that catches it: when a comment claims a *behaviour* is possible, check the other constraints on the same table, not just the one being added. It survived because nothing ever exercised the path — no endpoint has ever written `revoked_at`, so the claim was never tested against Postgres.
- **An accessibility assertion that watches for browser console warnings is worthless without `Accessibility.enable`, and it fails in the passing direction.** Headless Chromium computes the a11y tree lazily; with no client asking for it, checks like `aria-hidden`-on-focused-element never run and the warning is never emitted. The first version of `e2e/sheet-a11y.spec.ts` passed cleanly against code that was definitely broken. The fix is one CDP line (`await session.send("Accessibility.enable")`), but the habit that matters is the one that caught it: **before trusting a test that asserts the absence of something, make it fail on purpose.** Here that meant firing the exact violation by hand on `about:blank` and confirming the harness saw it — nothing, then two entries once a11y was enabled. Note also that `page.on("console")` alone is fine *once a11y is on*; the missing piece was never the listener.
- **`date AT TIME ZONE tz` and `timestamp AT TIME ZONE tz` are different overloads that produce different, silently-typed results — and "make it fail on purpose" catches a wrong fix, not only a missing one.** GAP-72's own suggested fix — `created_at >= $from::date AT TIME ZONE tz` — looked right and was wrong: Postgres upcasts the bare `date` to `timestamptz` at the **session's** zone first, then reinterprets that instant's wall-clock time in `tz`, landing eleven-plus hours off (`pg_typeof` even reported the result as `timestamp`, not `timestamptz` — a second, independent tell). Casting to a bare `::timestamp` first (genuinely naive, no zone attached) is what makes `AT TIME ZONE tz` read it as *that* wall-clock time *in* `tz`. **Verified directly against Postgres with a throwaway query, not assumed from the docs or from how the fix reads.** The discipline this confirms: running the new test against pre-fix code is necessary but not sufficient — run it against the *first-draft fix* too, before trusting that fix. Here, one of two new tests kept failing against the first draft; only the second run caught it.
- **`npm run typecheck` has now shipped broken twice, both times because the gate wasn't run before pushing.** `2822193` was the first (`api/vitest.config.ts`, TS4111). A0 was the second: `NewBusinessMember.role` was typed as the literal `"owner"`, so changing the one call site to `owner_manager` did not compile — found 7 Aug, a day after A0 shipped, by running `npm run check` for an unrelated reason. **The shape is worth naming: a type narrowed to the single value its only caller happens to pass.** It reads as precision and behaves as a tripwire on the next change; `role` is a set, and typing it as the set (`BusinessMemberRole`) is both more honest and less brittle. Note that no *test* failed either time — `test` and `typecheck` are separate scripts, and a green test run says nothing about whether the branch compiles.
- **A QA branch is only a meaningful live-verification target when it reflects what's actually merged.** `git log` is authoritative here, not the tracker's own "done" rows — A0 (`17da20d`) has been sitting on `build/p0-foundation`, unmerged into `develop`, since it shipped, so QA spent 6 August still running the pre-A0 signup path. Live-testing against a stale environment risks the exact failure it's meant to catch, inverted: a real fix reading as still-broken. Check `git log develop` (or the deployed commit) before trusting a live QA result to mean what it looks like it means.
- **A local branch ref is not the same fact as its remote-tracking ref, and `git fetch` only updates the second one.** Checking "has X been merged to `develop`" by diffing against a local `develop` that hasn't been checked out in days answers a question about a stale copy, not about what's actually on the remote — the exact inversion of the QA-staleness trap directly above, one layer lower in the stack. Diff against `origin/develop`, or fast-forward local `develop` first, before concluding a merge hasn't happened.
- **Before calling a single-writer column a bug, ask what the column is *for*.** A `grep` showing one writer proves one writer; it does not distinguish "everything else forgot to maintain this" from "this is a standing configuration and only one flow was ever meant to change it." GAP-54's first draft got that backwards and then went looking for live data to confirm the wrong reading — finding some, because a vehicle configured for daily lease with no daily lease yet *looks* like a stale figure and is in fact the ordinary case. The one-line answer was sitting in `createVehicle`'s own doc comment the whole time ("F-1.2 is what ever closes it"). **The distinguishing question is whether the column is derived from other rows or declared independently of them** — a derived one that nothing recomputes is a bug; a declared one with a single deliberate writer is a design, and its gap (if any) is the missing *flow*, not the missing writes.
- **An accessibility-tree snapshot taken immediately after a click can render before the async data it depends on has resolved, and the gap reads as real state.** Live-tested 7 Aug: a snapshot taken right after clicking a sheet's close button appeared to show a driver record that hadn't existed a moment before — momentarily indistinguishable from having just created one by accident. The network log settled it: the one `GET /api/driver` request for that page load, timestamped *before* the interaction in question, already carried that row in its response body — the earlier "0" was the query's loading state rendered as a fallback, not its resolved one. **Before concluding a UI action wrote something, check the network log for the actual request, not just the next snapshot** — this is the same discipline TRACKER already keeps for a11y console warnings (§5, `Accessibility.enable`) and it generalises to any assertion made from a point-in-time render rather than the data source itself.

**Writing code**

- **A U-3 prefill that crosses a direction-of-money boundary is the one prefill that must never be written, and this project has now reached for it twice.** U-3 ("sensible defaults, pre-filled") is a good rule that becomes a money bug the moment the source field points the opposite way from the target. The live case: `driverDayFeeMinor` (`packages/shared/src/schemas/driver.ts:23`) is money **you pay him**; `dailyLeaseAmountMinor` is money **he pays you**. Prefilling the second from the first seeds a plausible, wrong, unremarked figure into every record created. **This is not a hypothetical — UC §2's UC-04 note records that v1.1 of the specification itself called both a "per-day rate", and renaming them apart was a deliberate correction.** Plan.md's B10 write-up then re-proposed exactly the merge v1.1 had been corrected for, six days after the vocabulary lock was written down; it was caught by reading the schema rather than by any check. **Standing rule: before wiring any prefill, name the direction of both fields out loud. If they differ, there is no default — leave it empty.** The vocabulary lock (UI §9.6) exists to make this visible in copy; nothing yet makes it visible in code, which is why it needs to be a habit.
- **A bare single-statement write to an audited table records `changed_by` as `NULL`.** `withActor` wraps `.transaction`, so a write that never opens one is invisible to it. P9 fixed nine such functions by wrapping them even where atomicity was not otherwise needed. **Any new write to a `posted_period_id`-carrying table must open a transaction.**
- **`payment.amount_minor` has `CHECK (> 0)`** — a full reversal can never zero it. `status = 'reversed'` carries "nothing here counts"; the wire response reports the true remaining figure separately.
- **`tailwind-merge` does not know this project's scale.** Any new `--text-*` or spacing token must be added to `theme.text`/`theme.spacing` in `web/src/lib/cn.ts` or it is silently dropped next to a real colour class. Regression test in `cn.test.ts`.
- **`ownership_share` can never hold two open (`effective_to IS NULL`) sets for one vehicle — a second `POST /api/ownership-share` for a vehicle that already has a set always 400s.** `assert_shares_total` (migration 0001) sums every row whose date range contains the *new* row's own `effective_from`; a still-open first set's `[effective_from, ∞)` range contains any later date, so the total hits 200% and the trigger rejects it before a second set can ever exist. **This means `effective_to IS NULL` alone is a correct and sufficient "current split" filter** — not a coincidence, the deferred constraint's whole job — and a test that tries to `POST` a second set to prove otherwise needs a direct `db.insert` instead (found writing A2's `listOwnershipShares`, `partner.test.ts`'s own comment records the reasoning).
- **A `GENERATED ALWAYS` column reads back nullable through Drizzle even though Postgres never actually leaves it null.** `banking_event.discrepancy_minor` (`recorded − counted`, migration 0001) is one; a raw `SELECT` of it types as `bigint | null` in Drizzle regardless of the `NOT NULL` guarantee the two source columns give it. Defaulting the gap with `?? 0n` is exactly the W-56 pattern the lint rule exists to catch (and it caught it) — compute the figure from the two real columns instead, the same way `recordBankingEvent` (domain/partner.ts) already does on write, rather than trusting the generated column on read.
- **"A row exists for this idempotency key" is not the same fact as "this action already happened", the moment something *other* than the action itself can create that row (GAP-3).** `confirmDay` was idempotent on `(daily_lease_id, business_date)` since P3, correctly, right up until P13 added a second writer (`generate-day-cards`) that inserts the same row in a placeholder `open` state before the user ever acts. The idempotency check kept matching on existence alone and started silently no-op'ing real taps against real money. The fix — and the pattern for the next table that grows a second writer — is to gate the no-op on the row's own **state**, not its presence: a row in a non-terminal/placeholder state is the write path (an `UPDATE`, guarded on that same state in the `WHERE` clause so a concurrent racer's write affects 0 rows instead of throwing), and only a row already in a terminal state is the genuine no-op. Worth checking any other `find-then-return-if-exists` idempotency guard against whether a cron can now produce that same row ahead of the user — `day_record` was one of at least two tables (`vehicle_day_allocation` is the other, cron-only, no user-facing write competes with it today) P13 turned into a second writer.
- **A new invariant trigger firing on `INSERT` "because the reference implementation does" is a copy of the wrong half of the pattern.** `assert_business_has_owner()`'s (A11/INV-31) first draft matched `assert_shares_total`'s exact shape — `AFTER INSERT OR UPDATE`, deferred — and broke roughly a dozen existing integration-test fixtures that mint a `manager` as a business's sole member, because an `INSERT` can only ever *add* a row and can therefore never by itself take an active-owner count to zero; only an `UPDATE` that sets `revoked_at` can. `assert_shares_total` genuinely needs `INSERT` (a 60/40 split is built from inserts), which is exactly why copying its trigger *shape* without re-deriving which statement types can actually violate the *new* invariant produced a check that rejected valid, unrelated writes for no protective benefit. Ask "which statement type can actually cause the bad state" before choosing `INSERT`, `UPDATE`, or both — never default to "both" because a nearby trigger does.

**Three form bug classes, fixed structurally — every new form inherits the fix**

Found in P2 and fixed once each, at the structural level rather than per-form. A new form that hand-rolls around any of them is reintroducing a solved bug:

1. **Wire/domain mismatch on any zod schema with `.transform()`** (`moneyWireSchema`, `businessDateSchema`) — its `z.infer` output type differs from its `z.input` wire type, but `MoneyField`/`DateField` produce and consume the *domain* type. The fix: a form-local schema built from `z.custom<Minor>`/`z.custom<BusinessDate>` with no transform, keeping form state in domain types throughout, converting to wire via `toWire()` **only inside `mutationFn`**.
2. **`""` vs `undefined` on optional text inputs** — a native `<input>` always yields `""` when untouched, but `z.string().min(1).optional()` only skips validation for `undefined`. Fixed once in `lib/optionalTextField.ts`'s `blankToUndefined`, spread into `register(name, …)`.
3. **A cross-field error hidden inside a collapsed `Disclosure`** — W-55's error attaches to a level-2 field, invisible on a failed submit. Fixed by `Disclosure`'s `forceOpen?: boolean`, driven by `errors.fieldName !== undefined`.

**Migrations** — forward-only and numbered. `0001` DDL · `0002` audit trigger · `0003` single active membership · `0004` `business_id` on seven audited child tables · `0005` odometer `(lease_id, read_on)` partial unique · `0006` **`assert_period_open()` only fires on `INSERT` or an `UPDATE` that actually changes `posted_period_id`** — settling a claim or obligation months later is legal and was being rejected · `0007` `expense.voided_by` · `0008` **narrows `0006`'s exception to exclude a void** (`voided_at` `NULL → NOT NULL`) — GAP-35, A9a · `0009` widens `obligation_kind_check` to admit `trip_fare` — A6 · `0010` **`business_member`'s pair-unique made partial** (GAP-52), an explicit audit trigger (GAP-53), `business_member_invite`, **INV-31** (`assert_business_has_owner()`, `AFTER UPDATE` only — see §5 above for why not `INSERT`) — A11. Run `npm run check:drift` against both databases after any of them.

---

## 6. What validation corrected

1. **Root `CLAUDE.md` is materially wrong.** It says *"Repository state: documentation only. No application code exists yet."* Three workspaces, seven applied migrations and 352 passing tests in the gate later, this is the first thing a new session reads and it is false. **Not fixed here — it is not this file's to change** (the `doc-change` skill owns it), but it should be the next one-line commit.
2. **Web-P8b's own entry reported 77 web test files; the gate reports 74.** Its test *count* (264) and its delta (3 new files, 10 new tests) are both right — 71 + 3 = 74. Corrected in §3.
3. **GAP-22 had drifted out of the plan entirely.** The Web-P4 entry deferred the customer screen to Web-P6; Web-P6 shipped in four parts without it and the plan never named customers again. Now tracked with a track against it.
4. **`GET /api/expense` shipped with no caller** (GAP-33), deliberately and for a documented reason — §3.3 has no business-wide costs route, and `docs/` outranks the plan. The plan's `ExpenseListScreen` was withdrawn rather than built.

### A second pass, 5 August 2026 — validating A2/A3/B2/B3 before building them

Read route-def by route-def and screen by screen again, this time only over the four items about to be built. Three more corrections, all of them things the plan asserted and the code contradicts:

5. **`/more` is a placeholder, and it is the only door to `/cash`, `/partners/:id`, `/reports` and `/period/close`.** Neither B2 nor B3 mentioned it, and both are unreachable without it. Now **GAP-37**, promoted to its own Track B item (**B0**) because two items depend on it and neither owns it.
6. **`GET /api/payment` does not exist.** `POST /api/payment/{id}/correct` has been built and tested since P9 and **no interface can reach it** — there is no list query and no route. F-8.2 is unbuildable as specified. Now **GAP-38**, added to A3, which had named only write-offs and a period list.
7. **GAP-13's standing reason expired at P13 and nobody re-checked it.** "Indistinguishable from a day the pattern never scheduled" was true before day cards were generated; it is one `COUNT(*)` now. It had been deferred through four phases on a reason that stopped being true in the middle of them. **A gap's reason is a fact with a date on it** — this is the second time re-reading one changed the answer (GAP-23 was the first), and it is worth doing deliberately rather than accidentally.

Also settled rather than corrected: **F-7.6's host** (A2's one open decision — a composed `GET /api/partner/{userId}`, not a tenth report) and, with it, **GAP-4** — closed by deriving from `expense.paid_by_user_id` at read time rather than writing a current-account entry, because the entry would be a new money table and a `SUM` cannot drift from its own source.

### A third pass, 6 August 2026 — validating `UI-UX-REVIEW.md`'s own findings before scheduling them

That review is itself independent of this tracker's accounting (its own header says so), which is exactly why its claims got the same treatment as any other input rather than being copied straight into §4. Two of its eight remaining findings don't survive a source check:

8. **§7.3's `DialogConfirmFooter` claim is wrong.** It asserts the component "defaults its primary button text to exactly the forbidden word (`\"Confirm\"`) if a `confirmLabel` isn't provided." `Dialog.tsx:65` declares `confirmLabel: string` with no default value — omitting it fails to compile. There is no fallback to catch, forbidden or otherwise.
9. **§7.2's "Excess rate per km" claim misreads M-25.** §9.6's vocabulary lock bans "rate" as a synonym for exactly two words — "Daily lease amount" and "Driver day fee" — because those two are opposite directions of money that could otherwise both collapse to "rate." "Excess rate per km" names a third, unrelated concept, and it's the specification's *own* term for it: `use-cases.md` W-16/W-19 and the official happy-path test script both use it verbatim. Relabelling it would contradict `docs/`, which outranks both this tracker and the review that raised the point.

The other five (GAP-44 through GAP-48, §4) held up and are now scheduled under **B9**. One (§4's nested-`Sheet`/`AmountPad` interaction) is neither confirmed nor rejected — it needs the manual browser check the review itself asked for, not a source read, so it stays unresolved rather than being guessed at either way.

### A fourth pass, 7 August 2026 — this session correcting its own finding, an hour after recording it

10. **GAP-54's first draft claimed a bug that does not exist, and the live data it cited was evidence of correct behaviour.** The draft said `vehicle_arrangement` "never reflects what actually happens to the vehicle" and offered QA's bus as proof: badge reading "Daily lease", `daily_lease` empty. **Both halves were wrong.** `vehicle_arrangement` is a *standing configuration* — "this vehicle's business model is daily lease" — not a derived state — "a daily lease is running right now." It is supposed to survive leases starting and closing; that is what effective-dating is for. A vehicle configured `B` that has not started its daily lease yet is precisely the vehicle B10's flow was built to serve, and B10 correctly offered "Start a daily lease" on it. The badge, the empty calendar and the gating were all correct. **What made the draft wrong was reading a single-writer `grep` result as a symptom and then recruiting live data to confirm it**, rather than first asking what the column is *for* — `createVehicle`'s own doc comment answers that in one line ("F-1.2 is what ever closes it") and was not read until after the claim was written down. The surviving finding — F-1.2 unbuilt — was the `grep`, not the live check.
11. **The same draft asserted an `audit_log` fact before checking it.** "`audit_log` doesn't discriminate" was written from inference, then verified against the branch (`trip` and `expense` are the only two tables present) and rewritten to say what the query actually showed. It happened to hold; the order was still backwards, and in a document whose whole value is that its claims were checked, a lucky guess and a verified fact are not interchangeable.

### A fifth pass, 7 August 2026 — validating `MUST-FIX-FINDINGS.md` before scheduling any of it

**Note on what was validated: `MUST-FIX-FINDINGS.md` existed in two editions on 7 August**, and this pass covers both. A **web edition** (13 findings, web/API/use-case alignment) was validated first; the file was then regenerated in place as a **backend edition** (12 findings, handlers/domain/queries) partway through. They overlap on four items and diverge everywhere else, so both are treated as inputs and cited by edition — the file on disk today is the backend one.

Both were treated the way `UI-UX-REVIEW.md`'s findings were: each re-checked against source rather than taken on trust. **Most were already tracked and correctly so** — web §2/backend §2 = GAP-10 and web §3/backend §3 = GAP-39, both already A10 · web §5 = B4/B5/B0b · web §8 = B7 · web §9 = GAP-16/A7 · web §10/§11/§12 = GAP-44/46/47, all already B9 · backend §4 = GAP-5 · backend §7 = GAP-21 · backend §9 = GAP-8 · backend §10 = GAP-7, the last four all already in "correct to leave" with their reasons intact. **A review independently rediscovering this tracker's own queue, twice, from two different angles, is a reasonable signal the queue is pointed at the right things.**

**Five findings were new and are now GAP-56 through GAP-60** (§4 above): the borne-by date bug (both editions' §1, and the only new *money* defect either produced), the stale trip-receivable UI (web §4), the empty test manifest (web §13), cross-entity id coherence (backend §6), and void-and-replace linkage (backend §11). **One changed an existing gap's status rather than adding one** (GAP-1, via A11). And one does not hold up:

12. **§6's "`/more` only contains sign-out" is true and is not a defect.** B0 shipped that screen on an explicit **rows for what exists only** rule, recorded when it landed: Cash arrives with B2, Reports with B4, Close the month with B3, each adding its own row rather than the hub pointing at a placeholder. The finding reads a deliberate decision as an omission. Its underlying priority claim — that the monthly workflows matter — is not wrong, but it is already the plan's own ordering (B3 carries the only hard date on either track), so nothing is scheduled from it.

**One correction to the finding this pass rated highest, worth stating because it splits an item rather than confirming it.** §1 bundles two claims: that arrangement transitions are not maintained, and that borne-by resolves from the current arrangement rather than the expense date. **They are not the same defect and only the second is a money bug.** The first is GAP-54 — a missing *flow* (F-1.2), where the single-writer column is by design (`createVehicle`'s own comment: "F-1.2 is what ever closes it"). The second is **GAP-56**, is reachable today with one lease and no party change at all, mis-assigns real money to a real person, and **was not previously recorded anywhere**. §1's own "required fix" leads with the arrangement half; the ordering should be inverted.

### A sixth pass, 7 August 2026 — "0b" turned out to rest on a fact nobody had checked

13. **"The existing production `business_member` row still reads `owner`" was written as an observed fact in the 6 August A0 entry, and it was never observed.** It was a reasonable inference at the time — the product had been reachable, so someone might have signed up — but nothing in that session's own account describes checking the production database, only reasoning about what *would* be true if someone had. Queued as "0b" on that basis: read the row, scope the fix by `id`, verify by sign-in. **Checked directly 7 Aug 2026, read-only, against Neon `main`:** `business_member`, `app_user`, `business` — all zero rows. Nobody has ever signed up in production. No `UPDATE` was written, because there was no row to scope one to. **The same shape as GAP-54's withdrawn first draft, earlier in this same pass**: a plausible claim, stated with the confidence of a checked one, that turned out to be checkable in about thirty seconds and simply hadn't been. The standing lesson from §5 already covers it — "the memory says X exists is not the same as X exists now" — except this was inference standing in for memory, which is the same trap wearing a different cause.

### A seventh pass, 8 August 2026 — validating the QA rerun, where most claims were re-confirmations and one was not a defect

14. **The rerun's P3, "Close the month can be opened while status shows all zeros", does not describe a defect** — it describes U-7, implemented on purpose. FL/UI §7.7's close checklist **warns and lists, it never blocks**: `CloseMonthScreen` renders all five counts and keeps the action enabled regardless of what they say, because a count that gates the button turns an honest warning into a wrong refusal (the cron may not have run for a date yet, so a zero can under-report). The rerun's own two recommendations are also already built — the confirm is a `Dialog` stating the consequence rather than the word "Confirm" (M-10), and success surfaces the newly opened successor period by name. **Recorded rather than scheduled**, per the standing rule that an external review is validated before it is believed. Its third recommendation — e2e coverage in a seeded environment where closing a period is safe — is real, and is why [LIVE-TEST-PLAN.md](LIVE-TEST-PLAN.md) puts LT-8 last rather than never.
15. **Everything else the rerun found was already filed**, which is the useful signal in it: GAP-76 (validation copy), GAP-77 (trip history), GAP-78 (duplicate heading), GAP-79 (incident fallback), GAP-80 (leaked phase references), GAP-81 (no client void path), GAP-36 (no archive/edit paths) — a second independent pass rediscovering the same queue is evidence it is pointed at the right things. **One finding was genuinely new and is GAP-83**, and it was worse than reported: a `DateField` problem read as an Opening-balances problem, because the pass sampled one screen and the component is shared. **It also confirmed B14 live** — a booked trip with an agreed amount now reads its real due figure instead of `Rs 0`, which is the first time a fix from this queue has been verified on QA by an independent pass rather than by its own author.

---

## Alongside this tracker

- **[LIVE-TEST-PLAN.md](LIVE-TEST-PLAN.md)** — the eight checks that can only be answered by a real browser against a real environment, collected in one runnable queue instead of scattered as prose across this file and [Plan.md](Plan.md). Ordered so no test contaminates the state a later one needs, with the write cost of each stated up front — two are one-way and one is irreversible. Plan.md schedules the whole file as **V1**. **Findings go through the same validation every external review gets**: confirmed ones become §4 rows with a gap id, rejected ones become §6 paragraphs.
- **[ROLES-AND-CAPABILITIES.md](ROLES-AND-CAPABILITIES.md)** — the five actors, the enforced capability matrix, and what is actually reachable in the client today, with the three put side by side. A reference, not a record: **if it ever disagrees with `api/src/auth/policy.ts`, `policy.ts` is right.** It exists because those three drifted once already and the drift was invisible until someone hit it in production (GAP-42).

## Not in this tracker

UC §9.1 phase Third and UI §15 phase Third, listed so their absence is a decision: depreciation and disposal · driver retainers and spare-vehicle reassignment · loan and lease schedules · tax, if it applies · offline capture of photos · the desktop analytical dashboard beyond UI §14's three changes.

## Blocked

| Blocked | Waiting on |
|---|---|
| **P14** — messaging | Twelve Meta template approvals (6 messages × 2 languages), minutes to ~2 days each. `dispatch-messages`, the fifth Cron Trigger, waits with it |

Nothing else on either track waits on anyone outside this repository.

**Read this before writing "blocked externally" against anything again.** Real Asgardeo sat in this table for months described as "~10 minutes; blocks nothing else." The console change *was* ten minutes. The client half — SDK, PKCE flow, callback, sign-in screen, a real token getter — was unbuilt, unsized, and blocked **everything**: nobody could log in to a deployed build at all. The label described the blocker's cost and said nothing about the work behind it, and it kept the item ranked last for exactly that reason. P14 carries the same label today; its Meta approvals are genuinely external, but `dispatch-messages` and six message templates are not, and no one has sized them either.

## One open question, still unresolved

UC §9.1's phase lists name incidents nowhere — not First, Second or Third — yet **G-2, "one accident", is a golden fixture that must reproduce**. This tracker assumes incidents are phase one, since a fixture no scheduled phase can satisfy is a gap rather than a plan. If that is wrong, UC §9.1 is the document to change, deliberately, with the reason recorded.

## Adding an entry

A finished item becomes **one row** in §2 or §3 — what it delivers, its proof with real counts, its gap ids — not a section. Anything it did not build becomes a row in §4 **with a track**. Anything that will cost a future session an hour to rediscover goes in §5. The narrative belongs in the commit message; this file is the index.
